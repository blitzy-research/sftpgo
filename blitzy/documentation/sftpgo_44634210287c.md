# How SFTPGo `0.9.5-dev` Comes to Life When Launched Without a Configuration File

**Server:** SFTPGo `0.9.5-dev` — version string from `utils/version.go:L3` (`const version = "0.9.5-dev"`), surfaced by the running process (see the "Environment & Canonical Build" section).
**Commit:** `44634210287cb192f2a53147eafb84a33a96826b`
**Scenario:** the server started with **no configuration file** via its canonical entry point `./sftpgo serve`.

This document is a runtime‑forensic walkthrough of how the SFTPGo server "comes to life" during startup when it is launched **without a configuration file**. It answers five questions — **Q1** (ports opened and readiness signals), **Q2** (what the logs show when an SFTP client presents a username that does not exist), **Q3** (what the web‑admin root endpoint `/` returns), **Q4** (what happens when the expected SQLite database is missing), and **Q5** (how the default configuration reveals itself in the logs). Every behavioral claim is paired with **real captured output** (the exact command that produced it plus the complete, unedited log/response) and a precise `file:line` citation into the source at the commit above. The investigation was performed **run‑first**: the binary was built and executed, the SFTP/HTTP entry points were exercised, and the output was captured **before** this document was written.

> **Observed vs. inferred.** Everything shown in the fenced output blocks below was **observed** — captured directly from a running build of this exact commit. There are essentially no inferred statements; the one place the word *inferred* would apply (the 0‑byte "invalid database" edge case) was in fact **exercised for real** and is therefore labeled **observed**. The machine‑dependent values — the cold‑build wall‑clock time and the RSA host‑key generation delay — are called out explicitly as variable, with the observed ranges reported across two runs each.
>
> **Timestamp convention.** In every JSON log line the timestamp is shown as `"time":"..."` — a redaction of the per‑line variable value. The real on‑disk format is `2006-01-02T15:04:05.000` (`logger/logger.go:L22`, `const dateFormat`), e.g. `2020-01-15T12:34:56.789`. The `level`, `sender`, `connection_id` (and other structured fields) and `message` fields are shown **verbatim**.

---

## Environment & Canonical Build

The toolchain below was provisioned **outside** the repository checkout purely to build and observe the binary; none of it is committed and none of the product source was modified. The exact versions were captured as follows:

```
$ go version
go version go1.13.15 linux/amd64

$ gcc --version | head -1
gcc (Ubuntu 15.2.0-4ubuntu4) 15.2.0

$ sqlite3 --version
3.46.1 2024-08-13 09:16:08 c9c2ab54ba1f5f46360f1b4f35d849cd3f080e6fc2b6c60e91b16c63f69aalt1 (64-bit)

$ ssh -V
OpenSSH_10.0p2 Ubuntu-5ubuntu5.4, OpenSSL 3.5.3 16 Sep 2025

$ sshpass -V | head -1
sshpass 1.10

$ curl --version | head -1
curl 8.14.1 (x86_64-pc-linux-gnu) libcurl/8.14.1 OpenSSL/3.5.3 zlib/1.3.1 brotli/1.1.0 zstd/1.5.7 libidn2/2.3.8 libpsl/0.21.2 libssh2/1.11.1 nghttp2/1.64.0 librtmp/2.3 OpenLDAP/2.6.10
```

| Tool | Observed version | Purpose |
|------|------------------|---------|
| Go | `go1.13.15 linux/amd64` | Build toolchain. `go.mod:L3` pins `go 1.13`; `.travis.yml:L8` pins `go: ["1.13.x"]`. |
| gcc | `15.2.0` | C compiler required by CGO for the default `mattn/go-sqlite3` driver. |
| sqlite3 CLI | `3.46.1` | Create/initialize `sftpgo.db` for the operational scenario (Q4). |
| OpenSSH client | `10.0p2` | Exercise the SFTP authentication flow (Q2). |
| sshpass | `1.10` | Feed a password non‑interactively to the SFTP client (Q2, Mode B). |
| curl | `8.14.1` | Exercise the HTTP web‑admin endpoints (Q3). |

> **Note on tool versions.** Only the **Go toolchain** (`go 1.13`) is pinned by the project (`go.mod:L3`, `.travis.yml:L8`). The C compiler, `sqlite3`, OpenSSH, `sshpass`, and `curl` are host utilities used only to build and probe the binary, so their exact versions are incidental to SFTPGo's observed behavior; they are reported here exactly as observed on the run host. (In particular, the OpenSSH client version matters for Q2 only insofar as any modern OpenSSH — 8.8p1 or newer — disables the legacy `ssh-rsa` signature algorithm by default; see Q2.)

**Build environment:** `GOPATH=/root/go`, `GOCACHE=/root/.cache/go-build`, `GO111MODULE=on`, `CGO_ENABLED=1`, with the source copied to `/tmp/sftpgo_build` and the binary built there (the binary is **never** built inside the tracked checkout, which has no `.gitignore`).

**Canonical build command** (CGO is required for the default SQLite driver, `github.com/mattn/go-sqlite3 v2.0.2+incompatible`, pinned at `go.mod:L15`):

```
$ cd /tmp/sftpgo_build
$ CGO_ENABLED=1 go build -o sftpgo .
```

The build completes with exit status `0`. The CGO step prints one **upstream** warning from the vendored SQLite C amalgamation (not from SFTPGo Go code):

```
# github.com/mattn/go-sqlite3
sqlite3-binding.c: In function 'sqlite3SelectNew':
sqlite3-binding.c:125801:10: warning: function may return address of local variable [-Wreturn-local-addr]
```

The resulting binary size was measured directly, and the cold‑build wall‑clock time (throwaway `GOCACHE`, warmed module cache) was measured across two runs to confirm stability:

```
$ stat -c '%s bytes' sftpgo
31877432 bytes
$ du -h sftpgo
31M	sftpgo

# two cold builds (throwaway GOCACHE each time; module cache /root/go/pkg/mod stays warm)
Run 1: exit=0  elapsed=42.9s
Run 2: exit=0  elapsed=45.7s
```

So the canonical build produces a self‑contained binary of **31,877,432 bytes (≈ 31.9 MB / 30.4 MiB)**; the observed cold‑build time is **~43–46 s** across two runs. Build time is machine‑ and cache‑dependent and is therefore reported as an observed range rather than a single fixed value.

**Version banner (exact):**

```
$ ./sftpgo --version
SFTPGo version: 0.9.5-dev
```

*Mechanism.* The banner text comes from the cobra version template set at `cmd/root.go:L63`, combined with the compiled constant `const version = "0.9.5-dev"` at `utils/version.go:L3` (surfaced through `utils.GetAppVersion()`). The template is:

```go
// cmd/root.go:L63
rootCmd.SetVersionTemplate(`{{printf "SFTPGo version: "}}{{printf "%s" .Version}}
`)
```

**Canonical entry point:** `./sftpgo serve`. `main.go:L14` calls `cmd.Execute()`; the `serve` subcommand (`cmd/serve.go`) constructs a `service.Service` and calls `Start()` then, only if `Start()` returns no error, `Wait()` (`cmd/serve.go:L29-L31`):

```go
// cmd/serve.go:L29-L31
if err := service.Start(); err == nil {
    service.Wait()
}
```

---

## Methodology

### Reproducing "no config file" faithfully

SFTPGo uses `spf13/viper` for configuration. `config.LoadConfig` builds the search path from three `AddConfigPath` calls: the configured config dir (`config/config.go:L147`, `AddConfigPath(configDir)`, default `.`), the two Linux‑specific paths added by `setViperAdditionalConfigPaths()` (`config/config.go:L148` → `config/config_linux.go:L8-L10`, which add `$HOME/.config/sftpgo` and `/etc/sftpgo`), and the current directory (`config/config.go:L149`, `AddConfigPath(".")`). The config **name** searched is `sftpgo` (extension‑less), from `DefaultConfigName = "sftpgo"` at `config/config.go:L25`. The search path therefore has the **form** `[<cwd> /root/.config/sftpgo /etc/sftpgo]`, where `<cwd>` denotes the current working directory; the concrete observed values — e.g. `[/tmp/run_A /root/.config/sftpgo /etc/sftpgo]` and `[/tmp/run_B /root/.config/sftpgo /etc/sftpgo]` — appear verbatim in the captured `config` WARN lines under Q4 and Q5.

Two consequences shape the methodology:

- `~/.config/sftpgo` and `/etc/sftpgo` were confirmed **absent**, and the run directory contained **no** `sftpgo.*` file — so viper genuinely finds no config file and falls back to the compiled defaults.
- The binary is invoked by an **absolute path from outside the run directory** (e.g. `/tmp/sftpgo_build/sftpgo serve`). If instead the extension‑less `sftpgo` **binary** itself sits in the cwd, viper matches it as a config candidate and reports a *different* error — documented as **Q5 variant (b)**.

### The two scenarios

- **Scenario A — empty directory** (no config, no DB): exercises the config‑not‑found fallback (Q5) and the missing‑database WARN + fast‑fail ERROR (Q4).
- **Scenario B — no config + a pre‑initialized `sftpgo.db` + `templates/` + `static/`** copied into the run directory: the only configuration that reaches a fully operational state, so it is used for full startup/readiness (Q1), the default‑config dump on a successful boot (Q5), the web‑admin root redirect (Q3), and the nonexistent‑user authentication failure (Q2). `templates/` and `static/` must be present because `loadTemplates` uses `template.Must` (`httpd/web.go:L78`, `httpd/web.go:L95-L98`) — the process **panics** if the templates are missing.

The database for Scenario B is created with the canonical CI initialization from `.travis.yml` (`before_script`, `.travis.yml:L14`); see Q4 for the exact command.

---

## Q1 — Which ports are opened, and what log messages signal readiness?

**Direct answer.** With no config file, once the database exists (Scenario B) the server opens **two TCP ports**: **`2022/tcp` for SFTP**, bound on **`[::]:2022`** (all interfaces), and **`8080/tcp` for HTTP (web admin + REST)**, bound on **`127.0.0.1`**. The SFTP subsystem announces readiness with the log line **`server listener registered address: [::]:2022`** (`sftpd/server.go:L187`). The HTTP subsystem has **no explicit "ready/listening" line**; its observable readiness signal is the init DEBUG `initializing HTTP server with config {...}` (`httpd/httpd.go:L76`) followed by a successful bind (i.e., the **absence** of a `could not start HTTP server` error that would otherwise come from `service/service.go:L105`).

### Startup control flow (cite)

`service.Start()` (`service/service.go:L47-L117`) runs synchronously: it initializes the logger, prints the startup banner (`service/service.go:L60`), loads configuration via `config.LoadConfig` (`service/service.go:L65`), and initializes the data provider via `dataprovider.Initialize` (`service/service.go:L69`). It then launches the **SFTP server as a goroutine** (`service/service.go:L91-L98`; the DEBUG `initializing SFTP server with config %+v` is emitted at `service/service.go:L92` with `sender:"service"`) and — **only if `httpdConf.BindPort > 0`** — the **HTTP server as a goroutine** (`service/service.go:L100-L109`). Inside the SFTP goroutine, `sftpdConf.Initialize(configDir)` (`sftpd/server.go:L117-L198`) sets the umask, auto‑generates the host key, binds the listener at `sftpd/server.go:L178` (`net.Listen("tcp", fmt.Sprintf("%s:%d", c.BindAddress, c.BindPort))`), and logs readiness at `sftpd/server.go:L187`.

### Command

```
# from the initialized Scenario B run directory (/tmp/run_B), binary invoked by absolute path
/tmp/sftpgo_build/sftpgo serve
```

### Exact ordered `sftpgo.log` (JSON) startup sequence — observed, verbatim

```
{"level":"info","time":"...","sender":"service","connection_id":"","message":"starting SFTPGo 0.9.5-dev, config dir: ., config file: sftpgo, log max size: 10 log max backups: 5 log max age: 28 log verbose: true, log compress: false"}
{"level":"warn","time":"...","sender":"config","connection_id":"","message":"error loading configuration file: Config File \"sftpgo\" Not Found in \"[/tmp/run_B /root/.config/sftpgo /etc/sftpgo]\". Default configuration will be used: {SFTPD:{Banner:SFTPGo_0.9.5-dev BindPort:2022 BindAddress: IdleTimeout:15 MaxAuthTries:0 Umask:0022 UploadMode:0 Actions:{ExecuteOn:[] Command: HTTPNotificationURL:} Keys:[] IsSCPEnabled:false KexAlgorithms:[] Ciphers:[] MACs:[] LoginBannerFile: SetstatMode:0 EnabledSSHCommands:[md5sum sha1sum cd pwd]} ProviderConf:{Driver:sqlite Name:sftpgo.db Host: Port:5432 Username: Password:[redacted] SSLMode:0 ConnectionString: UsersTable:users ManageUsers:1 TrackQuota:1 PoolSize:0 UsersBaseDir: Actions:{ExecuteOn:[] Command: HTTPNotificationURL:} ExternalAuthProgram: ExternalAuthScope:0} HTTPDConfig:{BindPort:8080 BindAddress:127.0.0.1 TemplatesPath:templates StaticFilesPath:static BackupsPath:backups}}"}
{"level":"debug","time":"...","sender":"sqlite","connection_id":"","message":"sqlite database handle created, connection string: \"file:sftpgo.db?cache=shared\""}
{"level":"debug","time":"...","sender":"httpd","connection_id":"","message":"initializing HTTP server with config {BindPort:8080 BindAddress:127.0.0.1 TemplatesPath:templates StaticFilesPath:static BackupsPath:backups}"}
{"level":"debug","time":"...","sender":"service","connection_id":"","message":"initializing SFTP server with config {Banner:SFTPGo_0.9.5-dev BindPort:2022 BindAddress: IdleTimeout:15 MaxAuthTries:0 Umask:0022 UploadMode:0 Actions:{ExecuteOn:[] Command: HTTPNotificationURL:} Keys:[] IsSCPEnabled:false KexAlgorithms:[] Ciphers:[] MACs:[] LoginBannerFile: SetstatMode:0 EnabledSSHCommands:[md5sum sha1sum cd pwd]}"}
{"level":"debug","time":"...","sender":"utils","connection_id":"","message":"set umask to 0022 (18)"}
{"level":"info","time":"...","sender":"sftpd","connection_id":"","message":"No host keys configured and \"id_rsa\" does not exist; creating new private key for server"}
{"level":"info","time":"...","sender":"sftpd","connection_id":"","message":"Loading private key: id_rsa"}
{"level":"info","time":"...","sender":"sftpd","connection_id":"","message":"server listener registered address: [::]:2022"}
```

### Port verification — closed before, open after, across two runs (observed)

The same probe was run **before** and **after** startup, for two independent boots. Before each start, neither port is listening; after the readiness line appears, both are. (The `pid=` value is the PID of that boot's process; it is the only field that differs between runs — the address/port columns are byte‑identical.)

**Run 1** — `pid=77343`:

```
# BEFORE start
$ ss -ltnp | grep -E ':2022|:8080'
$                       # (no output — 2022 and 8080 are both closed)

# AFTER the readiness line
$ ss -ltnp | grep -E ':2022|:8080'
LISTEN 0      1024       127.0.0.1:8080      0.0.0.0:*    users:(("sftpgo",pid=77343,fd=6))
LISTEN 0      1024               *:2022            *:*    users:(("sftpgo",pid=77343,fd=7))
```

**Run 2** — a second independent boot (host key deleted so it regenerates), `pid=78912`:

```
# BEFORE start
$ ss -ltnp | grep -E ':2022|:8080'
$                       # (no output — 2022 and 8080 are both closed)

# AFTER the readiness line
$ ss -ltnp | grep -E ':2022|:8080'
LISTEN 0      1024       127.0.0.1:8080      0.0.0.0:*    users:(("sftpgo",pid=78912,fd=6))
LISTEN 0      1024               *:2022            *:*    users:(("sftpgo",pid=78912,fd=7))
```

So both ports were **closed before start** and **open after** the readiness line in **both** runs, and the listening addresses (`127.0.0.1:8080` for HTTP, wildcard `*:2022` for SFTP) were identical run‑to‑run.

### Facts (each with citation)

- **`2022/tcp` (SFTP)** is bound on **`[::]:2022`** — all interfaces — because the default `BindAddress` is the empty string (`config/config.go:L47`), so `net.Listen("tcp", ":2022")` at `sftpd/server.go:L178` listens on the wildcard address (`ss` shows `*:2022`, and `listener.Addr().String()` renders it as `[::]:2022`).
- **`8080/tcp` (HTTP)** is bound on **`127.0.0.1`** — the default `HTTPDConfig.BindAddress` (`config/config.go:L89`). The bind happens in `httpServer.ListenAndServe()` with `Addr: fmt.Sprintf("%s:%d", c.BindAddress, c.BindPort)` (`httpd/httpd.go:L92`, `httpd/httpd.go:L98`).
- **SFTP readiness signal — the line to watch:** `server listener registered address: [::]:2022`, emitted by `logger.Info` at **`sftpd/server.go:L187`** (`sender:"sftpd"`). It is written to the JSON log file `sftpgo.log`, **not** the console.
- **HTTP readiness signal — the nuance:** there is **no** explicit "listening"/"ready" line for HTTP. The observable init DEBUG `initializing HTTP server with config {BindPort:8080 BindAddress:127.0.0.1 TemplatesPath:templates StaticFilesPath:static BackupsPath:backups}` is emitted at **`httpd/httpd.go:L76`** (`sender:"httpd"`); a successful bind is confirmed by the absence of a `could not start HTTP server` error (`service/service.go:L105`) and by the open `127.0.0.1:8080` socket above.
- **Host‑key generation delay/timing.** On first boot no host key exists, so `checkHostKeys` (`sftpd/server.go:L417-L430`) auto‑generates an RSA‑4096 key (`rsa.GenerateKey(rand.Reader, 4096)` at `sftpd/server.go:L465`) and logs `No host keys configured and "id_rsa" does not exist; creating new private key for server` (`sftpd/server.go:L422-L423`; the `%#v` verb renders the joined path `id_rsa` in quotes, from `defaultPrivateKeyName = "id_rsa"` at `sftpd/server.go:L27`; also mirrored to the console via `InfoToConsole`). The delay is the interval between that INFO line and the readiness line, computed directly from the JSON `time` field of each. The relevant raw log lines for the two runs were:

  ```
  # Run 1
  {"level":"info","time":"2026-07-13T18:12:04.002","sender":"sftpd",...,"message":"No host keys configured and \"id_rsa\" does not exist; creating new private key for server"}
  {"level":"info","time":"2026-07-13T18:12:09.974","sender":"sftpd",...,"message":"server listener registered address: [::]:2022"}
  # Run 2
  {"level":"info","time":"2026-07-13T18:14:12.959","sender":"sftpd",...,"message":"No host keys configured and \"id_rsa\" does not exist; creating new private key for server"}
  {"level":"info","time":"2026-07-13T18:14:15.357","sender":"sftpd",...,"message":"server listener registered address: [::]:2022"}
  ```

  The deltas are therefore **Run 1 = `18:12:09.974 − 18:12:04.002` = 5.972 s** and **Run 2 = `18:14:15.357 − 18:14:12.959` = 2.398 s**. This delay is **variable** — it is dominated by the RSA‑4096 prime search in `rsa.GenerateKey` — so it is reported as the observed range **≈ 2.4 – 6.0 s** rather than a single value. What is **stable** across both runs is the qualitative behavior: the readiness line `server listener registered address: [::]:2022` and both open ports (shown byte‑identical above) appear on every boot once key generation finishes.

---

## Q2 — What appears in the logs when an SFTP client uses a nonexistent username, and how does the server react?

**Direct answer.** It depends on how far the SSH handshake gets, so **both modes** are documented. The server's auto‑generated host key is a cryptographically strong **RSA‑4096** key (`rsa.GenerateKey(rand.Reader, 4096)`, `sftpd/server.go:L465`), but this SFTPGo version advertises it using only the **legacy `ssh-rsa` signature algorithm** (RSA keyed with **SHA‑1**), which every modern OpenSSH (≥ 8.8p1) disables by default. Consequently a **modern OpenSSH client (Mode A)** never reaches the username lookup at all: the connection is rejected during **key‑exchange host‑key negotiation**, with an **empty** username, and the data provider is never consulted — the client sees **`no matching host key type found`**, *not* `Permission denied`. When the client **opts into `ssh-rsa` (Mode B)**, the handshake proceeds to password authentication, the server looks up the username, the SQLite provider finds no matching row, and a **`RecordNotFoundError`** surfaces as **`Not found: sql: no rows in result set`**; **only in Mode B** does the client see **`Permission denied (password,publickey)`**. In **both** modes the server logs the failure and **keeps listening (no crash)** — after both failed attempts the process is still alive and both ports remain open (verified via `ss` and `kill -0` in Q1).

> **⚠️ Reproduction‑only security note.** The options below deliberately re‑enable a **deprecated signature algorithm** and disable host‑key checking **solely** to drive the server's authentication path on an isolated `127.0.0.1` during this historical‑commit investigation. **Never use them in production or in a global SSH client/server configuration.**
> - **The RSA‑4096 key size is *not* the weakness.** The auto‑generated host key is a strong 4096‑bit RSA key (`sftpd/server.go:L465`). What is deprecated is the **`ssh-rsa` *signature algorithm*** — RSA signatures computed with **SHA‑1** — which is the only host‑key algorithm this 2020‑era SFTPGo advertises. Modern OpenSSH disables `ssh-rsa` by default because SHA‑1 is broken, **independent of key size**. Do not conflate the 4096‑bit key strength with the weak SHA‑1 signature scheme.
> - `-o HostKeyAlgorithms=+ssh-rsa` together with `-o StrictHostKeyChecking=no` / `-o UserKnownHostsFile=/dev/null` merely lets a *throwaway* client accept that legacy signature on localhost. Re‑enabling `ssh-rsa` or disabling host‑key verification on a real network re‑exposes SHA‑1 downgrade and man‑in‑the‑middle risks.
> - **`testpass123` is a synthetic, disposable test value**, invented only to occupy the password field for a *nonexistent* user (the whole point of Q2 is that the lookup fails, so it authenticates nothing). Supplying it via **`sshpass -p` exposes the value in the process list and shell history** — acceptable only for a disposable localhost test, never for a real secret.

### Authentication path (cite the chain)

The advertised `ServerVersion` is `SSH-2.0-SFTPGo_0.9.5-dev` (`sftpd/server.go:L144`, `"SSH-2.0-" + c.Banner`; `Banner` default `SFTPGo_0.9.5-dev` from `defaultBanner` at `config/config.go:L32`). Password authentication flows through: `PasswordCallback` (`sftpd/server.go:L128-L135`) → `validatePasswordCredentials` (`sftpd/server.go:L448-L461`) → `dataprovider.CheckUserAndPass` (`dataprovider/dataprovider.go:L279-L288`, which calls `p.validateUserAndPass` at `dataprovider/dataprovider.go:L287`) → `sqlCommonValidateUserAndPass` (`dataprovider/sqlcommon.go:L28-L38`) → `getUserByUsername`/`getUserFromDbRow`, which returns a **`RecordNotFoundError`** on `sql.ErrNoRows` (`dataprovider/sqlcommon.go:L296`). That error type is defined at `dataprovider/dataprovider.go:L201`, and its `Error()` method returns `"Not found: %s"` (`dataprovider/dataprovider.go:L205-L206`).

### Mode A — modern OpenSSH default (`ssh-rsa` rejected at key exchange; username never looked up)

**Command:**

```
sftp -P 2022 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null nonexistentuser@127.0.0.1
```

**Client output (complete, observed — OpenSSH 10.0p2):**

```
Unable to negotiate with 127.0.0.1 port 2022: no matching host key type found. Their offer: ssh-rsa
Connection closed.
Connection closed
```

The salient line is the first: `ssh` aborts at key exchange because the server offers only `ssh-rsa`. The two trailing `Connection closed` lines are the `sftp` wrapper reporting that its `ssh` transport exited (client exit status `255`).

**Server `sftpgo.log` (observed; only the JSON `time` field is redacted — the client‑offered algorithm list is shown complete in both lines):**

```
{"level":"warn","time":"...","sender":"sftpd","connection_id":"","message":"failed to accept an incoming connection: ssh: no common algorithm for host key; client offered: [ssh-ed25519-cert-v01@openssh.com ecdsa-sha2-nistp256-cert-v01@openssh.com ecdsa-sha2-nistp384-cert-v01@openssh.com ecdsa-sha2-nistp521-cert-v01@openssh.com sk-ssh-ed25519-cert-v01@openssh.com sk-ecdsa-sha2-nistp256-cert-v01@openssh.com rsa-sha2-512-cert-v01@openssh.com rsa-sha2-256-cert-v01@openssh.com ssh-ed25519 ecdsa-sha2-nistp256 ecdsa-sha2-nistp384 ecdsa-sha2-nistp521 sk-ssh-ed25519@openssh.com sk-ecdsa-sha2-nistp256@openssh.com rsa-sha2-512 rsa-sha2-256], server offered: [ssh-rsa]"}
{"level":"debug","time":"...","sender":"connection_failed","client_ip":"127.0.0.1","username":"","login_type":"no_auth_tryed","error":"ssh: no common algorithm for host key; client offered: [ssh-ed25519-cert-v01@openssh.com ecdsa-sha2-nistp256-cert-v01@openssh.com ecdsa-sha2-nistp384-cert-v01@openssh.com ecdsa-sha2-nistp521-cert-v01@openssh.com sk-ssh-ed25519-cert-v01@openssh.com sk-ecdsa-sha2-nistp256-cert-v01@openssh.com rsa-sha2-512-cert-v01@openssh.com rsa-sha2-256-cert-v01@openssh.com ssh-ed25519 ecdsa-sha2-nistp256 ecdsa-sha2-nistp384 ecdsa-sha2-nistp521 sk-ssh-ed25519@openssh.com sk-ecdsa-sha2-nistp256@openssh.com rsa-sha2-512 rsa-sha2-256], server offered: [ssh-rsa]"}
```

*(The exact algorithm list is what **this** client — OpenSSH 10.0p2 — offered; a different client version would offer a different set. Both the WARN and the `connection_failed` DEBUG carry the identical, complete list — nothing is elided.)*

*Mechanism.* The `username` field is **empty** and the data‑provider lookup is **never reached** (there is **no** `sqlite` WARN line). The accept‑failure WARN comes from `sftpd/server.go:L251` (`"failed to accept an incoming connection: %v"`). The `no_auth_tryed` `ConnectionFailedLog` (DEBUG, `sender:"connection_failed"`) fires from `sftpd/server.go:L253` **because the handshake error is not a `*ssh.ServerAuthError`** — the guard at `sftpd/server.go:L252` is true only for non‑auth errors. `ConnectionFailedLog` is defined at `logger/logger.go:L175-L184` and emits at DEBUG with the fields `client_ip`, `username`, `login_type`, and `error`.

### Mode B — opt into `ssh-rsa` → reach true password auth → username lookup fails

**Command:**

```
sshpass -p 'testpass123' sftp -P 2022 -o HostKeyAlgorithms=+ssh-rsa -o PubkeyAuthentication=no \
  -o PreferredAuthentications=password -o NumberOfPasswordPrompts=1 \
  -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null nonexistentuser@127.0.0.1
```

**Client output (complete, observed):**

```
Warning: Permanently added '[127.0.0.1]:2022' (RSA) to the list of known hosts.
nonexistentuser@127.0.0.1: Permission denied (password,publickey).
Connection closed.
Connection closed
```

Here the handshake gets past key exchange (the client accepted the `ssh-rsa` host key — hence the `Warning: Permanently added ... (RSA)` line), so the failure is now a genuine **authentication** rejection: `Permission denied (password,publickey)` (client exit status `255`).

**Server `sftpgo.log` (observed — three lines; only the JSON `time` field is redacted):**

```
{"level":"warn","time":"...","sender":"sqlite","connection_id":"","message":"error authenticating user: nonexistentuser, error: Not found: sql: no rows in result set"}
{"level":"debug","time":"...","sender":"connection_failed","client_ip":"127.0.0.1","username":"nonexistentuser","login_type":"password","error":"Not found: sql: no rows in result set"}
{"level":"warn","time":"...","sender":"sftpd","connection_id":"","message":"failed to accept an incoming connection: [ssh: no auth passed yet, Authentication error: could not validate password credentials: Not found: sql: no rows in result set]"}
```

*Precise details (each with citation):*

- The auth‑failure WARN is emitted at **`dataprovider/sqlcommon.go:L35`** via `providerLog` (guarded by `if err != nil` at `dataprovider/sqlcommon.go:L34`). Its **`sender` field is `"sqlite"`** — not `sqlcommon` — because `logSender` was set to `SQLiteDataProviderName = "sqlite"` inside `initializeSQLiteProvider` (`dataprovider/sqlite.go:L21`; the constant is defined at `dataprovider/dataprovider.go:L43`). The message text is `error authenticating user: nonexistentuser, error: Not found: sql: no rows in result set`.
- The `connection_failed` DEBUG in Mode B originates from `validatePasswordCredentials` at **`sftpd/server.go:L457`** (not `L253`) and carries `username:"nonexistentuser"` and `login_type:"password"`. It comes from `L457` rather than `L253` because in Mode B the handshake error **is** a `*ssh.ServerAuthError`, so the guard at `sftpd/server.go:L252` suppresses the `L253` log.
- The final accept WARN (`sftpd/server.go:L251`) wraps the `authenticationError` created in `PasswordCallback` (`sftpd/server.go:L131`, `"could not validate password credentials: %v"`), yielding `could not validate password credentials: Not found: sql: no rows in result set`.

### Before/after contrast (state change)

| | Mode A (modern OpenSSH default) | Mode B (client opts into `ssh-rsa`) |
|---|---|---|
| Where it fails | Key exchange (host‑key algorithm) | Password authentication (username lookup) |
| `username` field | `""` (empty) | `nonexistentuser` |
| Data‑provider lookup | never reached (no `sqlite` WARN) | reached → not found (`sqlite` WARN present) |
| `login_type` | `no_auth_tryed` (`sftpd/server.go:L253`) | `password` (`sftpd/server.go:L457`) |
| Server after failure | keeps listening (no crash) | keeps listening (no crash) |
| Client sees | `no matching host key type found` | `Permission denied (password,publickey)` |


---

## Q3 — What does the web‑admin root endpoint `/` return, and how is the browser redirected?

**Direct answer.** A request to `/` returns an **HTTP `301 Moved Permanently`** with a **`Location: /web/users`** header (and a tiny HTML body linking to the same path); the browser follows it to `GET /web/users`, which returns **HTTP `200 OK`** and renders the web‑admin **Users** page (`<title>SFTPGo - Users</title>`).

### Commands + exact output — observed

For both requests the status line and **all** response headers returned by `curl -si` are reproduced below with their real values (nothing redacted, including the actual `Date` header). The redirect body for `/` is shown complete; only the long `GET /web/users` **HTML body** is shortened, and that truncation is explicitly marked.

```
$ curl -si http://127.0.0.1:8080/
HTTP/1.1 301 Moved Permanently
Content-Type: text/html; charset=utf-8
Location: /web/users
Date: Mon, 13 Jul 2026 18:12:35 GMT
Content-Length: 45

<a href="/web/users">Moved Permanently</a>.
```

```
$ curl -si http://127.0.0.1:8080/web/users
HTTP/1.1 200 OK
Date: Mon, 13 Jul 2026 18:12:35 GMT
Content-Type: text/html; charset=utf-8
Transfer-Encoding: chunked

<!DOCTYPE html>
<html lang="en">

<head>

    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>SFTPGo - Users</title>

    <link rel="shortcut icon" href="/static/favicon.ico" />

(HTML body truncated here for readability — the lines above are the exact contiguous opening of the page; the full observed body is 11319 bytes, confirmed by resp_size in the request log below.)
```

> **On completeness (correcting the earlier phrasing).** The status line and every response header for **both** responses are shown above exactly as returned — with the real `Date` value, not a placeholder. Only the `GET /web/users` HTML **body** is truncated for readability (the shown lines, `<!DOCTYPE html>` through the favicon `<link>`, are the exact contiguous opening of the page); its full observed length is **11319 bytes** (see `resp_size` in the server request log below). The `/` redirect body — `<a href="/web/users">Moved Permanently</a>.`, 45 bytes — is shown in full.

### Mechanism (cite)

- The chi router registers `GET "/"` with a handler that calls `http.Redirect(w, r, webUsersPath, http.StatusMovedPermanently)` — an **HTTP 301** to **`/web/users`** (`httpd/router.go:L36-L38`; `webUsersPath = "/web/users"` at `httpd/httpd.go:L31`):

  ```go
  // httpd/router.go:L36-L38
  router.Get("/", func(w http.ResponseWriter, r *http.Request) {
      http.Redirect(w, r, webUsersPath, http.StatusMovedPermanently)
  })
  ```

- `GET /web/users` routes to `handleGetWebUsers` (`httpd/router.go:L103-L104`; handler at `httpd/web.go:L316`), which renders the users template loaded by `loadTemplates` via `template.Must` (`httpd/web.go:L78`, `httpd/web.go:L95-L98`).
- The middleware stack, in order, is `RequestID`, `RealIP`, the structured request logger, and `Recoverer` (`httpd/router.go:L23-L26`).
- `GET "/web"` also 301‑redirects to `/web/users` (`httpd/router.go:L40-L42`).
- HTTP server timeouts (for completeness): `ReadTimeout = 300s` and `WriteTimeout = 300s` (`httpd/httpd.go:L94-L95`), and `MaxHeaderBytes = 1 << 16` (64 KB) (`httpd/httpd.go:L96`).

### Server‑side request logs (observed)

The chi structured logger (`sender:"httpd"`) records each request; the two lines below are reproduced exactly as observed (only the JSON `time` field is redacted). Every other field — including the real `request_id`, `remote_addr`, and `user_agent` — is shown verbatim:

```
{"level":"info","time":"...","sender":"httpd","method":"GET","proto":"HTTP/1.1","remote_addr":"127.0.0.1:60544","request_id":"reverse-code-generator-72555211-t6th9/w2sXErtk0y-000001","uri":"http://127.0.0.1:8080/","user_agent":"curl/8.14.1","resp_status":301,"resp_size":45,"elapsed_ms":0}
{"level":"info","time":"...","sender":"httpd","method":"GET","proto":"HTTP/1.1","remote_addr":"127.0.0.1:60552","request_id":"reverse-code-generator-72555211-t6th9/w2sXErtk0y-000002","uri":"http://127.0.0.1:8080/web/users","user_agent":"curl/8.14.1","resp_status":200,"resp_size":11319,"elapsed_ms":0}
```

That is, `GET /` → `resp_status:301, resp_size:45`; `GET /web/users` → `resp_status:200, resp_size:11319`. The `request_id` values are produced by the chi `RequestID` middleware (`httpd/router.go:L23`); their format is `<hostname>/<base32-random>-<sequence>` — here the run host's name was `reverse-code-generator-72555211-t6th9` and the two requests were the 1st and 2nd (`-000001`, `-000002`) since server start. `user_agent` is `curl/8.14.1`, matching the `curl` version reported in the baseline section.


---

## Q4 — Which warnings/errors appear if the SQLite database is missing, and what sequence does the server take?

**Direct answer.** In `0.9.5-dev` the default SQLite provider does **not** auto‑create the database. On first startup with the database **absent**, the server logs a **WARN** `sqlite database file does not exists, please be sure to create and initialize a database before starting sftpgo` (`sender:"sqlite"`) followed by a fatal **ERROR** `error initializing data provider: stat sftpgo.db: no such file or directory` (`sender:"service"`); `service.Start()` then **returns early**, so **no goroutine is launched and no port is bound** — the process fast‑fails. Providing an **empty (0‑byte)** file yields a *different* error class — `sqlite database file is invalid, ...` — with **no** `sqlite` WARN. Only after the database is **created and initialized** does startup proceed to open ports `2022` and `8080`.

### Mechanism (cite)

`initializeSQLiteProvider` (`dataprovider/sqlite.go:L18-L51`) performs an `os.Stat(dbPath)` at `dataprovider/sqlite.go:L27` and branches on the result — the two failure branches and the success branch below map directly to the three observed states.

### State 1 — database file ABSENT

**Commands (full ordered run — socket probe before launch, launch capturing the exit code, socket probe after exit):**

```
cd /tmp/run_A
ls sftpgo.db                                                              # confirm the DB is absent
ss -ltnp | grep -E ':2022|:8080' || echo "(no listeners on 2022/8080)"   # BEFORE
/tmp/sftpgo_build/sftpgo serve ; echo "exit_code=$?"
ss -ltnp | grep -E ':2022|:8080' || echo "(no listeners on 2022/8080)"   # AFTER
```

**Socket probe BEFORE launch — observed, verbatim:**

```
(no listeners on 2022/8080)
```

**Console (stdout, the human `ConsoleWriter`; ANSI‑stripped) — observed, verbatim:**

```
2026-07-13T18:28:01.246 WRN error loading configuration file: Config File "sftpgo" Not Found in "[/tmp/run_A /root/.config/sftpgo /etc/sftpgo]". Default configuration will be used.
2026-07-13T18:28:01.246 ERR error initializing data provider: stat sftpgo.db: no such file or directory
```

**Process exit code — observed, verbatim:**

```
exit_code=0
```

**Socket probe AFTER exit — observed, verbatim:**

```
(no listeners on 2022/8080)
```

**`sftpgo.log` (JSON), in order — observed, verbatim (only the JSON `time` field is redacted per the convention above):**

```
{"level":"info","time":"...","sender":"service","connection_id":"","message":"starting SFTPGo 0.9.5-dev, config dir: ., config file: sftpgo, log max size: 10 log max backups: 5 log max age: 28 log verbose: true, log compress: false"}
{"level":"warn","time":"...","sender":"config","connection_id":"","message":"error loading configuration file: Config File \"sftpgo\" Not Found in \"[/tmp/run_A /root/.config/sftpgo /etc/sftpgo]\". Default configuration will be used: {SFTPD:{Banner:SFTPGo_0.9.5-dev BindPort:2022 BindAddress: IdleTimeout:15 MaxAuthTries:0 Umask:0022 UploadMode:0 Actions:{ExecuteOn:[] Command: HTTPNotificationURL:} Keys:[] IsSCPEnabled:false KexAlgorithms:[] Ciphers:[] MACs:[] LoginBannerFile: SetstatMode:0 EnabledSSHCommands:[md5sum sha1sum cd pwd]} ProviderConf:{Driver:sqlite Name:sftpgo.db Host: Port:5432 Username: Password:[redacted] SSLMode:0 ConnectionString: UsersTable:users ManageUsers:1 TrackQuota:1 PoolSize:0 UsersBaseDir: Actions:{ExecuteOn:[] Command: HTTPNotificationURL:} ExternalAuthProgram: ExternalAuthScope:0} HTTPDConfig:{BindPort:8080 BindAddress:127.0.0.1 TemplatesPath:templates StaticFilesPath:static BackupsPath:backups}}"}
{"level":"warn","time":"...","sender":"sqlite","connection_id":"","message":"sqlite database file does not exists, please be sure to create and initialize a database before starting sftpgo"}
{"level":"error","time":"...","sender":"service","connection_id":"","message":"error initializing data provider: stat sftpgo.db: no such file or directory"}
```

The console (stdout, the human `ConsoleWriter`) shows only the WARN + ERR lines. The missing‑DB WARN is emitted at `dataprovider/sqlite.go:L29-L30` (via `providerLog`). Because `os.Stat` failed, `initializeSQLiteProvider` returns the error, `dataprovider.Initialize` propagates it, and `service.Start()` logs `error initializing data provider: ...` (ERROR + `ErrorToConsole`) at `service/service.go:L71-L72` and **returns** at `service/service.go:L73`. Consequently **no SFTP/HTTP goroutine is started and no port is bound** (verified: `2022` and `8080` both closed).

### State 2 — database file EMPTY / 0‑byte

**Commands (full ordered run — create a 0‑byte file, socket probe before launch, launch capturing the exit code, socket probe after exit):**

```
cd /tmp/run_A0
touch sftpgo.db                                                          # create an empty (0‑byte) file
ls -l sftpgo.db                                                          # -> -rw-r--r-- ... 0 ... sftpgo.db
ss -ltnp | grep -E ':2022|:8080' || echo "(no listeners on 2022/8080)"   # BEFORE
/tmp/sftpgo_build/sftpgo serve ; echo "exit_code=$?"
ss -ltnp | grep -E ':2022|:8080' || echo "(no listeners on 2022/8080)"   # AFTER
```

Here **no `sqlite` WARN** appears: `os.Stat` succeeds, and the `fi.Size() == 0` branch at `dataprovider/sqlite.go:L33-L36` returns a plain `errors.New` (not a `providerLog`), so `sftpgo.log` holds only **three** lines (the `service` banner, the config‑not‑found WARN, and the `service` ERROR) — the only *failure* line is the `service` ERROR.

**Console (stdout, ANSI‑stripped) — observed, verbatim:**

```
2026-07-13T18:28:01.299 WRN error loading configuration file: Config File "sftpgo" Not Found in "[/tmp/run_A0 /root/.config/sftpgo /etc/sftpgo]". Default configuration will be used.
2026-07-13T18:28:01.299 ERR error initializing data provider: sqlite database file is invalid, please be sure to create and initialize a database before starting sftpgo
```

**Process exit code — observed, verbatim:**

```
exit_code=0
```

**Socket probes BEFORE launch and AFTER exit — observed, verbatim (both identical, no listeners):**

```
(no listeners on 2022/8080)
```

**`sftpgo.log` (JSON), all three lines in order — observed, verbatim (only the JSON `time` field is redacted per the convention above; note the **absence** of any `sqlite` line):**

```
{"level":"info","time":"...","sender":"service","connection_id":"","message":"starting SFTPGo 0.9.5-dev, config dir: ., config file: sftpgo, log max size: 10 log max backups: 5 log max age: 28 log verbose: true, log compress: false"}
{"level":"warn","time":"...","sender":"config","connection_id":"","message":"error loading configuration file: Config File \"sftpgo\" Not Found in \"[/tmp/run_A0 /root/.config/sftpgo /etc/sftpgo]\". Default configuration will be used: {SFTPD:{Banner:SFTPGo_0.9.5-dev BindPort:2022 BindAddress: IdleTimeout:15 MaxAuthTries:0 Umask:0022 UploadMode:0 Actions:{ExecuteOn:[] Command: HTTPNotificationURL:} Keys:[] IsSCPEnabled:false KexAlgorithms:[] Ciphers:[] MACs:[] LoginBannerFile: SetstatMode:0 EnabledSSHCommands:[md5sum sha1sum cd pwd]} ProviderConf:{Driver:sqlite Name:sftpgo.db Host: Port:5432 Username: Password:[redacted] SSLMode:0 ConnectionString: UsersTable:users ManageUsers:1 TrackQuota:1 PoolSize:0 UsersBaseDir: Actions:{ExecuteOn:[] Command: HTTPNotificationURL:} ExternalAuthProgram: ExternalAuthScope:0} HTTPDConfig:{BindPort:8080 BindAddress:127.0.0.1 TemplatesPath:templates StaticFilesPath:static BackupsPath:backups}}"}
{"level":"error","time":"...","sender":"service","connection_id":"","message":"error initializing data provider: sqlite database file is invalid, please be sure to create and initialize a database before starting sftpgo"}
```

No ports are bound. This is a **distinct error class** from State 1, and it was **exercised for real** (observed, not inferred).

### State 3 — database INITIALIZED → operational

Once the database exists and is non‑empty, `initializeSQLiteProvider` opens the handle and logs the DEBUG `sqlite database handle created, connection string: "file:sftpgo.db?cache=shared"` (`dataprovider/sqlite.go:L43`); startup then proceeds to bind ports `2022` and `8080` (the full Scenario B sequence in Q1).

### Report explicitly

- **Before / after:** before the database exists → **0 ports bound** and the process exits; after initializing the database → **`2022` and `8080` bound**.
- **Observed exit code = `0`** even on the fast‑fail. The `serve` command skips `Wait()` when `Start()` returns an error (`cmd/serve.go:L29-L31`) and cobra does not set a non‑zero code, so — stated precisely — **the process does not return a non‑zero exit code on data‑provider failure**.
- **Database initialization command actually used** — the canonical CI initialization from `.travis.yml` (`before_script`, `.travis.yml:L14`). No database is ever committed into the repository. **Observed, verbatim:**

```
sqlite3 sftpgo.db 'CREATE TABLE "users" ("id" integer NOT NULL PRIMARY KEY AUTOINCREMENT, "username" varchar(255) NOT NULL UNIQUE, "password" varchar(255) NULL, "public_keys" text NULL, "home_dir" varchar(255) NOT NULL, "uid" integer NOT NULL, "gid" integer NOT NULL, "max_sessions" integer NOT NULL, "quota_size" bigint NOT NULL, "quota_files" integer NOT NULL, "permissions" text NOT NULL, "used_quota_size" bigint NOT NULL, "used_quota_files" integer NOT NULL, "last_quota_update" bigint NOT NULL, "upload_bandwidth" integer NOT NULL, "download_bandwidth" integer NOT NULL, "expiration_date" bigint NOT NULL, "last_login" bigint NOT NULL, "status" integer NOT NULL, "filters" TEXT NULL, "filesystem" text NULL);'
```


---

## Q5 — How does the default configuration reveal itself in the logs when no config file is found?

**Direct answer.** When viper finds no config file, `config.LoadConfig` logs a single **WARN** whose message ends with a **full `%+v` dump of the default configuration** (the compiled defaults, with the provider password redacted to `[redacted]`), then returns early so those compiled defaults remain in effect. The dump reveals every applied value — the SFTP bind port `2022` on all interfaces, the HTTP bind `127.0.0.1:8080`, the `sqlite` provider on `sftpgo.db`, the `15`‑minute idle timeout, the default enabled SSH commands, and more. This full dump appears **only** in the JSON `sftpgo.log`; the console gets an abbreviated WARN without the dump.

### Mechanism (cite)

`config.LoadConfig` (`config/config.go:L145-L182`) calls `viper.ReadInConfig()`; on failure (`config/config.go:L151`) it logs a WARN that includes `%+v` of `getRedactedGlobalConf()` at `config/config.go:L152-L153`, emits an abbreviated console WARN **without** the dump at `config/config.go:L154` (`WarnToConsole`), and **returns early** at `config/config.go:L155`. Because it returns before `viper.Unmarshal`, the compiled defaults established in the package `init()` (`config/config.go:L41-L94`) stay in effect. `getRedactedGlobalConf()` (`config/config.go:L134-L138`) copies the global config and sets `ProviderConf.Password = "[redacted]"`.

### Exact captured WARN dump — observed, verbatim

**Command (variant (a) canonical — binary invoked from outside the run dir, no `sftpgo*` file in the cwd):**

```
cd /tmp/run_A && /tmp/sftpgo_build/sftpgo serve
```

The **complete** JSON config WARN line — the `%+v` default‑config dump is the tail of its `message` field (only the JSON `time` field is redacted per the convention above):

```
{"level":"warn","time":"...","sender":"config","connection_id":"","message":"error loading configuration file: Config File \"sftpgo\" Not Found in \"[/tmp/run_A /root/.config/sftpgo /etc/sftpgo]\". Default configuration will be used: {SFTPD:{Banner:SFTPGo_0.9.5-dev BindPort:2022 BindAddress: IdleTimeout:15 MaxAuthTries:0 Umask:0022 UploadMode:0 Actions:{ExecuteOn:[] Command: HTTPNotificationURL:} Keys:[] IsSCPEnabled:false KexAlgorithms:[] Ciphers:[] MACs:[] LoginBannerFile: SetstatMode:0 EnabledSSHCommands:[md5sum sha1sum cd pwd]} ProviderConf:{Driver:sqlite Name:sftpgo.db Host: Port:5432 Username: Password:[redacted] SSLMode:0 ConnectionString: UsersTable:users ManageUsers:1 TrackQuota:1 PoolSize:0 UsersBaseDir: Actions:{ExecuteOn:[] Command: HTTPNotificationURL:} ExternalAuthProgram: ExternalAuthScope:0} HTTPDConfig:{BindPort:8080 BindAddress:127.0.0.1 TemplatesPath:templates StaticFilesPath:static BackupsPath:backups}}"}
```

For readability the `%+v` dump portion alone — everything after `Default configuration will be used: ` in the `message` field above — is reproduced below. It is **identical across Scenario A, Scenario B, variant (b), and both runs:**

```
{SFTPD:{Banner:SFTPGo_0.9.5-dev BindPort:2022 BindAddress: IdleTimeout:15 MaxAuthTries:0 Umask:0022 UploadMode:0 Actions:{ExecuteOn:[] Command: HTTPNotificationURL:} Keys:[] IsSCPEnabled:false KexAlgorithms:[] Ciphers:[] MACs:[] LoginBannerFile: SetstatMode:0 EnabledSSHCommands:[md5sum sha1sum cd pwd]} ProviderConf:{Driver:sqlite Name:sftpgo.db Host: Port:5432 Username: Password:[redacted] SSLMode:0 ConnectionString: UsersTable:users ManageUsers:1 TrackQuota:1 PoolSize:0 UsersBaseDir: Actions:{ExecuteOn:[] Command: HTTPNotificationURL:} ExternalAuthProgram: ExternalAuthScope:0} HTTPDConfig:{BindPort:8080 BindAddress:127.0.0.1 TemplatesPath:templates StaticFilesPath:static BackupsPath:backups}}
```

*(The field order in the dump follows the Go struct definitions — e.g. `SSLMode` prints before `ConnectionString` in `ProviderConf` — not the order of the `init()` literal. It is reproduced exactly as emitted.)*

### Defaults table (values from the dump, cross‑checked to `config/config.go` `init()`)

| Group | Field | Default | Source line |
|------|-------|---------|-------------|
| SFTPD | Banner | `SFTPGo_0.9.5-dev` | config/config.go:L45 (`defaultBanner`, defined L32) |
| SFTPD | BindPort | `2022` | config/config.go:L46 |
| SFTPD | BindAddress | `""` (all interfaces → `[::]:2022`) | config/config.go:L47 |
| SFTPD | IdleTimeout | `15` (minutes; idle ticker every 5 min `sftpd/sftpd.go:L132`; handshake timeout 2 min `sftpd/sftpd.go:L46`) | config/config.go:L48 |
| SFTPD | MaxAuthTries | `0` | config/config.go:L49 |
| SFTPD | Umask | `0022` | config/config.go:L50 |
| SFTPD | EnabledSSHCommands | `[md5sum sha1sum cd pwd]` | config/config.go:L63 (`sftpd/sftpd.go:L68`) |
| Provider | Driver | `sqlite` | config/config.go:L66 |
| Provider | Name | `sftpgo.db` | config/config.go:L67 |
| Provider | Port | `5432` | config/config.go:L69 |
| Provider | UsersTable | `users` | config/config.go:L73 |
| Provider | ManageUsers | `1` | config/config.go:L74 |
| Provider | TrackQuota | `1` | config/config.go:L76 (NOTE: shipped `sftpgo.json:L35` sets `2`; the **compiled default is 1**) |
| HTTPD | BindPort | `8080` | config/config.go:L88 |
| HTTPD | BindAddress | `127.0.0.1` | config/config.go:L89 |
| HTTPD | TemplatesPath | `templates` | config/config.go:L90 |
| HTTPD | StaticFilesPath | `static` | config/config.go:L91 |
| HTTPD | BackupsPath | `backups` | config/config.go:L92 |

### Two config‑not‑found variants (both observed)

Both variants were exercised for real. Each is shown with its exact command and observed output; only the JSON `time` field is redacted per the convention above.

**(a) Canonical** — the binary is invoked from **outside** the run dir, with no `sftpgo*` file in the cwd; viper reports the file as *not found* and the WARN reveals the exact search path `[/tmp/run_A /root/.config/sftpgo /etc/sftpgo]`. Command:

```
cd /tmp/run_A && /tmp/sftpgo_build/sftpgo serve
```

Its complete JSON `config` WARN line (with the full `%+v` dump) is the one shown verbatim above under **Exact captured WARN dump**.

**(b) Variant** — the extension‑less `sftpgo` **binary** is copied into the cwd and launched as `./sftpgo serve`; viper matches the binary itself as a config candidate but cannot determine its type. Commands:

```
cp /tmp/sftpgo_build/sftpgo /tmp/run_vb/sftpgo
cd /tmp/run_vb && ./sftpgo serve
```

Console (stdout, ANSI‑stripped) — observed, verbatim:

```
2026-07-13T18:14:50.728 WRN error loading configuration file: Unsupported Config Type "". Default configuration will be used.
2026-07-13T18:14:50.728 ERR error initializing data provider: stat sftpgo.db: no such file or directory
```

The complete JSON `config` WARN line — the `%+v` default‑config dump is **identical** to variant (a); only the error prefix differs (`Unsupported Config Type ""` rather than `Config File ... Not Found`):

```
{"level":"warn","time":"...","sender":"config","connection_id":"","message":"error loading configuration file: Unsupported Config Type \"\". Default configuration will be used: {SFTPD:{Banner:SFTPGo_0.9.5-dev BindPort:2022 BindAddress: IdleTimeout:15 MaxAuthTries:0 Umask:0022 UploadMode:0 Actions:{ExecuteOn:[] Command: HTTPNotificationURL:} Keys:[] IsSCPEnabled:false KexAlgorithms:[] Ciphers:[] MACs:[] LoginBannerFile: SetstatMode:0 EnabledSSHCommands:[md5sum sha1sum cd pwd]} ProviderConf:{Driver:sqlite Name:sftpgo.db Host: Port:5432 Username: Password:[redacted] SSLMode:0 ConnectionString: UsersTable:users ManageUsers:1 TrackQuota:1 PoolSize:0 UsersBaseDir: Actions:{ExecuteOn:[] Command: HTTPNotificationURL:} ExternalAuthProgram: ExternalAuthScope:0} HTTPDConfig:{BindPort:8080 BindAddress:127.0.0.1 TemplatesPath:templates StaticFilesPath:static BackupsPath:backups}}"}
```

### Logging sinks (state precisely, cite)

Structured JSON is written to `sftpgo.log` via lumberjack (`logger/logger.go:L50-L56`), while a human‑readable `ConsoleWriter` on stdout receives only the `*ToConsole` INFO/WARN/ERROR messages (`logger/logger.go:L74-L81`). The timestamp format is `dateFormat = "2006-01-02T15:04:05.000"` (`logger/logger.go:L22`). Each **structured JSON** entry carries a `sender` field (observed values: `service`, `config`, `sqlite`, `httpd`, `sftpd`, `utils`, `connection_failed`), because the structured `Debug`/`Info`/`Warn`/`Error` helpers each call `.Str("sender", sender)` (`logger/logger.go:L99-L115`); the human‑readable console `*ToConsole` lines (`logger/logger.go:L119-L134`) do **not** include a `sender` field. Most structured evidence — including the SFTP readiness line and the full default‑config dump — must be read from `sftpgo.log`, **not** the console. Concretely, the console for a config‑not‑found operational boot (Scenario B, `cd /tmp/run_B`) shows only:

```
2026-07-13T18:12:04.002 WRN error loading configuration file: Config File "sftpgo" Not Found in "[/tmp/run_B /root/.config/sftpgo /etc/sftpgo]". Default configuration will be used.
2026-07-13T18:12:04.002 INF No host keys configured and "id_rsa" does not exist; creating new private key for server
```

— note the console WARN ends with `Default configuration will be used.` and does **not** contain the `%+v` dump (`config/config.go:L154`).


---

## Coverage Checklist

Each item below was confirmed answered in this document, grounded in observed output plus a `file:line` citation:

- [x] **Q1** — Ports **`2022`** and **`8080`** both named, with **complete before (closed) and after (open) `ss` probes for both runs** quoted; SFTP readiness line `server listener registered address: [::]:2022` (`sftpd/server.go:L187`) quoted; HTTP "no explicit ready line" nuance stated (init DEBUG `httpd/httpd.go:L76` + successful bind, absence of the `service/service.go:L105` error); host‑key generation delay measured from the raw JSON timestamps (Run 1 = 5.972 s, Run 2 = 2.398 s → variable, observed range **≈ 2.4 – 6.0 s**), with the readiness line and both ports byte‑identical across the two runs.
- [x] **Q2** — **Both modes** documented: (A) `ssh-rsa` key‑exchange rejection — **empty** `username`, `login_type:"no_auth_tryed"`, and the Mode‑A client sees a **host‑key negotiation error** (`no matching host key type found`), *not* `Permission denied`; (B) true password authentication — `username:"nonexistentuser"`, `login_type:"password"`, and **only the Mode‑B client** sees `Permission denied (password,publickey)`. Both server DEBUG lines carry the **complete** client‑offered algorithm list (no `[...]` elision); the three Mode‑B server log lines are quoted; the `RecordNotFoundError` → `Not found: sql: no rows in result set` chain is cited; the observed `sender:"sqlite"` nuance (`dataprovider/sqlite.go:L21`) is explained; the server keeps listening after each failure.
- [x] **Q3** — `GET /` → **`301`** with `Location: /web/users` and the full redirect body quoted (all response headers shown with **real** values, including the actual `Date`); follow‑on `GET /web/users` → **`200`** (`SFTPGo - Users`), body shown as a clearly labeled excerpt; 301 mechanism cited (`httpd/router.go:L36-L38`); server request logs quoted with **real** `request_id`/`user_agent`/`remote_addr` and `resp_status`/`resp_size` = `301`/`45` and `200`/`11319`.
- [x] **Q4** — Both states run from **real absolute `/tmp/...` paths** with complete ordered captures (console, full JSON log, `ss` socket probes, and `echo $?` exit code): missing‑DB WARN (`dataprovider/sqlite.go:L29-L30`) + fatal ERROR (`service/service.go:L71-L72`) quoted; 0‑byte "invalid" edge (`dataprovider/sqlite.go:L33-L36`) quoted with the observed *absence* of the `sqlite` WARN; **no ports bound before or after** (probes shown) vs. `2022`+`8080` once initialized; exit‑code‑`0` nuance cited (`cmd/serve.go:L29-L31`); canonical DB‑init command (`.travis.yml:L14`) quoted.
- [x] **Q5** — Full default `%+v` dump quoted (as both the complete `config` WARN line and its extracted tail); defaults table covering bind addresses, ports, timeouts, and provider settings with `file:line` anchors; `TrackQuota` compiled default = `1` (`config/config.go:L76`) vs shipped `sftpgo.json:L35` = `2`; **both config‑not‑found variants (a and b) shown with exact commands and complete console + JSON output**; logging sinks explained — **only structured JSON entries carry `sender`** (`logger/logger.go:L99-L115`) while console `*ToConsole` lines do not (`logger/logger.go:L119-L134`), plus JSON‑file vs. `ConsoleWriter` sinks (`logger/logger.go:L50-L56`, `logger/logger.go:L74-L81`, `logger/logger.go:L22`).
- [x] Every behavioral claim is paired with actual, complete captured output and a `file:line` citation; nothing required an *inferred* label (the 0‑byte State 2 was exercised for real). The **only** redactions are the two explicitly permitted ones — the per‑line JSON `time` field (see the Timestamp convention) and the clearly labeled `GET /web/users` HTML‑body excerpt; there are **no** `// ...` code elisions and no `[...]`‑truncated output.
- [x] **No source or reference file was modified.** The investigation built and ran the binary strictly **outside** the checkout (under `/tmp`) and removed all artifacts; the only change introduced is this documentation file under `blitzy/documentation/`. All source and reference files remain **byte‑identical to baseline** `44634210287cb192f2a53147eafb84a33a96826b` — `git diff --name-status <baseline> HEAD` lists exactly one entry, `A  blitzy/documentation/sftpgo_44634210287c.md`. This document is added as a **dedicated documentation commit on top of** that baseline, so current HEAD contains **only** the permitted documentation commit and is therefore **not equal** to the baseline commit.
