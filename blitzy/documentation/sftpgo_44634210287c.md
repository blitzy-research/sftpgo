# SFTPGo Security-Model Investigation — SSH Commands, SCP Parsing, Permissions & Path Containment

**Target under investigation:** SFTPGo `0.9.5-dev` — module `github.com/drakkan/sftpgo` (`go.mod`), Go directive `go 1.13`.
**Source branch the deliverable is named after:** `sftpgo_44634210287c` (hence this file is `blitzy/documentation/sftpgo_44634210287c.md`).

This document answers six security-model questions about how SFTPGo behaves when external file-synchronization utilities and SSH commands are invoked. It is **evidence-driven**: every behavioral claim is backed by output captured from *actually running* the built binary and driving it through a **real client** (system `ssh`/`scp`/`sftp`/`rsync`, or crafted SCP protocol bytes over an SSH `exec` channel). The methodology was **run first, write second** — the code was built, launched in its default (canonical) configuration, exercised through its genuine client-facing entry points, and only then documented.

Statements that could only be derived by *reading* the code (never observed at runtime) are explicitly labeled **`(inferred)`**. Everything else is anchored both to a `file:line` code reference **and** to captured runtime output (the exact command, its complete unedited output, and its exit status).

> **Scope note (read-only).** No SFTPGo source, test, or configuration file was modified. The only artifact added to the repository is this single Markdown document (and its parent directory `blitzy/documentation/`). All transient artifacts used to capture evidence — the built binary, a temporary SQLite database, generated host keys, test-user home directories, and throwaway scripts — were created **outside** the repository tree (under `/tmp`) and deleted before completion. The closing "Cleanup & verification" section pastes the final `git status --porcelain` as proof.

---

## Table of Contents

- [Build / Run Environment Preamble](#build--run-environment-preamble)
- [Q1 — Enabling / disabling external utilities (the SSH-command allowlist)](#q1--enabling--disabling-external-utilities-the-ssh-command-allowlist)
- [Q2 — The client / server control boundary](#q2--the-client--server-control-boundary)
- [Q3 — Filtering / validation rules](#q3--filtering--validation-rules)
- [Q4 — Delimiter / boundary assumptions under unusual input](#q4--delimiter--boundary-assumptions-under-unusual-input)
- [Q5 — Where permission checks sit in the chain (resolve-then-authorize)](#q5--where-permission-checks-sit-in-the-chain-resolve-then-authorize)
- [Q6 — Protections against filesystem tricks (path traversal / containment)](#q6--protections-against-filesystem-tricks-path-traversal--containment)
- [Closing Coverage Pass](#closing-coverage-pass)
- [Cleanup & Verification](#cleanup--verification)

---

## Build / Run Environment Preamble

Everything below was run in the canonical (default) configuration, as a normal operator would, with the pinned toolchain.

### Toolchain

```
$ go version
go version go1.13.15 linux/amd64
```

The module pins the toolchain in `go.mod`:

```
module github.com/drakkan/sftpgo

go 1.13
```

### cgo / gcc requirement

The default data provider is **SQLite** via `github.com/mattn/go-sqlite3`, a cgo package. A C compiler is therefore **required** to build the canonical binary. `gcc 15.2.0` is present at `/usr/bin/gcc` with `CGO_ENABLED=1`. Without a C compiler on `$PATH`, the build fails with `exec: "gcc": executable file not found in $PATH` **(inferred** — gcc was present throughout, so this exact failure was not forced; the requirement is a documented property of the cgo SQLite driver).

### Build command and result

```
$ cd /tmp/blitzy/sftpgo/blitzy-933a2d22-4d4f-407b-b451-7ab747130255_599118
$ CGO_ENABLED=1 go build -o /tmp/sftpgo_bin .
# exit status: 0
```

The build completed with exit status **0**. The only compiler diagnostic emitted was a benign C warning from the vendored SQLite amalgamation (not an SFTPGo Go-code warning):

```
# github.com/mattn/go-sqlite3
sqlite3-binding.c: In function 'sqlite3SelectNew':
sqlite3-binding.c:125801: warning: function may return address of local variable [-Wreturn-local-addr]
```

### Version banner (canonical-config evidence)

The version string is produced by the real CLI. Mechanism: `cmd/root.go:L59-65` sets a cobra version template (`SetVersionTemplate` at L63) whose value comes from `utils.GetAppVersion()` (`cmd/root.go:L60`), and the underlying constant is `const version = "0.9.5-dev"` (`utils/version.go:L3`).

```
$ /tmp/sftpgo_bin --version
SFTPGo version: 0.9.5-dev
# exit status: 0
```

### Server invocation (default config)

To keep every runtime artifact outside the repository tree, the server was run from an isolated directory `/tmp/sftpgo_run`. The repository's `sftpgo.json` was copied there **unchanged except** for making the three `httpd` paths absolute (`templates_path`, `static_files_path`, `backups_path`) so `serve` works from any working directory; the `sftpd` block (including `enable_scp` and `enabled_ssh_commands`) was left at its repository defaults. The serve flags were confirmed via `/tmp/sftpgo_bin serve --help`: `-c/--config-dir` (default `.`), `--config-file` (default `sftpgo`), `-l/--log-file-path` (default `sftpgo.log`).

This revision does **not** auto-create the provider's `users` table (there is no `initprovider` subcommand in `0.9.5-dev`), so — mirroring the project's `.travis.yml` `before_script` — the SQLite `users` table was pre-created from the shipped schema before first launch.

Canonical serve invocation:

```
$ /tmp/sftpgo_bin serve -c /tmp/sftpgo_run -l /tmp/sftpgo_run/sftpgo.log
```

Complete unedited startup log (`/tmp/sftpgo_run/sftpgo.log`) for the canonical run — note `IsSCPEnabled:false`, `EnabledSSHCommands:[md5sum sha1sum cd pwd]`, `Driver:sqlite`, SFTP bound on `[::]:2022`, HTTP admin on `127.0.0.1:8080`, and the host key auto-generated at `/tmp/sftpgo_run/id_rsa`:

```json
{"level":"info","time":"2026-07-08T05:11:49.093","sender":"service","connection_id":"","message":"starting SFTPGo 0.9.5-dev, config dir: /tmp/sftpgo_run, config file: sftpgo, log max size: 10 log max backups: 5 log max age: 28 log verbose: true, log compress: false"}
{"level":"debug","time":"2026-07-08T05:11:49.094","sender":"config","connection_id":"","message":"config file used: '/tmp/sftpgo_run/sftpgo.json', config loaded: {SFTPD:{Banner:SFTPGo_0.9.5-dev BindPort:2022 BindAddress: IdleTimeout:15 MaxAuthTries:0 Umask:0022 UploadMode:0 Actions:{ExecuteOn:[] Command: HTTPNotificationURL:} Keys:[] IsSCPEnabled:false KexAlgorithms:[] Ciphers:[] MACs:[] LoginBannerFile: SetstatMode:0 EnabledSSHCommands:[md5sum sha1sum cd pwd]} ProviderConf:{Driver:sqlite Name:sftpgo.db Host: Port:5432 Username: Password:[redacted] SSLMode:0 ConnectionString: UsersTable:users ManageUsers:1 TrackQuota:2 PoolSize:0 UsersBaseDir: Actions:{ExecuteOn:[] Command: HTTPNotificationURL:} ExternalAuthProgram: ExternalAuthScope:0} HTTPDConfig:{BindPort:8080 BindAddress:127.0.0.1 TemplatesPath:/tmp/blitzy/sftpgo/blitzy-933a2d22-4d4f-407b-b451-7ab747130255_599118/templates StaticFilesPath:/tmp/blitzy/sftpgo/blitzy-933a2d22-4d4f-407b-b451-7ab747130255_599118/static BackupsPath:/tmp/sftpgo_run/backups}}"}
{"level":"debug","time":"2026-07-08T05:11:49.094","sender":"sqlite","connection_id":"","message":"sqlite database handle created, connection string: \"file:/tmp/sftpgo_run/sftpgo.db?cache=shared\""}
{"level":"debug","time":"2026-07-08T05:11:49.094","sender":"httpd","connection_id":"","message":"initializing HTTP server with config {BindPort:8080 BindAddress:127.0.0.1 TemplatesPath:/tmp/blitzy/sftpgo/blitzy-933a2d22-4d4f-407b-b451-7ab747130255_599118/templates StaticFilesPath:/tmp/blitzy/sftpgo/blitzy-933a2d22-4d4f-407b-b451-7ab747130255_599118/static BackupsPath:/tmp/sftpgo_run/backups}"}
{"level":"debug","time":"2026-07-08T05:11:49.094","sender":"service","connection_id":"","message":"initializing SFTP server with config {Banner:SFTPGo_0.9.5-dev BindPort:2022 BindAddress: IdleTimeout:15 MaxAuthTries:0 Umask:0022 UploadMode:0 Actions:{ExecuteOn:[] Command: HTTPNotificationURL:} Keys:[] IsSCPEnabled:false KexAlgorithms:[] Ciphers:[] MACs:[] LoginBannerFile: SetstatMode:0 EnabledSSHCommands:[md5sum sha1sum cd pwd]}"}
{"level":"debug","time":"2026-07-08T05:11:49.094","sender":"utils","connection_id":"","message":"set umask to 0022 (18)"}
{"level":"info","time":"2026-07-08T05:11:49.094","sender":"sftpd","connection_id":"","message":"Loading private key: /tmp/sftpgo_run/id_rsa"}
{"level":"info","time":"2026-07-08T05:11:49.097","sender":"sftpd","connection_id":"","message":"server listener registered address: [::]:2022"}
{"level":"warn","time":"2026-07-08T05:11:49.592","sender":"sftpd","connection_id":"","message":"failed to accept an incoming connection: EOF"}
{"level":"debug","time":"2026-07-08T05:11:49.592","sender":"connection_failed","client_ip":"127.0.0.1","username":"","login_type":"no_auth_tryed","error":"EOF"}
```

(The trailing `EOF`/`no_auth_tryed` lines are the harness's TCP readiness probe against the port; they are benign.)

### Test user creation via the REAL entry point (REST API)

Users were created through SFTPGo's genuine admin REST API — `POST /api/v1/user` (`httpd/httpd.go`). This revision ships **no REST authentication** by default, so the POST is direct. The baseline user grants all permissions at root so that, for path-containment tests (Q6), only the containment check — not the permission model — can block:

```
$ curl -s -w '\nHTTP_STATUS=%{http_code}\n' -X POST http://127.0.0.1:8080/api/v1/user \
    -H 'Content-Type: application/json' \
    -d '{"username":"testuser","password":"testpass","public_keys":["<throwaway pubkey>"],
         "home_dir":"/tmp/sftpgo_run/testuser","status":1,"permissions":{"/":["*"]}}'
HTTP_STATUS=200
```

DB row confirmed present after creation (`id|username|home_dir|status|permissions`):

```
1|testuser|/tmp/sftpgo_run/testuser|1|{"/":["*"]}
```

> `testpass` and the SSH keys used throughout are throwaway test credentials generated for this investigation only; they are not real secrets. SFTPGo's own config dump prints `Password:[redacted]` for the *provider* password (its built-in redaction), not a leaked value.

### Client connection notes

The bundled OpenSSH client is `OpenSSH_10.0p2`, which by default rejects the server's legacy `ssh-rsa` host key. `/root/.ssh/config` was pre-set with `HostKeyAlgorithms +ssh-rsa`, `PubkeyAcceptedAlgorithms +ssh-rsa`, `StrictHostKeyChecking no`, `UserKnownHostsFile /dev/null`. Per-invocation client options used throughout (abbreviated as `<sshopts>` below): `-o IdentitiesOnly=yes -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o LogLevel=ERROR -o ConnectTimeout=10`, plus `-i /tmp/sftpgo_run/testkey` for public-key auth.

---

## Q1 — Enabling / disabling external utilities (the SSH-command allowlist)

### Direct finding

SFTPGo governs external utilities and SSH commands through a **fixed allowlist of supported names** plus a **configurable enabled subset**. Concretely:

- The complete set of names SFTPGo will *ever* accept is hard-coded as `supportedSSHCommands` = `{scp, md5sum, sha1sum, sha256sum, sha384sum, sha512sum, cd, pwd, git-receive-pack, git-upload-pack, git-upload-archive, rsync}`. Anything outside this set can never be enabled.
- The subset active by default is `defaultSSHCommands` = `{md5sum, sha1sum, cd, pwd}`.
- **`scp` is *not* enabled by default**; it is gated by a *separate* boolean `enable_scp` (default `false`).
- **`rsync` is *not* enabled by default**; it must be added to `enabled_ssh_commands`.
- Observed at runtime: with defaults, both `scp` (legacy protocol) and `rsync` are **refused**; after enabling them, they are **accepted**; an unsupported name placed in `enabled_ssh_commands` is **silently dropped at startup with a warning**; and the wildcard `"*"` expands to the **entire supported set**.
- **`sftpgo-copy` / `sftpgo-remove` do NOT exist in this revision.** They are built-in SSH commands in later SFTPGo 2.x releases, but here they are neither in `supportedSSHCommands` nor recognized anywhere — a name like `sftpgo-copy` is treated exactly like any other unsupported string (dropped at startup, refused at runtime). This is demonstrated in Q1d below.

### Code anchors

- Name lists — `sftpd/sftpd.go:L66-70`:
  - `supportedSSHCommands` (L66-67), `defaultSSHCommands` (L68), `sshHashCommands` (L69), `systemCommands` (L70).
  - Accessors `GetDefaultSSHCommands` (`sftpd/sftpd.go:L136`), `GetSupportedSSHCommands` (`sftpd/sftpd.go:L143`).
- Defaults — `config/config.go`: `BindPort:2022` (L46), `IsSCPEnabled:false` (L58), `EnabledSSHCommands: sftpd.GetDefaultSSHCommands()` (L63), `Driver:"sqlite"` (L66), `Name:"sftpgo.db"` (L67). Sample config `sftpgo.json`: `bind_port:2022` (L3), `enable_scp:false` (L16), `enabled_ssh_commands:["md5sum","sha1sum","cd","pwd"]` (L22), `driver:sqlite` (L25).
- Startup normalization — `sftpd/server.go` `checkSSHCommands` (L396-414): `"*"` → `GetSupportedSSHCommands()` and early-return (L397-399); if `IsSCPEnabled` then `"scp"` is prepended (L402-404); any entry not in `supportedSSHCommands` is dropped with a log `unsupported ssh command: %#v ignored` (L406-411, warning emitted at L409-410).

The exact declarations, as read from the source:

```go
// sftpd/sftpd.go:L66-70
supportedSSHCommands = []string{"scp", "md5sum", "sha1sum", "sha256sum", "sha384sum", "sha512sum", "cd", "pwd",
    "git-receive-pack", "git-upload-pack", "git-upload-archive", "rsync"}
defaultSSHCommands = []string{"md5sum", "sha1sum", "cd", "pwd"}
sshHashCommands    = []string{"md5sum", "sha1sum", "sha256sum", "sha384sum", "sha512sum"}
systemCommands     = []string{"git-receive-pack", "git-upload-pack", "git-upload-archive", "rsync"}
```

```go
// sftpd/server.go:L396-414
func (c *Configuration) checkSSHCommands() {
    if utils.IsStringInSlice("*", c.EnabledSSHCommands) {
        c.EnabledSSHCommands = GetSupportedSSHCommands()
        return
    }
    sshCommands := []string{}
    if c.IsSCPEnabled {
        sshCommands = append(sshCommands, "scp")
    }
    for _, command := range c.EnabledSSHCommands {
        if utils.IsStringInSlice(command, supportedSSHCommands) {
            sshCommands = append(sshCommands, command)
        } else {
            logger.Warn(logSender, "", "unsupported ssh command: %#v ignored", command)
            logger.WarnToConsole("unsupported ssh command: %#v ignored", command)
        }
    }
    c.EnabledSSHCommands = sshCommands
}
```

### Q1a — Default configuration refuses `scp` (legacy protocol) and `rsync`

**Before-state (enabled set as loaded from the canonical config):**

```
enable_scp= False  enabled_ssh_commands= ['md5sum', 'sha1sum', 'cd', 'pwd']
```

Raw `exec` payloads through the real `ssh` client. Both the `scp` and `rsync` command names are rejected:

```
--- CLIENT: ssh <sshopts> -p 2022 testuser@127.0.0.1 "rsync --server -vlogDtpre.iLsfxC . /probe.txt" ---
exec request failed on channel 0
CLIENT_EXIT=255

--- CLIENT: ssh <sshopts> -p 2022 testuser@127.0.0.1 "scp -t /probe2.txt" ---
exec request failed on channel 0
CLIENT_EXIT=255
```

Corresponding server log lines (each command reaches the server, then is rejected at the allowlist gate):

```json
{"level":"debug","time":"2026-07-08T05:13:51.396","sender":"ssh","connection_id":"afe0da09b27e57859becc3b1910b9434f483f8527f4e55a84af06afb593509e6","message":"new ssh command: \"rsync\" args: [--server -vlogDtpre.iLsfxC . /probe.txt] user: testuser, error: <nil>"}
{"level":"info","time":"2026-07-08T05:13:51.396","sender":"ssh","connection_id":"afe0da09b27e57859becc3b1910b9434f483f8527f4e55a84af06afb593509e6","message":"ssh command not enabled/supported: \"rsync\""}
{"level":"debug","time":"2026-07-08T05:13:51.510","sender":"ssh","connection_id":"1601cfc87d0fafd5c7db6d02bda059d2f32230e44a7b7f26ff1e890e090f61a6","message":"new ssh command: \"scp\" args: [-t /probe2.txt] user: testuser, error: <nil>"}
{"level":"info","time":"2026-07-08T05:13:51.510","sender":"ssh","connection_id":"1601cfc87d0fafd5c7db6d02bda059d2f32230e44a7b7f26ff1e890e090f61a6","message":"ssh command not enabled/supported: \"scp\""}
```

**Honest nuance — OpenSSH 10 `scp` defaults to SFTP, which bypasses the scp SSH-command.** A *plain* `scp` upload against the default server actually returned exit 0 and created the file:

```
CMD: scp -P 2022 /tmp/scp_src.txt testuser@127.0.0.1:/scp_dest.txt
SCP_CLIENT_EXIT=0
```

This is **not** the scp SSH-command being honored — modern OpenSSH `scp` negotiates the **SFTP subsystem** by default, and the SFTP subsystem is a separate, always-available service gated only by user permissions (here `{"/":["*"]}`). To faithfully exercise the *legacy SCP protocol* path (the one governed by `enable_scp`), the client must be forced with `scp -O`. With `scp` disabled, that legacy path is refused, reproducibly, and **no file is created**:

```
=== BEFORE: home dir (scp_dest.txt is the SFTP-protocol upload above) ===
-rw-r--r-- 1 root root   12 Jul  8 04:57 probe.txt
-rw-r--r-- 1 root root    6 Jul  8 05:13 scp_dest.txt

=== Q1a FAITHFUL: scp -O (force legacy SCP protocol) with scp DISABLED ===
CMD: scp -O -P 2022 /tmp/scp_src.txt testuser@127.0.0.1:/scp_dest_O.txt
exec request failed on channel 0
lost connection
SCP_O_CLIENT_EXIT=1
=== reproducibility: repeat scp -O (run 2) ===
SCP_O_CLIENT_EXIT_run2=1
exec request failed on channel 0
lost connection

=== SERVER LOG for scp -O attempts ===
{"level":"debug","time":"2026-07-08T05:14:27.431","sender":"ssh","connection_id":"161d3bc778c65b3287f4f223c1f89b50b11ffa312d1f4f467684049bdc0e6ce6","message":"new ssh command: \"scp\" args: [-t /scp_dest_O.txt] user: testuser, error: <nil>"}
{"level":"info","time":"2026-07-08T05:14:27.431","sender":"ssh","connection_id":"161d3bc778c65b3287f4f223c1f89b50b11ffa312d1f4f467684049bdc0e6ce6","message":"ssh command not enabled/supported: \"scp\""}
{"level":"debug","time":"2026-07-08T05:14:27.544","sender":"ssh","connection_id":"cf0d60970fe79ca373cba503aa395014a07f2c6cfd93bbb64dfac585b9f32b1f","message":"new ssh command: \"scp\" args: [-t /scp_dest_O2.txt] user: testuser, error: <nil>"}
{"level":"info","time":"2026-07-08T05:14:27.544","sender":"ssh","connection_id":"cf0d60970fe79ca373cba503aa395014a07f2c6cfd93bbb64dfac585b9f32b1f","message":"ssh command not enabled/supported: \"scp\""}

=== AFTER: home dir (scp_dest_O*.txt were NOT created) ===
-rw-r--r-- 1 root root   12 Jul  8 04:57 probe.txt
-rw-r--r-- 1 root root    6 Jul  8 05:13 scp_dest.txt
```

A real `rsync` client (rather than a raw `exec`) confirms the same refusal — the connection is dropped because the server rejects the `rsync` command:

```
CMD: rsync -e "ssh -p 2022 <sshopts>" /tmp/scp_src.txt testuser@127.0.0.1:/rsync_dest.txt
exec request failed on channel 0
rsync: connection unexpectedly closed (0 bytes received so far) [sender]
rsync error: unexplained error (code 255) at io.c(232) [sender=3.4.1]
RSYNC_CLIENT_EXIT=255
```

### Q1b — Enabling `rsync` via `enabled_ssh_commands` → accepted

Variant config (only change: append `rsync`):

```
enable_scp= False  enabled_ssh_commands= ['md5sum', 'sha1sum', 'cd', 'pwd', 'rsync']
```

The exact command that was refused under defaults now succeeds (file transferred), reproducibly across two runs:

```
CMD: rsync -e "ssh -p 2022 <sshopts>" /tmp/rsrc.txt testuser@127.0.0.1:/rs_safe.txt
RSYNC_EXIT(testuser)=0
===== Q1b: reproduce acceptance (rsync now enabled) run 2 as testuser =====
RSYNC_EXIT(run2)=0
```

Home directory after (the transferred files prove acceptance):

```
-- testuser home --
-rw-r--r-- 1 root root   12 Jul  8 04:57 probe.txt
-rw-r--r-- 1 root root   12 Jul  8 05:15 rs_safe.txt
-rw-r--r-- 1 root root   12 Jul  8 05:15 rs_safe2.txt
```

**Before/after enabled set:** `rsync` moved from *refused* (Q1a) to *accepted* purely by its presence in `enabled_ssh_commands`.

### Q1c — Enabling `scp` via `enable_scp:true` → accepted

Variant config (only change: `enable_scp:true`; `enabled_ssh_commands` left at default and **does not** contain `scp`):

```
CONFIG enable_scp= True  enabled_ssh_commands= ['md5sum', 'sha1sum', 'cd', 'pwd']
```

The forced legacy-protocol upload now succeeds (exit 0, reproducible), and the server shows the `scp` command accepted with **no** rejection line:

```
CMD: scp -O -P 2022 /tmp/scp_src.txt testuser@127.0.0.1:/scp_ok.txt
SCP_O_EXIT=0
--- reproduce run 2 ---
SCP_O_EXIT_run2=0
--- SERVER LOG (scp accepted: new ssh command scp, NO rejection) ---
{"level":"debug","time":"2026-07-08T05:16:10.350","sender":"ssh","connection_id":"6738aa9c2b5a398dbc82662d72263770a89e63f4a2419814a6c961892f38a754","message":"new ssh command: \"scp\" args: [-t /scp_ok.txt] user: testuser, error: <nil>"}
{"level":"debug","time":"2026-07-08T05:16:10.466","sender":"ssh","connection_id":"d50334c11a1ea674861c243d87049ddc10805464f55bac5aa3e28a8d063760c0","message":"new ssh command: \"scp\" args: [-t /scp_ok2.txt] user: testuser, error: <nil>"}
--- home dir: scp_ok.txt created = accepted ---
-rw-r--r-- 1 root root   10 Jul  8 05:16 scp_ok.txt
-rw-r--r-- 1 root root   10 Jul  8 05:16 scp_ok2.txt
```

**Causal explanation:** `scp` became acceptable even though it is not in `enabled_ssh_commands`, because `checkSSHCommands` prepends `"scp"` to the enabled set when `IsSCPEnabled` is true (`sftpd/server.go:L402-404`). This confirms `scp` is gated by the *separate* `enable_scp` flag, not by `enabled_ssh_commands`.

### Q1d — Unsupported names are dropped at startup with a warning

Variant config injecting two unsupported names, one arbitrary (`bogus`) and one that exists only in later releases (`sftpgo-copy`):

```
CONFIG enabled_ssh_commands= ['md5sum', 'sha1sum', 'cd', 'pwd', 'bogus', 'sftpgo-copy']
```

Startup emits the drop warning to **both** the log file and the console — exact text `unsupported ssh command: %#v ignored`:

```
--- startup warnings in LOG FILE (logger.Warn) ---
{"level":"warn","time":"2026-07-08T05:16:11.579","sender":"sftpd","connection_id":"","message":"unsupported ssh command: \"bogus\" ignored"}
{"level":"warn","time":"2026-07-08T05:16:11.581","sender":"sftpd","connection_id":"","message":"unsupported ssh command: \"sftpgo-copy\" ignored"}
--- startup warnings on CONSOLE (logger.WarnToConsole -> serve_stdout.log) ---
2026-07-08T05:16:11.579 WRN unsupported ssh command: "bogus" ignored
2026-07-08T05:16:11.581 WRN unsupported ssh command: "sftpgo-copy" ignored
```

Both are also refused at runtime:

```
--- verify bogus is refused at runtime too ---
BOGUS_EXIT=255
SFTPGOCOPY_EXIT=255
{"level":"info","time":"2026-07-08T05:16:13.250","sender":"ssh","connection_id":"5a7ae0f4507f144dc5140dd9125bfc32437fa8534bc85f6eafb0850d4b67e8f2","message":"ssh command not enabled/supported: \"bogus\""}
{"level":"info","time":"2026-07-08T05:16:13.377","sender":"ssh","connection_id":"968bc05053167ed6f0706e147ba213f3ca83dce34dfeabc3a943d2afae27ee17","message":"ssh command not enabled/supported: \"sftpgo-copy\""}
```

**Causal explanation:** `checkSSHCommands` iterates `EnabledSSHCommands`, keeping only entries in `supportedSSHCommands` and logging the rest as ignored (`sftpd/server.go:L406-411`). Because `sftpgo-copy` is not in this revision's `supportedSSHCommands`, it is dropped exactly like `bogus` — **direct evidence that `sftpgo-copy`/`sftpgo-remove` are absent in `0.9.5-dev`.**

### Q1e — Wildcard `"*"` expands to the entire supported set

Variant config `enabled_ssh_commands=["*"]` with `enable_scp:false`:

```
CONFIG enable_scp= False  enabled_ssh_commands= ['*']
```

Commands that are neither in the default set nor separately enabled are now all accepted:

```
--- reference sha256 of probe.txt content ---
c3e46a42b2fb4576808bc2d8fb6d6b37dd8dd9e81a02a5789557700e83e7eebc  -
--- sha256sum (NOT in default set) should now be ACCEPTED ---
SHA256_EXIT=0
--- scp -O ACCEPTED even though enable_scp=false (scp is in supported set, * expands to it) ---
SCP_O_EXIT=0
--- rsync ACCEPTED ---
RSYNC_EXIT=0
--- git-upload-pack routed to system command (accepted at gate; may fail later for no repo) ---
GITUP_EXIT=255
--- NO unsupported-warning at startup ("*" short-circuits) ---
count of unsupported warnings: 0
```

Server dispatch lines (all accepted; `scp` accepted despite `enable_scp:false`; `git-upload-pack` routed to a system command):

```json
{"level":"debug","time":"2026-07-08T05:16:40.887","sender":"ssh","connection_id":"2fe637411910720d1a019144785f68acc2fcffd12bcda24e099d812c9917fc32","message":"new ssh command: \"sha256sum\" args: [/probe.txt] user: testuser, error: <nil>"}
{"level":"debug","time":"2026-07-08T05:16:40.997","sender":"ssh","connection_id":"2bcda102282524751b050481160a9b34a4525d028893d19e6059e228b6f1d476","message":"new ssh command: \"scp\" args: [-t /wild_scp.txt] user: testuser, error: <nil>"}
{"level":"debug","time":"2026-07-08T05:16:41.148","sender":"ssh","connection_id":"eea67cd5215200f78acff5122f37ae8364b99d3a3229f505315a5b005c046af6","message":"new ssh command: \"rsync\" args: [--server -e.LsfxCIvu . /wild_rs.txt] user: testuser, error: <nil>"}
{"level":"debug","time":"2026-07-08T05:16:41.148","sender":"ssh","connection_id":"eea67cd5215200f78acff5122f37ae8364b99d3a3229f505315a5b005c046af6","message":"new system command \"rsync\", with args: [--safe-links --server -e.LsfxCIvu . /tmp/sftpgo_run/testuser/wild_rs.txt] path: /tmp/sftpgo_run/testuser/wild_rs.txt"}
{"level":"debug","time":"2026-07-08T05:16:41.341","sender":"ssh","connection_id":"cabcf2d1b0cc35933a17810d812e7f776d73bd2cc479ec1f7362bcd50c9663d4","message":"new ssh command: \"git-upload-pack\" args: [/probe.txt] user: testuser, error: <nil>"}
{"level":"debug","time":"2026-07-08T05:16:41.341","sender":"ssh","connection_id":"cabcf2d1b0cc35933a17810d812e7f776d73bd2cc479ec1f7362bcd50c9663d4","message":"new system command \"git-upload-pack\", with args: [/tmp/sftpgo_run/testuser/probe.txt] path: /tmp/sftpgo_run/testuser/probe.txt"}
```

**Causal explanation:** `checkSSHCommands` sees `"*"` first (`sftpd/server.go:L397-399`), replaces the enabled set with `GetSupportedSSHCommands()`, and **returns immediately** — so the per-entry supported-check loop never runs and **zero** "unsupported" warnings are emitted (`count of unsupported warnings: 0`). `git-upload-pack` is accepted at the gate and routed as a *system command* (it later fails with a git error because the target is not a repository, but acceptance + routing is proven). After this experiment the config was **restored to canonical** (`enable_scp=false`, `['md5sum','sha1sum','cd','pwd']`).

### Q1 coverage summary

| Named item | Addressed |
|---|---|
| `supportedSSHCommands` (L66-67) | Q1a/Q1e (full set enumerated; `"*"` expands to it) |
| `defaultSSHCommands` (L68) | baseline enabled set in every run |
| `sshHashCommands` (L69) | Q1e (`sha256sum` accepted); see Q3d |
| `systemCommands` (L70) | Q1e (`git-upload-pack`/`rsync` routed as system commands) |
| `GetDefaultSSHCommands` (L136) / `GetSupportedSSHCommands` (L143) | default set; `"*"` expansion |
| `enable_scp` default false (`config/config.go:L58`, `sftpgo.json:L16`) | Q1a refused / Q1c accepted |
| `enabled_ssh_commands` default (`config/config.go:L63`, `sftpgo.json:L22`) | Q1a/Q1b |
| `checkSSHCommands` (L396-414): `"*"` (L397-399), scp-prepend (L402-404), drop+warn (L406-411) | Q1e / Q1c / Q1d |
| `sftpgo-copy` / `sftpgo-remove` absence | Q1d (dropped as unsupported) |

---


## Q2 — The client / server control boundary

### Direct finding

**The client fully controls the raw command *string*; the server alone decides whether to honor it.** A client can send *any* string in an SSH `exec` request — `whoami`, `ls -la /etc`, `rsync ...`, anything. SFTPGo unmarshals that string, tokenizes it, logs it verbatim (proving it arrived), and then applies the allowlist gate. Only names present in the enabled set are dispatched; everything else is refused with `ssh command not enabled/supported: %#v` and the channel reply is `false`. So the boundary is: **client = arbitrary command text in; server = allowlist decision + dispatch.**

### Code anchors

- Dispatch of the `exec` request — `sftpd/server.go:L326-327`: `ok = processSSHCommand(req.Payload, &connection, channel, c.EnabledSSHCommands)`, and the client is answered at `req.Reply(ok, nil)` (`sftpd/server.go:L329`).
- The decision point — `sftpd/ssh_cmd.go:L45-81` `processSSHCommand`:
  - `ssh.Unmarshal(payload, &msg)` decodes the client's `exec` payload (L47).
  - `parseCommandPayload(msg.Command)` tokenizes it (L48).
  - The client string is logged verbatim: `new ssh command: %#v args: %v user: %v, error: %v` (L49-50).
  - **The gate:** `if err == nil && utils.IsStringInSlice(name, enabledSSHCommands)` (L51). The `scp` branch additionally requires `len(args) >= 2` (L53).
  - Rejection log `ssh command not enabled/supported: %#v` (L77) → returns `false`.

```go
// sftpd/ssh_cmd.go:L45-53
func processSSHCommand(payload []byte, connection *Connection, channel ssh.Channel, enabledSSHCommands []string) bool {
    var msg sshSubsystemExecMsg
    if err := ssh.Unmarshal(payload, &msg); err == nil {
        name, args, err := parseCommandPayload(msg.Command)
        connection.Log(logger.LevelDebug, logSenderSSH, "new ssh command: %#v args: %v user: %v, error: %v",
            name, args, connection.User.Username, err)
        if err == nil && utils.IsStringInSlice(name, enabledSSHCommands) {
            connection.command = fmt.Sprintf("%v %v", name, strings.Join(args, " "))
            if name == "scp" && len(args) >= 2 {
```

```go
// sftpd/ssh_cmd.go:L77
connection.Log(logger.LevelInfo, logSenderSSH, "ssh command not enabled/supported: %#v", name)
```

### Exact commands and complete output (default configuration)

A single `md5sum` is *allowed* (it is in the default set); the arbitrary/disallowed strings `whoami`, `ls -la /etc`, and `rsync ...` are *rejected*:

```
--- CLIENT: ssh <sshopts> -p 2022 testuser@127.0.0.1 "md5sum /probe.txt" ---
323bba2fbd43cb9a41cb5ac14b0d4527  /probe.txt
CLIENT_EXIT=0

--- CLIENT: ssh <sshopts> -p 2022 testuser@127.0.0.1 "whoami" ---
exec request failed on channel 0
CLIENT_EXIT=255

--- CLIENT: ssh <sshopts> -p 2022 testuser@127.0.0.1 "ls -la /etc" ---
exec request failed on channel 0
CLIENT_EXIT=255

--- CLIENT: ssh <sshopts> -p 2022 testuser@127.0.0.1 "rsync --server -vlogDtpre.iLsfxC . /probe.txt" ---
exec request failed on channel 0
CLIENT_EXIT=255
```

Correlated server logs — every client string is first logged verbatim (proving the client fully controlled the text that reached the server), and only the disallowed ones get a rejection line:

```json
{"level":"debug","time":"2026-07-08T05:13:51.039","sender":"ssh","connection_id":"9f4d97eb2851f45a2e1a159972ac1aa87a6a3c97008675aaa8de89bfa3c722f1","message":"new ssh command: \"md5sum\" args: [/probe.txt] user: testuser, error: <nil>"}
{"level":"debug","time":"2026-07-08T05:13:51.156","sender":"ssh","connection_id":"409be92f69466df7b4df9705b8eba724616af6930572bdeeb354cb8e751877c6","message":"new ssh command: \"whoami\" args: [] user: testuser, error: <nil>"}
{"level":"info","time":"2026-07-08T05:13:51.156","sender":"ssh","connection_id":"409be92f69466df7b4df9705b8eba724616af6930572bdeeb354cb8e751877c6","message":"ssh command not enabled/supported: \"whoami\""}
{"level":"debug","time":"2026-07-08T05:13:51.272","sender":"ssh","connection_id":"4f81ae880fcd7d50fc36457ffcc62cb507b0f51898d5736bd48eba33baf7a34c","message":"new ssh command: \"ls\" args: [-la /etc] user: testuser, error: <nil>"}
{"level":"info","time":"2026-07-08T05:13:51.272","sender":"ssh","connection_id":"4f81ae880fcd7d50fc36457ffcc62cb507b0f51898d5736bd48eba33baf7a34c","message":"ssh command not enabled/supported: \"ls\""}
{"level":"debug","time":"2026-07-08T05:13:51.396","sender":"ssh","connection_id":"afe0da09b27e57859becc3b1910b9434f483f8527f4e55a84af06afb593509e6","message":"new ssh command: \"rsync\" args: [--server -vlogDtpre.iLsfxC . /probe.txt] user: testuser, error: <nil>"}
{"level":"info","time":"2026-07-08T05:13:51.396","sender":"ssh","connection_id":"afe0da09b27e57859becc3b1910b9434f483f8527f4e55a84af06afb593509e6","message":"ssh command not enabled/supported: \"rsync\""}
```

### Causal explanation

The `md5sum` string produced `new ssh command: "md5sum" ... error: <nil>` and **no** rejection line, so it passed the `utils.IsStringInSlice(name, enabledSSHCommands)` gate (`sftpd/ssh_cmd.go:L51`) and was dispatched, returning the hash and exit 0. `whoami`, `ls`, and `rsync` each produced a `new ssh command` line (the server received exactly what the client typed) **followed by** `ssh command not enabled/supported`, so `processSSHCommand` returned `false`, and `server.go`'s `req.Reply(false, nil)` (`sftpd/server.go:L329`) is what the client surfaces as `exec request failed on channel 0` / exit 255. Note `ls -la /etc` was tokenized to name=`ls`, args=`[-la /etc]` — the naive space split (see Q4) — and rejected on the **name** `ls`. This is the precise client/server boundary: the client dictates the string; the server's allowlist dictates the outcome.

---


## Q3 — Filtering / validation rules

### Direct finding

Acceptance and validation of SSH commands is layered:

1. **Allowlist membership** decides acceptance, computed by one primitive: `utils.IsStringInSlice` (a linear equality scan). The *same* primitive is used at startup normalization, at the runtime gate, and for command routing.
2. **Command routing** in `handle` classifies an accepted command into one of four handlers: hash commands, system commands, `cd` (no-op, exit 0), or `pwd` (writes a hard-coded `"/"`).
3. **A local-filesystem capability gate** restricts *hash* and *system* commands to the local (OS) backend; on a non-local backend they return `errUnsupportedConfig` ("command unsupported for this configuration").
4. **rsync-specific argument injection**: the destination argument is resolved to an absolute path and substituted, and a symlink-safety flag is injected — `--safe-links` if the user has `create_symlinks`, otherwise `--munge-links`.
5. Spawned system commands run under the user's mapped uid/gid via `wrapCmd`.

### Code anchors

- Allowlist primitive — `utils/utils.go:L20-27` `IsStringInSlice`.
- Router — `sftpd/ssh_cmd.go:L83-103` `handle`: hash → `handleHashCommands` (L87-88); system → `getSystemCommand`+`executeSystemCommand` (L89-94); `cd` → `sendExitStatus(nil)` (L95-96); `pwd` → `channel.Write([]byte("/\n"))` + exit (L97-100).
- Local-fs gate — `handleHashCommands` (`sftpd/ssh_cmd.go:L106-108`) and `executeSystemCommand` (`sftpd/ssh_cmd.go:L152-153`) both `return c.sendErrorResponse(errUnsupportedConfig)` when `!vfs.IsLocalOsFs(c.connection.fs)`. `IsLocalOsFs` = `fs.Name() == osFsName` (`vfs/vfs.go:L67-69`).
- System-command permission set — `executeSystemCommand` (`sftpd/ssh_cmd.go:L158-160`): `HasPerms([download, upload, create_dirs, list, overwrite, delete, rename], getDestPath())`.
- rsync injection — `getSystemCommand` (`sftpd/ssh_cmd.go:L306-322`): `HasPerm(PermCreateSymlinks, ...)` → prepend `--safe-links` if absent (L313-316); else prepend `--munge-links` if absent (L317-320); DEBUG log `new system command %#v, with args: %v path: %v` (L323); `exec.Command` (L324) → `wrapCmd(cmd, uid, gid)` (L325-327).
- uid/gid credential — `sftpd/cmd_unix.go:L10-15` `wrapCmd`: sets `SysProcAttr.Credential = &syscall.Credential{Uid, Gid}` only `if uid > 0 || gid > 0`.
- Error variables — `sftpd/ssh_cmd.go`: `errQuotaExceeded` ("denying write due to space limit", L29), `errPermissionDenied` ("Permission denied. You don't have the permissions to execute this command", L30), `errUnsupportedConfig` ("command unsupported for this configuration", L31).

```go
// utils/utils.go:L20-27
func IsStringInSlice(obj string, list []string) bool {
    for _, v := range list {
        if v == obj {
            return true
        }
    }
    return false
}
```

```go
// sftpd/ssh_cmd.go:L306-327  (rsync symlink-flag injection + spawn)
if c.command == "rsync" {
    // ... comments ...
    if c.connection.User.HasPerm(dataprovider.PermCreateSymlinks, c.getDestPath()) {
        if !utils.IsStringInSlice("--safe-links", args) {
            args = append([]string{"--safe-links"}, args...)
        }
    } else {
        if !utils.IsStringInSlice("--munge-links", args) {
            args = append([]string{"--munge-links"}, args...)
        }
    }
}
c.connection.Log(logger.LevelDebug, logSenderSSH, "new system command %#v, with args: %v path: %v", c.command, args, path)
cmd := exec.Command(c.command, args...)
uid := c.connection.User.GetUID()
gid := c.connection.User.GetGID()
cmd = wrapCmd(cmd, uid, gid)
```

### Q3a / Q3b — rsync symlink-flag injection (both branches, cross-product on `create_symlinks`)

With `rsync` enabled, two users were exercised: `testuser` (`{"/":["*"]}` → **has** `create_symlinks` via `PermAny`) and `nosymuser` (`{"/":[download,upload,create_dirs,list,overwrite,delete,rename]}` → **lacks** `create_symlinks`). `nosymuser` was created via the REST API (id:2). Both real `rsync` client invocations succeeded (files transferred), confirming acceptance:

```
CMD: rsync -e "ssh -p 2022 <sshopts>" /tmp/rsrc.txt testuser@127.0.0.1:/rs_safe.txt
RSYNC_EXIT(testuser)=0
CMD: rsync -e "ssh -p 2022 <sshopts>" /tmp/rsrc.txt nosymuser@127.0.0.1:/rs_munge.txt
RSYNC_EXIT(nosymuser)=0
```

The DEBUG log `new system command "rsync", with args: [...]` shows the injected flag in the *actual argument vector*:

```json
{"level":"debug","time":"2026-07-08T05:15:27.952","sender":"ssh","connection_id":"1194d16436c369470def0a5c37379b4ede651a94e9870b54b2572cfbc9ef4ba8","message":"new ssh command: \"rsync\" args: [--server -e.LsfxCIvu . /rs_safe.txt] user: testuser, error: <nil>"}
{"level":"debug","time":"2026-07-08T05:15:27.952","sender":"ssh","connection_id":"1194d16436c369470def0a5c37379b4ede651a94e9870b54b2572cfbc9ef4ba8","message":"new system command \"rsync\", with args: [--safe-links --server -e.LsfxCIvu . /tmp/sftpgo_run/testuser/rs_safe.txt] path: /tmp/sftpgo_run/testuser/rs_safe.txt"}
{"level":"debug","time":"2026-07-08T05:15:28.153","sender":"ssh","connection_id":"d09c31aa89e40e7c1b4f4467f9198018101375ce2726470a3537715b2ac5b58f","message":"new ssh command: \"rsync\" args: [--server -e.LsfxCIvu . /rs_munge.txt] user: nosymuser, error: <nil>"}
{"level":"debug","time":"2026-07-08T05:15:28.153","sender":"ssh","connection_id":"d09c31aa89e40e7c1b4f4467f9198018101375ce2726470a3537715b2ac5b58f","message":"new system command \"rsync\", with args: [--munge-links --server -e.LsfxCIvu . /tmp/sftpgo_run/nosymuser/rs_munge.txt] path: /tmp/sftpgo_run/nosymuser/rs_munge.txt"}
{"level":"debug","time":"2026-07-08T05:15:28.358","sender":"ssh","connection_id":"0128b0a6b3fb3f461acd15c270f16decbcbdd8c2562c05fd9ea09110e1f3f50c","message":"new ssh command: \"rsync\" args: [--server -e.LsfxCIvu . /rs_safe2.txt] user: testuser, error: <nil>"}
{"level":"debug","time":"2026-07-08T05:15:28.358","sender":"ssh","connection_id":"0128b0a6b3fb3f461acd15c270f16decbcbdd8c2562c05fd9ea09110e1f3f50c","message":"new system command \"rsync\", with args: [--safe-links --server -e.LsfxCIvu . /tmp/sftpgo_run/testuser/rs_safe2.txt] path: /tmp/sftpgo_run/testuser/rs_safe2.txt"}
```

**Observed both branches:**
- `testuser` (has `create_symlinks`) → args begin with **`--safe-links`**.
- `nosymuser` (no `create_symlinks`) → args begin with **`--munge-links`**.

Note also the **last-argument resolution**: the client sent `. /rs_safe.txt`, and the server replaced the trailing `/rs_safe.txt` with the resolved absolute `/tmp/sftpgo_run/testuser/rs_safe.txt` in both the `path:` field and the final arg. This foreshadows Q5 (resolve-then-authorize) — resolution happens as the command is assembled.

**uid/gid credential (`wrapCmd`, `sftpd/cmd_unix.go:L10-15`):** the test users have `uid:0`/`gid:0` (unset), so the `if uid > 0 || gid > 0` guard is **false** and no `syscall.Credential` is attached — the spawned `rsync` runs as the server's own uid. With a non-zero mapped uid/gid the credential would be set. The zero-uid path is what was observed here; the non-zero credential attachment is **(inferred)** from the code because the test users used the default uid/gid of 0.

### Q3c — Local-filesystem gate

On the default **local** backend (`osfs`), hash commands run normally (this is the acceptance path shown throughout — e.g. `md5sum` in Q2 and `sha256sum` in Q1e both returned correct digests, exit 0). The complementary rejection — a *non-local* backend returning `errUnsupportedConfig` — is **(inferred)**: standing up an S3 backend requires AWS credentials/endpoint that are unavailable offline, so that branch could not be exercised at runtime. The mechanism is `handleHashCommands` (`sftpd/ssh_cmd.go:L106-108`) and `executeSystemCommand` (`sftpd/ssh_cmd.go:L152-153`), both returning `errUnsupportedConfig` = `"command unsupported for this configuration"` (`sftpd/ssh_cmd.go:L31`) when `!vfs.IsLocalOsFs(c.connection.fs)`.

```go
// sftpd/ssh_cmd.go:L105-108
func (c *sshCommand) handleHashCommands() error {
    if !vfs.IsLocalOsFs(c.connection.fs) {
        return c.sendErrorResponse(errUnsupportedConfig)
    }
```

### Q3d — Command router: `cd` (no-op) and `pwd` (hard-coded `/`)

Both accepted (in the default set), and each demonstrates a distinct router branch:

```
--- CLIENT: ssh ... "cd"  -> exit 0, output=[]
--- CLIENT: ssh ... "pwd" -> exit 0, output=[/]
```

```json
{"level":"debug","time":"2026-07-08T05:13:51.902","sender":"ssh","connection_id":"529abe438ec5bd75cfb3b49d0d72e36651f97a2eac4316284963004fe358edaa","message":"new ssh command: \"cd\" args: [] user: testuser, error: <nil>"}
{"level":"debug","time":"2026-07-08T05:13:52.011","sender":"ssh","connection_id":"f081d597976e6a82614bdaf91ff722019e0a59eefcdb65c6b7ed711b588f6bd0","message":"new ssh command: \"pwd\" args: [] user: testuser, error: <nil>"}
```

**Causal explanation:** `handle` (`sftpd/ssh_cmd.go:L95-100`) treats `cd` as a no-op exit-0 (empty output) and `pwd` as a hard-coded write of `"/\n"` — `pwd` does not consult the real filesystem; it always reports `/`.

### Q3 — the acceptance primitive is `IsStringInSlice`

The same `utils.IsStringInSlice` primitive (`utils/utils.go:L20-27`) is the single validation gate across all three touch points, correlated across Q1/Q2 evidence:
- **Startup normalization:** `checkSSHCommands` uses it for both the `"*"` check and the `supportedSSHCommands` membership test (`sftpd/server.go:L397,L406`).
- **Runtime acceptance gate:** `processSSHCommand` uses it against `enabledSSHCommands` (`sftpd/ssh_cmd.go:L51`).
- **Command routing:** `handle` uses it against `sshHashCommands` and `systemCommands` (`sftpd/ssh_cmd.go:L87,L89`).

### Q3 coverage summary

| Named item | Addressed |
|---|---|
| `IsStringInSlice` (`utils/utils.go:L20`) | acceptance primitive across Q1/Q2/Q3 |
| `handle` router (L83-103): hash/system/cd/pwd | Q3a/b (system), Q3d (cd, pwd), Q1e (hash) |
| local-fs gate `handleHashCommands` (L106-108), `executeSystemCommand` (L152-153) | Q3c (local observed; non-local inferred) |
| `IsLocalOsFs` (`vfs/vfs.go:L67`) | Q3c mechanism |
| `HasPerms` system set (L158-160) | Q3a/b (system commands run under this perm set) |
| rsync `--safe-links` / `--munge-links` (L306-322) | Q3a/Q3b cross-product |
| DEBUG log `new system command` (L323) | Q3a/Q3b captured |
| `wrapCmd` uid/gid (`cmd_unix.go:L10-15`) | Q3a/b (uid=0 → no credential observed; non-zero inferred) |
| `errUnsupportedConfig` / `errQuotaExceeded` / `errPermissionDenied` (L29-31) | quoted; `errUnsupportedConfig` in Q3c |

---


## Q4 — Delimiter / boundary assumptions under unusual input

### Direct finding

Two parsers carry baked-in delimiter assumptions, and both break in predictable ways on unusual input:

1. **SSH command tokenizer** (`parseCommandPayload`) splits the command string on a **single space** with **no quoting/escaping awareness**. A double space yields an empty token; multi-word commands are split into name + remaining tokens.
2. **SCP upload-header parser** (`parseUploadMessage`) assumes each `C`/`D` header is exactly **three space-separated parts** (`<mode> <size> <name>`). Consequences observed at runtime:
   - a filename **containing a space** produces **more than three** parts → `Error splitting upload message`;
   - a header **not starting with `C` or `D`** → `unknown or invalid upload message`;
   - an **empty name** → `error getting name from upload message, cannot be empty`;
   - a **non-numeric size** → `Error getting size: strconv.ParseInt: ... invalid syntax`.
   In **every** malformed case, **no file is created** (before/after home listing identical).
3. SCP protocol messages are **newline-terminated and read one byte at a time** (`readProtocolMessage`).

### Code anchors

- Tokenizer — `sftpd/ssh_cmd.go:L423-429` `parseCommandPayload`: `parts := strings.Split(command, " ")`; `if len(parts) < 2 { return parts[0], []string{}, nil }`; else `return parts[0], parts[1:], nil`.
- Byte-by-byte newline reader — `sftpd/scp.go:L542-564` `readProtocolMessage`: `buf := make([]byte, 1)`; loop reads 1 byte; `if n == 1 && readed[0] == newLine[0] { break }`.
- Header parser — `sftpd/scp.go:L631-664` `parseUploadMessage`:
  - not `C`/`D` → `unknown or invalid upload message: %v args: %v user: %v` (L635-636);
  - `parts := strings.Split(command, " ")` (L642), `if len(parts) == 3` (L643);
  - `strconv.ParseInt` failure → `Error getting size: %v` (L646-647);
  - `name = parts[2]`; empty → `error getting name from upload message, cannot be empty` (L650-652);
  - otherwise (≠3 parts) → `Error splitting upload message: %v` (L657-658).

```go
// sftpd/ssh_cmd.go:L423-429
func parseCommandPayload(command string) (string, []string, error) {
    parts := strings.Split(command, " ")
    if len(parts) < 2 {
        return parts[0], []string{}, nil
    }
    return parts[0], parts[1:], nil
}
```

```go
// sftpd/scp.go:L631-664 (parseUploadMessage — exactly-three-parts assumption)
func (c *scpCommand) parseUploadMessage(command string) (int64, string, error) {
    var size int64
    var name string
    var err error
    if !strings.HasPrefix(command, "C") && !strings.HasPrefix(command, "D") {
        err = fmt.Errorf("unknown or invalid upload message: %v args: %v user: %v",
            command, c.args, c.connection.User.Username)
        c.connection.Log(logger.LevelWarn, logSenderSCP, "error: %v", err)
        c.sendErrorMessage(err.Error())
        return size, name, err
    }
    parts := strings.Split(command, " ")
    if len(parts) == 3 {
        size, err = strconv.ParseInt(parts[1], 10, 64)
        if err != nil {
            c.connection.Log(logger.LevelWarn, logSenderSCP, "error getting size from upload message: %v", err)
            c.sendErrorMessage(fmt.Sprintf("Error getting size: %v", err))
            return size, name, err
        }
        name = parts[2]
        if len(name) == 0 {
            err = fmt.Errorf("error getting name from upload message, cannot be empty")
            c.connection.Log(logger.LevelWarn, logSenderSCP, "error: %v", err)
            c.sendErrorMessage(err.Error())
            return size, name, err
        }
    } else {
        err = fmt.Errorf("Error splitting upload message: %v", command)
        c.connection.Log(logger.LevelWarn, logSenderSCP, "error: %v", err)
        c.sendErrorMessage(err.Error())
        return size, name, err
    }
    return size, name, err
}
```

### Method

With `scp` enabled, the real SCP sink was driven by piping crafted protocol bytes into the genuine `scp -t` server command over an SSH `exec` channel: `printf '<header>' | ssh <sshopts> -p 2022 testuser@127.0.0.1 "scp -t /upload"`. The client stdout is shown via `od -c` so the raw SCP framing is visible: `\0` = 0x00 success-ack, `\002` = 0x02 fatal-error frame, followed by the server's message text.

**Before-state** (testuser home) — only `probe.txt`:

```
total 12
-rw-r--r-- 1 root root   12 Jul  8 05:19 probe.txt
```

### Q4b — Filename containing a space → `Error splitting upload message`

```
CLIENT stdin bytes (crafted SCP header): C0644 6 file with spaces\n
CMD: printf 'C0644 6 file with spaces\n' | ssh <sshopts> -p 2022 testuser@127.0.0.1 "scp -t /upload"
CLIENT stdout (od -c, shows \0 confirm + \002 error frame):
0000000  \0 002   E   r   r   o   r       s   p   l   i   t   t   i   n
0000020   g       u   p   l   o   a   d       m   e   s   s   a   g   e
0000040   :       C   0   6   4   4       6       f   i   l   e       w
0000060   i   t   h       s   p   a   c   e   s  \n
0000073
CLIENT_EXIT=255
```

### Q4c — Header not starting with `C`/`D` → `unknown or invalid upload message`

```
CLIENT stdin bytes (crafted SCP header): X0644 6 test\n
CMD: printf 'X0644 6 test\n' | ssh <sshopts> -p 2022 testuser@127.0.0.1 "scp -t /upload"
CLIENT stdout (od -c, shows \0 confirm + \002 error frame):
0000000  \0 002   u   n   k   n   o   w   n       o   r       i   n   v
0000020   a   l   i   d       u   p   l   o   a   d       m   e   s   s
0000040   a   g   e   :       X   0   6   4   4       6       t   e   s
0000060   t       a   r   g   s   :       [   -   t       /   u   p   l
0000100   o   a   d   ]       u   s   e   r   :       t   e   s   t   u
0000120   s   e   r  \n
0000122
CLIENT_EXIT=255
```

### Q4d-i — Empty name → `error getting name from upload message, cannot be empty`

```
CLIENT stdin bytes (crafted SCP header): C0644 6 \n
CMD: printf 'C0644 6 \n' | ssh <sshopts> -p 2022 testuser@127.0.0.1 "scp -t /upload"
CLIENT stdout (od -c, shows \0 confirm + \002 error frame):
0000000  \0 002   e   r   r   o   r       g   e   t   t   i   n   g    
0000020   n   a   m   e       f   r   o   m       u   p   l   o   a   d
0000040       m   e   s   s   a   g   e   ,       c   a   n   n   o   t
0000060       b   e       e   m   p   t   y  \n
0000072
CLIENT_EXIT=255
```

### Q4d-ii — Non-numeric size → `Error getting size: strconv.ParseInt`

```
CLIENT stdin bytes (crafted SCP header): C0644 XX test\n
CMD: printf 'C0644 XX test\n' | ssh <sshopts> -p 2022 testuser@127.0.0.1 "scp -t /upload"
CLIENT stdout (od -c, shows \0 confirm + \002 error frame):
0000000  \0 002   E   r   r   o   r       g   e   t   t   i   n   g    
0000020   s   i   z   e   :       s   t   r   c   o   n   v   .   P   a
0000040   r   s   e   I   n   t   :       p   a   r   s   i   n   g    
0000060   "   X   X   "   :       i   n   v   a   l   i   d       s   y
0000100   n   t   a   x  \n
0000105
CLIENT_EXIT=255
```

### Complete server logs for all Q4 headers (exact text; each case run twice → reproducible)

```json
{"level":"debug","time":"2026-07-08T05:19:48.146","sender":"scp","connection_id":"5c9c0e03ed569799098549843c5435a8afa6f0a5307546a559f879d215bc2690","message":"handle scp command, args: [-t /upload] user: testuser command type: -t, dest path: \"/upload\""}
{"level":"warn","time":"2026-07-08T05:19:48.146","sender":"scp","connection_id":"5c9c0e03ed569799098549843c5435a8afa6f0a5307546a559f879d215bc2690","message":"error: Error splitting upload message: C0644 6 file with spaces"}
{"level":"debug","time":"2026-07-08T05:19:48.253","sender":"scp","connection_id":"54e28f767ea17fb3251138c66b0b96507c3adc5f68ff92de047f2f47ac58713b","message":"handle scp command, args: [-t /upload] user: testuser command type: -t, dest path: \"/upload\""}
{"level":"warn","time":"2026-07-08T05:19:48.253","sender":"scp","connection_id":"54e28f767ea17fb3251138c66b0b96507c3adc5f68ff92de047f2f47ac58713b","message":"error: Error splitting upload message: C0644 6 file with spaces"}
{"level":"debug","time":"2026-07-08T05:19:48.407","sender":"scp","connection_id":"afc357cb8d571f5eb3f5457921f42540b635596fe949df4564a2221671914f5e","message":"handle scp command, args: [-t /upload] user: testuser command type: -t, dest path: \"/upload\""}
{"level":"warn","time":"2026-07-08T05:19:48.408","sender":"scp","connection_id":"afc357cb8d571f5eb3f5457921f42540b635596fe949df4564a2221671914f5e","message":"error: unknown or invalid upload message: X0644 6 test args: [-t /upload] user: testuser"}
{"level":"debug","time":"2026-07-08T05:19:48.474","sender":"scp","connection_id":"eef1be4f3a1361e574899881ccb2a60f97b06fe8644c0119925e2a9670e84f3e","message":"handle scp command, args: [-t /upload] user: testuser command type: -t, dest path: \"/upload\""}
{"level":"warn","time":"2026-07-08T05:19:48.474","sender":"scp","connection_id":"eef1be4f3a1361e574899881ccb2a60f97b06fe8644c0119925e2a9670e84f3e","message":"error: unknown or invalid upload message: X0644 6 test args: [-t /upload] user: testuser"}
{"level":"debug","time":"2026-07-08T05:19:48.594","sender":"scp","connection_id":"bea7267ec4bb622cbe02f0f102f297709441feeb32dfe5d2f6623255d50dc4e8","message":"handle scp command, args: [-t /upload] user: testuser command type: -t, dest path: \"/upload\""}
{"level":"warn","time":"2026-07-08T05:19:48.594","sender":"scp","connection_id":"bea7267ec4bb622cbe02f0f102f297709441feeb32dfe5d2f6623255d50dc4e8","message":"error: error getting name from upload message, cannot be empty"}
{"level":"debug","time":"2026-07-08T05:19:48.705","sender":"scp","connection_id":"d2ff9aa9fbf77d98cb07f5b4ac5d0a61ecbfcf1f5074f4519261cb3191fbc135","message":"handle scp command, args: [-t /upload] user: testuser command type: -t, dest path: \"/upload\""}
{"level":"warn","time":"2026-07-08T05:19:48.705","sender":"scp","connection_id":"d2ff9aa9fbf77d98cb07f5b4ac5d0a61ecbfcf1f5074f4519261cb3191fbc135","message":"error: error getting name from upload message, cannot be empty"}
{"level":"debug","time":"2026-07-08T05:19:48.861","sender":"scp","connection_id":"7253439638f2ecbaea98870eb697b75cb21f36423d0c93e137fe2583d4e71af3","message":"handle scp command, args: [-t /upload] user: testuser command type: -t, dest path: \"/upload\""}
{"level":"warn","time":"2026-07-08T05:19:48.861","sender":"scp","connection_id":"7253439638f2ecbaea98870eb697b75cb21f36423d0c93e137fe2583d4e71af3","message":"error getting size from upload message: strconv.ParseInt: parsing \"XX\": invalid syntax"}
{"level":"debug","time":"2026-07-08T05:19:48.967","sender":"scp","connection_id":"d8392813de33db24582f841c91faf5a96555fdb83e97b7ea8dfb8bf97bcb0842","message":"handle scp command, args: [-t /upload] user: testuser command type: -t, dest path: \"/upload\""}
{"level":"warn","time":"2026-07-08T05:19:48.967","sender":"scp","connection_id":"d8392813de33db24582f841c91faf5a96555fdb83e97b7ea8dfb8bf97bcb0842","message":"error getting size from upload message: strconv.ParseInt: parsing \"XX\": invalid syntax"}
```

**After-state** (testuser home) — still only `probe.txt`; **no file created by any malformed header**:

```
total 12
-rw-r--r-- 1 root root   12 Jul  8 05:19 probe.txt
```

### Q4e — The SSH command tokenizer (`parseCommandPayload`) single-space split

Three inputs exercised via `exec` (server logs show the resulting `name` + `args` split):

```
--- (a) normal single space: md5sum /probe.txt ---
323bba2fbd43cb9a41cb5ac14b0d4527  /probe.txt      # EXIT=0
--- (b) DOUBLE space: "md5sum  /probe.txt" (empty middle token) ---
EXIT=0
--- (c) multiword: "ls -la /etc" tokenized name=ls args=[-la /etc] ---
EXIT=255
```

```json
{"level":"debug","time":"2026-07-08T05:19:50.094","sender":"ssh","connection_id":"3461e3807fd2c90a1659892113af5efde587ce961eff27aad51e7a2d2c69a134","message":"new ssh command: \"md5sum\" args: [/probe.txt] user: testuser, error: <nil>"}
{"level":"debug","time":"2026-07-08T05:19:50.263","sender":"ssh","connection_id":"833059847b38bac54b5c31e19866cef93a43ab7e9bc581126ebe3a30e092dc32","message":"new ssh command: \"md5sum\" args: [ /probe.txt] user: testuser, error: <nil>"}
{"level":"debug","time":"2026-07-08T05:19:50.370","sender":"ssh","connection_id":"a4a8685f366b46cc551ea5a0ef1a93f58562ab63fb47777a7f1f91f36062cb64","message":"new ssh command: \"ls\" args: [-la /etc] user: testuser, error: <nil>"}
```

**Causal explanation:** In (b), splitting `"md5sum  /probe.txt"` (two spaces) on a single space yields `["md5sum", "", "/probe.txt"]`, so `args = ["", "/probe.txt"]` — visible in the log as `args: [ /probe.txt]` (note the leading empty token). The command still succeeds with exit 0 because `getDestPath` uses the **last** argument, `/probe.txt`. In (c), `"ls -la /etc"` becomes name=`ls`, args=`[-la /etc]`; the whole thing is rejected on the **name** `ls`. This is the same naive delimiter assumption the SCP header parser makes — there is no quoting/escaping layer.

### Q4 coverage summary

| Named item | Addressed |
|---|---|
| `parseCommandPayload` single-space split (L423-429) | Q4e (normal / double-space / multiword) |
| `readProtocolMessage` byte-by-byte newline (L542-564) | mechanism cited; all Q4 headers are `\n`-terminated single messages consumed by it |
| not `C`/`D` → `unknown or invalid upload message` (L635-636) | Q4c |
| `len(parts)==3` assumption (L643) | Q4b (space → >3 parts) |
| `Error splitting upload message` (L657-658) | Q4b |
| empty name → `cannot be empty` (L650-652) | Q4d-i |
| bad size → `Error getting size` (L646-647) | Q4d-ii |
| before/after (no file created) | Q4 before/after home listing |

---


## Q5 — Where permission checks sit in the chain (resolve-then-authorize)

### Direct finding

**Path resolution/normalization runs *before* the authorization check.** In the SCP upload path, `ResolvePath` is called first; only afterward is `HasPerm` consulted — `PermUpload` on the parent directory for a *new* file, or `PermOverwrite` on the target for an *existing* file. The permission model itself is a **per-directory, deepest-match** lookup: `GetPermissionsForPath` returns the permissions of the **most specific** configured directory that is a prefix of the target, so a permission granted at `/` can be **overridden** (narrowed) at a subdirectory. This ordering matters: a path that resolves successfully can still be denied, and the denial is decided by the *deepest* matching directory's permission set — not by the root grant.

### Code anchors

- SCP upload — `sftpd/scp.go:L226-285` `handleUpload`:
  - `p, err := c.connection.fs.ResolvePath(uploadFilePath)` **first** (L231); on error → `error uploading file` + return (L232-235).
  - `stat, statErr := c.connection.fs.Stat(p)` (L241).
  - new file (does not exist): `if !c.connection.User.HasPerm(dataprovider.PermUpload, path.Dir(uploadFilePath))` → `Permission denied`, log `cannot upload file: %#v, permission denied` (L242-248).
  - existing file: `if !c.connection.User.HasPerm(dataprovider.PermOverwrite, uploadFilePath)` → `Permission denied`, log `cannot overwrite file: %#v, permission denied` (L265-270).
- Hash path mirrors it: `handleHashCommands` resolves (`ResolvePath`, `sftpd/ssh_cmd.go:L133`) **before** `HasPerm(PermListItems, sshPath)` (`sftpd/ssh_cmd.go:L137`).
- System-command path: `getDestPath` normalizes (trim quotes → `filepath.ToSlash` → force leading `/` → `path.Clean`, `sftpd/ssh_cmd.go:L356-371`) **before** `executeSystemCommand`'s `HasPerms` (`sftpd/ssh_cmd.go:L158-160`).
- Permission model — `dataprovider/user.go`: constants L17-42 (`PermAny="*"` L19; `PermListItems="list"` L21; `PermDownload="download"` L23; `PermUpload="upload"` L25; `PermOverwrite="overwrite"` L28; `PermDelete="delete"` L30; `PermRename="rename"` L32; `PermCreateDirs="create_dirs"` L34; `PermCreateSymlinks="create_symlinks"` L36; `PermChmod="chmod"` L38; `PermChown="chown"` L40; `PermChtimes="chtimes"` L42); `Permissions map[string][]string` (L93); `GetPermissionsForPath` (L122-156); `HasPerm` (L159-165); `HasPerms` (L168-179).

```go
// sftpd/scp.go:L231 (resolve FIRST) ... L242-248 (new-file authz) ... L265-270 (overwrite authz)
p, err := c.connection.fs.ResolvePath(uploadFilePath)   // L231
// ...
stat, statErr := c.connection.fs.Stat(p)                // L241
if c.connection.fs.IsNotExist(statErr) {
    if !c.connection.User.HasPerm(dataprovider.PermUpload, path.Dir(uploadFilePath)) { // L242-243
        err := fmt.Errorf("Permission denied")
        c.connection.Log(logger.LevelWarn, logSenderSCP, "cannot upload file: %#v, permission denied", uploadFilePath)
        // ...
    }
    // ...
}
// existing file:
if !c.connection.User.HasPerm(dataprovider.PermOverwrite, uploadFilePath) { // L265
    err := fmt.Errorf("Permission denied")
    c.connection.Log(logger.LevelWarn, logSenderSCP, "cannot overwrite file: %#v, permission denied", uploadFilePath)
    // ...
}
```

```go
// dataprovider/user.go:L122-156 (deepest-match; note the single-root short-circuit at L124-127)
func (u *User) GetPermissionsForPath(p string) []string {
    permissions := []string{}
    if perms, ok := u.Permissions["/"]; ok {
        // if only root permissions are defined returns them unconditionally
        if len(u.Permissions) == 1 {
            return perms
        }
        permissions = perms  // fallback
    }
    // ... builds dirsForPath in reverse (deepest first) ...
    for _, val := range dirsForPath {
        if perms, ok := u.Permissions[val]; ok {
            permissions = perms
            break   // first (deepest) match wins
        }
    }
    return permissions
}
```

> Because of the `len(u.Permissions) == 1` short-circuit (`dataprovider/user.go:L124-127`), a user whose map has **only** `"/"` gets those permissions everywhere; to observe deepest-match overriding, the map must define **more than one** directory. All Q5 users below therefore define multiple paths.

### Q5a — Upload denied in a list-only dir, allowed in an upload dir

`permuser` permissions: `{"/":[list,download], "/upload":[upload,list], "/ro":[list], "/full":[*]}`.

**Before** (`/ro` and `/upload` both empty):

```
/tmp/sftpgo_run/permuser/ro:
total 8
/tmp/sftpgo_run/permuser/upload:
total 8
```

Denied into `/ro` (only `list`), allowed into `/upload` (has `upload`):

```
===== Q5a-denied /ro/a.txt : scp -O /src.txt permuser@:/ro/a.txt =====
Permission denied
SCP_EXIT=1
===== Q5a-allowed /upload/a.txt : scp -O /src.txt permuser@:/upload/a.txt =====
SCP_EXIT=0
```

**After** (`/ro` still empty; `/upload/a.txt` created, 8 bytes):

```
/tmp/sftpgo_run/permuser/ro:
total 8
/tmp/sftpgo_run/permuser/upload:
total 12
-rw-r--r-- 1 root root    8 Jul  8 05:20 a.txt
```

### Q5b — New-file (`PermUpload`) vs overwrite (`PermOverwrite`) cross-product

`/upload` has `[upload,list]` but **not** `overwrite`; `/full` has `[*]`. Running the full cross-product of {new, overwrite} × {upload-only dir, full dir}:

```
===== Q5b new /upload/b.txt (PermUpload => ALLOWED) =====
SCP_EXIT=0
===== Q5b OVERWRITE /upload/b.txt (needs PermOverwrite => DENIED) =====
Permission denied
SCP_EXIT=1
===== Q5b new /full/c.txt (PermAny => ALLOWED) =====
SCP_EXIT=0
===== Q5b OVERWRITE /full/c.txt (PermAny => ALLOWED) =====
SCP_EXIT=0
```

Server permission-denied lines for Q5a + Q5b (exact text — note **`cannot upload file`** vs **`cannot overwrite file`**, matching the two distinct anchors):

```json
{"level":"warn","time":"2026-07-08T05:20:59.770","sender":"scp","connection_id":"af14f8508ecff3bba968a354cbbbf300284da7bfc2fff8768ec4b6a1106b87e8","message":"cannot upload file: \"/ro/a.txt\", permission denied"}
{"level":"warn","time":"2026-07-08T05:21:00.158","sender":"scp","connection_id":"a1373dbead1bc5daac74f9837e1eb80ed57bcdc4c224559975e004292251d9fe","message":"cannot overwrite file: \"/upload/b.txt\", permission denied"}
```

**Causal explanation:** the *new* file into `/upload` checks `PermUpload` on the parent dir (`sftpd/scp.go:L242-243`) → granted. The *overwrite* of the now-existing `/upload/b.txt` checks `PermOverwrite` on the target (`sftpd/scp.go:L265`) → `/upload` lacks `overwrite` → denied. In `/full`, `PermAny` satisfies both `HasPerm` checks (`dataprovider/user.go:L161`), so new and overwrite both succeed. This is the exact `PermUpload`-vs-`PermOverwrite` distinction (upload permission gates creation; overwrite permission is separately required to replace an existing file).

### Q5c — Deepest-match overrides a root grant

`deepuser` permissions: `{"/":["*"], "/sub":["list"]}`. Upload to root succeeds (root `*`); upload to `/sub/x.txt` is **denied** even though root has `*`, reproducibly across two runs:

```
===== Q5c-i: upload to ROOT /top.txt (root has * => ALLOWED) =====
SCP_EXIT(/top.txt)=0
===== Q5c-ii: upload to /sub/x.txt (deepest match /sub=[list] => DENIED although root has *) =====
SCP_EXIT(/sub/x.txt)=1
-- reproduce run 2 --
SCP_EXIT_run2=1
```

```json
{"level":"warn","time":"2026-07-08T05:21:24.202","sender":"scp","connection_id":"755bb4d885f0f3b14b7900c6bb9ebd24c50d817fcd8254e1654d653825325196","message":"cannot upload file: \"/sub/x.txt\", permission denied"}
{"level":"warn","time":"2026-07-08T05:21:24.309","sender":"scp","connection_id":"82994e6e7d2400e79a28e6d1e6a6696dacfd816a23a3630fb92863643b92df5b","message":"cannot upload file: \"/sub/x2.txt\", permission denied"}
```

**After** (root has `top.txt`; `/sub` has no `x.txt`):

```
-- root --
drwxr-xr-x 2 root root 4096 Jul  8 05:21 sub
-rw-r--r-- 1 root root    5 Jul  8 05:21 top.txt
-- /sub --
total 8
(no x.txt)
```

**Causal explanation:** for target `/sub/x.txt`, `GetPermissionsForPath` builds the dir chain deepest-first (`["/sub/x.txt", "/sub", "/"]`) and returns the **first** configured match — `/sub` → `[list]` (`dataprovider/user.go:L145-154`). `list` does not include `upload` or `*`, so `HasPerm(PermUpload, "/sub")` is false and the upload is denied, **despite** the root `*` grant.

### Q5d — Resolution precedes authorization (ordering proof)

The `/ro` denial (Q5a) is itself the ordering proof: there is **no** `error uploading file` log for `/ro/a.txt`, which means `ResolvePath("/ro/a.txt")` (`sftpd/scp.go:L231`) returned **successfully**; execution then reached the `HasPerm(PermUpload, "/ro")` gate (`sftpd/scp.go:L242-243`) and produced `cannot upload file: "/ro/a.txt", permission denied`. Resolution came first; authorization is the gate that fired second. (If resolution had failed, the log would read `error uploading file: ..., err: ...` and the permission check would never be reached.)

### Q5 coverage summary

| Named item | Addressed |
|---|---|
| `handleUpload` resolve-first (L231) | Q5d ordering proof |
| `PermUpload` on parent for new file (L242-243) | Q5a/Q5b (allowed) |
| `PermOverwrite` for existing file (L265) | Q5b (overwrite denied) |
| permission constants L17-42 | enumerated; `list`/`upload`/`overwrite`/`*` exercised |
| `Permissions` map (L93) | all Q5 users |
| `GetPermissionsForPath` deepest-match (L122-156) incl. single-root short-circuit (L124-127) | Q5c |
| `HasPerm` (L159-165) / `HasPerms` (L168-179) | Q5a/b/c (HasPerm); Q3a/b (HasPerms) |
| resolve-then-authorize ordering | Q5d |

---


## Q6 — Protections against filesystem tricks (path traversal / containment)

### Direct finding

SFTPGo's local-backend protection is `OsFs.ResolvePath`, which **joins the request onto the user's root, `filepath.Clean`s it, evaluates symlinks, and then confirms containment** via `isSubDir`. Two distinct outcomes were observed:

1. **`..` traversal is BLOCKED — but by *cleaning at the virtual root*, not primarily by the containment check.** Because the request path is forced to be absolute and `path.Clean`ed *relative to the user's home*, sequences like `../../../../../../etc/passwd` collapse to a path that stays under the home dir (e.g. `.../testuser/etc/passwd`), which simply does not exist → "not found". For pure `..` payloads, the `isSubDir` "is not inside" error does **not** even fire — the cleaning neutralizes the traversal earlier.
2. **The `isSubDir` containment check *does* engage for symlink escapes, and it has a real coverage limit.** Its test is a **bare `strings.HasPrefix(sub, parent)` with no trailing-separator boundary** (`vfs/osfs.go:L285`). This blocks a symlink to `/etc` and a symlink to a *non-prefix* sibling directory — but a symlink to a **sibling directory whose absolute path shares the home dir as a string prefix** (home `.../testuser`, sibling `.../testuser-evil`) **passes** the check, allowing reads outside the home. This limit was **manifested at runtime** (a secret file in the sibling was exfiltrated). **Per the read-only scope, this is DOCUMENTED and DEMONSTRATED, not patched.**

### Code anchors

- `vfs/osfs.go:L200-223` `ResolvePath`: reject non-absolute root (L201-203); `r := filepath.Clean(filepath.Join(fs.rootDir, sftpPath))` (L204); `filepath.EvalSymlinks(r)` (L205); if not-yet-existing → `findFirstExistingDir` walk-up (L211-215); else `isSubDir(p, fs.rootDir)` (L218); warn `Invalid path resolution, dir: %#v outside user home: %#v err: %v` (L220).
- `vfs/osfs.go:L278-291` `isSubDir`: `parent, err := filepath.EvalSymlinks(rootPath)` (L280, err **is** checked L281-284); containment `if !strings.HasPrefix(sub, parent)` (L285, **no trailing separator**); error `path %#v is not inside: %#v` (L286).
- Virtual-root cleaning for the system/hash paths — `getDestPath` forces a leading `/` then `path.Clean` (`sftpd/ssh_cmd.go:L363-366`); the SFTP layer applies the equivalent normalization before `ResolvePath`.
- Local backend gate — `IsLocalOsFs` (`vfs/vfs.go:L67`).

```go
// vfs/osfs.go:L200-223 (ResolvePath — join, clean, eval-symlinks, contain)
func (fs OsFs) ResolvePath(sftpPath string) (string, error) {
    if !filepath.IsAbs(fs.rootDir) {
        return "", fmt.Errorf("Invalid root path: %v", fs.rootDir)
    }
    r := filepath.Clean(filepath.Join(fs.rootDir, sftpPath))
    p, err := filepath.EvalSymlinks(r)
    if err != nil && !os.IsNotExist(err) {
        return "", err
    } else if os.IsNotExist(err) {
        _, err = fs.findFirstExistingDir(r, fs.rootDir)
        if err != nil {
            fsLog(fs, logger.LevelWarn, "error resolving not existent path: %#v", err)
        }
        return r, err
    }
    err = fs.isSubDir(p, fs.rootDir)
    if err != nil {
        fsLog(fs, logger.LevelWarn, "Invalid path resolution, dir: %#v outside user home: %#v err: %v", p, fs.rootDir, err)
    }
    return r, err
}
```

```go
// vfs/osfs.go:L278-291 (isSubDir — bare HasPrefix, NO trailing-separator boundary)
func (fs *OsFs) isSubDir(sub, rootPath string) error {
    parent, err := filepath.EvalSymlinks(rootPath)
    if err != nil {
        fsLog(fs, logger.LevelWarn, "invalid home dir %#v: %v", rootPath, err)
        return err
    }
    if !strings.HasPrefix(sub, parent) {
        err = fmt.Errorf("path %#v is not inside: %#v", sub, parent)
        fsLog(fs, logger.LevelWarn, "error: %v ", err)
        return err
    }
    return nil
}
```

### Q6a — `..` traversal is blocked (before/after: no escape)

`testuser` (`{"/":["*"]}`, so *only* containment/cleaning — not permissions — can block). **Before**: `/etc/sftpgo_evil.txt` absent.

```
### BEFORE: /etc has no sftpgo_evil.txt; /tmp has no stolen file ###
ls: cannot access '/etc/sftpgo_evil.txt': No such file or directory
```

Real `sftp` and `scp -O` traversal attempts:

```
===== Q6a-i: SFTP get ../..(x6)/etc/passwd  and  put into ../..(x6)/etc/sftpgo_evil.txt =====
Connected to 127.0.0.1.
sftp> get ../../../../../../etc/passwd /tmp/stolen_via_sftp.txt
File "/../../../../../../etc/passwd" not found.
sftp> put /tmp/src.txt ../../../../../../etc/sftpgo_evil.txt
Uploading /tmp/src.txt to /../../../../../../etc/sftpgo_evil.txt
dest open "/../../../../../../etc/sftpgo_evil.txt": No such file or directory

===== Q6a-ii: SFTP with leading-slash absolute traversal /../../etc/passwd =====
Connected to 127.0.0.1.
sftp> get /../../../../../../etc/passwd /tmp/stolen2.txt
File "/../../../../../../etc/passwd" not found.

===== Q6a-iii: scp -O download traversal (scp -O testuser@:../../etc/passwd) =====
stat /tmp/sftpgo_run/testuser/etc/passwd: no such file or directory
```

For the interactive `sftp` client each `get`/`put` reports its per-operation outcome as the printed status line (here `File "..." not found.` / `dest open "...": No such file or directory`), and the `scp -O` download surfaced its failure as `stat /tmp/sftpgo_run/testuser/etc/passwd: no such file or directory` — those messages **are** the complete client-visible result of each attempt.

**After**: nothing escaped — the target file was never created and no host file was exfiltrated:

```
### AFTER: no escape — /etc/sftpgo_evil.txt must NOT exist ###
ls: cannot access '/etc/sftpgo_evil.txt': No such file or directory
stolen files (should be empty/nonexistent since traversal blocked):
ls: cannot access '/tmp/stolen_via_sftp.txt': No such file or directory
ls: cannot access '/tmp/stolen2.txt': No such file or directory
ls: cannot access '/tmp/stolen_scp.txt': No such file or directory
sizes (0 or missing = nothing exfiltrated):
/tmp/stolen_via_sftp.txt MISSING
/tmp/stolen2.txt MISSING
/tmp/stolen_scp.txt MISSING
```

**Causal explanation:** the SCP download error is the most telling — `stat /tmp/sftpgo_run/testuser/etc/passwd: no such file or directory`. The `../../` sequence was **collapsed against the virtual root** (`getDestPath`'s force-leading-`/` + `path.Clean`, `sftpd/ssh_cmd.go:L363-366`; the SFTP layer does the equivalent) into `.../testuser/etc/passwd` — i.e. it stayed *inside* the home and simply pointed at a nonexistent file. For these pure-`..` payloads the `isSubDir` "is not inside" error **never fires**, because cleaning neutralized the traversal before containment mattered. Either way, **no escape occurred**.

### Q6b — `isSubDir` engages for symlink escapes — and its sibling-prefix coverage limit (manifested at runtime)

To reach the containment check (rather than have `..` cleaned away), in-home **symlinks** were created pointing outside the home, plus sibling directories. The string-prefix relationship was confirmed first:

```
home=/tmp/sftpgo_run/testuser ; sibling=/tmp/sftpgo_run/testuser-evil (shares prefix "testuser")
HasPrefix check: does "/tmp/sftpgo_run/testuser-evil" start with "/tmp/sftpgo_run/testuser"? True
```

Layout: sibling `/tmp/sftpgo_run/testuser-evil/secret.txt` (content `SIBLING-PREFIX SECRET`), non-prefix sibling `/tmp/sftpgo_run/otherdir/secret.txt`, and in-home symlinks `evil_link → testuser-evil`, `other_link → otherdir`, `etc_link → /etc`.

Reads through the symlinks via the real `sftp` client:

```
===== Q6b: read through symlinks via REAL sftp client =====
Connected to 127.0.0.1.
sftp> get evil_link/secret.txt /tmp/got_sibling.txt
Fetching /evil_link/secret.txt to /tmp/got_sibling.txt
sftp> get other_link/secret.txt /tmp/got_other.txt
File "/other_link/secret.txt" not found.
sftp> get etc_link/passwd /tmp/got_etc.txt
File "/etc_link/passwd" not found.
```

Server containment logs — `isSubDir` fires the **exact** `is not inside` error for the two *blocked* targets (`/etc` and the *non-prefix* sibling `otherdir`), but there is **no such log for the prefix-sharing sibling**:

```json
{"level":"warn","time":"2026-07-08T05:24:01.510","sender":"osfs","connection_id":"27c4b111ddc3812551a379ab7d5b4daa64cad00422f648a4b1ecb0af969e909e","message":"error: path \"/tmp/sftpgo_run/otherdir/secret.txt\" is not inside: \"/tmp/sftpgo_run/testuser\" "}
{"level":"warn","time":"2026-07-08T05:24:01.510","sender":"osfs","connection_id":"27c4b111ddc3812551a379ab7d5b4daa64cad00422f648a4b1ecb0af969e909e","message":"Invalid path resolution, dir: \"/tmp/sftpgo_run/otherdir/secret.txt\" outside user home: \"/tmp/sftpgo_run/testuser\" err: path \"/tmp/sftpgo_run/otherdir/secret.txt\" is not inside: \"/tmp/sftpgo_run/testuser\""}
{"level":"warn","time":"2026-07-08T05:24:01.510","sender":"osfs","connection_id":"27c4b111ddc3812551a379ab7d5b4daa64cad00422f648a4b1ecb0af969e909e","message":"error: path \"/etc/passwd\" is not inside: \"/tmp/sftpgo_run/testuser\" "}
{"level":"warn","time":"2026-07-08T05:24:01.510","sender":"osfs","connection_id":"27c4b111ddc3812551a379ab7d5b4daa64cad00422f648a4b1ecb0af969e909e","message":"Invalid path resolution, dir: \"/etc/passwd\" outside user home: \"/tmp/sftpgo_run/testuser\" err: path \"/etc/passwd\" is not inside: \"/tmp/sftpgo_run/testuser\""}
```

Result — the prefix-sharing sibling read **succeeded** (secret exfiltrated), the others were **blocked**; reproducible across two runs:

```
===== RESULT: which reads succeeded? =====
/tmp/got_sibling.txt => EXFILTRATED content=[SIBLING-PREFIX SECRET] size=21
/tmp/got_other.txt => BLOCKED (no file)
/tmp/got_etc.txt => BLOCKED (no file)
===== reproduce sibling-prefix read (run 2) =====
Connected to 127.0.0.1.
sftp> get evil_link/secret.txt /tmp/got_sibling2.txt
Fetching /evil_link/secret.txt to /tmp/got_sibling2.txt
run2 sibling content=[SIBLING-PREFIX SECRET]
```

**Causal explanation:** `isSubDir` computes `parent = EvalSymlinks(home) = /tmp/sftpgo_run/testuser` and tests `strings.HasPrefix(sub, parent)` (`vfs/osfs.go:L285`).
- `/etc/passwd` → does not start with `/tmp/sftpgo_run/testuser` → **blocked** (`is not inside`).
- `/tmp/sftpgo_run/otherdir/secret.txt` → does not start with the home prefix → **blocked**.
- `/tmp/sftpgo_run/testuser-evil/secret.txt` → **does** start with the string `/tmp/sftpgo_run/testuser` (the next character is `-`, not a path separator) → `HasPrefix` is **true** → `isSubDir` returns `nil` → access to the **sibling** is allowed.

This is the exact coverage limit of a prefix test that lacks a trailing-separator boundary: a correct containment test would compare against `parent + string(os.PathSeparator)` (or verify the next rune is a separator). The check *does* provide real protection (it blocked `/etc` and the non-prefix sibling), but a sibling directory sharing the home's name as a prefix slips through. **Documented and demonstrated only — not patched, per the read-only scope (AAP §0.5.2).** All sibling directories and symlinks created for this probe were removed afterward.

### Q6 coverage summary

| Named item | Addressed |
|---|---|
| `ResolvePath` join+clean+eval-symlinks+contain (L200-223) | Q6a (clean neutralizes `..`), Q6b (containment path) |
| virtual-root `path.Clean` (`getDestPath` L363-366 / SFTP layer) | Q6a mechanism (`..` collapsed inside home) |
| `isSubDir` bare `strings.HasPrefix`, no trailing sep (L285) | Q6b (blocks `/etc` & non-prefix sibling; lets prefix-sibling through) |
| error `path %#v is not inside: %#v` (L286) | Q6b server logs |
| warn `Invalid path resolution ... outside user home` (L220) | Q6b server logs |
| `findFirstExistingDir` walk-up (L250-276) | invoked for not-yet-existing upload paths (Q6a `put`) |
| `IsLocalOsFs` gate (`vfs/vfs.go:L67`) | local backend in effect throughout |
| sibling-prefix limit documented, NOT patched | Q6b |

---


## Closing Coverage Pass

This section confirms every named mechanism, function, struct, constant, file, and flag was addressed **by name**, and that every behavioral claim is backed by captured output. Items that could only be read (never observed) are marked `(inferred)`.

### Per-question coverage

- **Q1 (allowlist enable/disable):** `supportedSSHCommands`, `defaultSSHCommands`, `sshHashCommands`, `systemCommands` (`sftpd/sftpd.go:L66-70`); `GetDefaultSSHCommands`/`GetSupportedSSHCommands` (L136/L143); `enable_scp` default false; `enabled_ssh_commands` default; `checkSSHCommands` `"*"`-expansion / scp-prepend / drop-with-warning (`sftpd/server.go:L396-414`). Default refusal of `scp -O` & `rsync` (Q1a), rsync-enable (Q1b), scp-enable (Q1c), unsupported-name warning (Q1d), `"*"` expansion (Q1e) — all with captured output + exit status. **`sftpgo-copy`/`sftpgo-remove` confirmed ABSENT** (dropped as unsupported in Q1d).
- **Q2 (client/server boundary):** exec dispatch (`sftpd/server.go:L326-329`); `processSSHCommand` unmarshal/tokenize/gate/reject (`sftpd/ssh_cmd.go:L45-81`, gate L51, reject L77). Allowed `md5sum` vs disallowed `whoami`/`ls -la /etc`/`rsync` with correlated `new ssh command` + `ssh command not enabled/supported` logs.
- **Q3 (filtering/validation):** `IsStringInSlice` (`utils/utils.go:L20-27`); `handle` router (`sftpd/ssh_cmd.go:L83-103`) incl. `cd`/`pwd`; local-fs gate (L106-108, L152-153) + `IsLocalOsFs` (`vfs/vfs.go:L67`); system-command `HasPerms` set (L158-160); rsync `--safe-links`/`--munge-links` cross-product (L306-322) + DEBUG `new system command` log (L323); `wrapCmd` uid/gid (`sftpd/cmd_unix.go:L10-15`); error vars L29-31. Non-local S3 gate is **(inferred)** (cannot stand up S3 offline); `wrapCmd` non-zero-uid credential is **(inferred)** (test users have uid/gid 0).
- **Q4 (delimiters/boundaries):** `parseCommandPayload` single-space split (`sftpd/ssh_cmd.go:L423-429`); `readProtocolMessage` byte-by-byte newline (`sftpd/scp.go:L542-564`); `parseUploadMessage` (`sftpd/scp.go:L631-664`) — `unknown or invalid upload message` (L636), `Error splitting upload message` (L658), `cannot be empty` (L652), `Error getting size` (L647). Space-in-filename (Q4b), bad prefix (Q4c), empty name (Q4d-i), bad size (Q4d-ii), tokenizer normal/double-space/multiword (Q4e); before/after proves no file created.
- **Q5 (resolve-then-authorize):** `handleUpload` resolve-first (`sftpd/scp.go:L231`), `PermUpload` (L242-243), `PermOverwrite` (L265); permission constants (`dataprovider/user.go:L17-42`); `Permissions` map (L93); `GetPermissionsForPath` deepest-match + single-root short-circuit (L122-156); `HasPerm` (L159-165); `HasPerms` (L168-179). `/ro`-vs-`/upload` (Q5a), new-vs-overwrite cross-product (Q5b), deepest-match override (Q5c), ordering proof (Q5d).
- **Q6 (filesystem tricks):** `ResolvePath` (`vfs/osfs.go:L200-223`); `isSubDir` bare `strings.HasPrefix` no-trailing-sep (L285) + `is not inside` error (L286) + `Invalid path resolution` warn (L220); `findFirstExistingDir` (L250-276); virtual-root cleaning (`getDestPath` L363-366). `..` blocked via cleaning (Q6a); `isSubDir` engages for `/etc` and non-prefix sibling, but the **sibling-prefix limit** lets a prefix-sharing sibling through (Q6b) — **manifested at runtime, documented, NOT patched** (AAP §0.5.2).

### Explicit notes required by the mission

- **`sftpgo-copy` / `sftpgo-remove` do not exist in `0.9.5-dev`.** They are not in `supportedSSHCommands` (`sftpd/sftpd.go:L66-67`); `sftpgo-copy` was dropped at startup exactly like the arbitrary name `bogus` (Q1d).
- **The `isSubDir` string-prefix containment boundary is documented and demonstrated, not patched.** Q6b shows the sibling-prefix read succeeding at runtime; no source file was changed to address it.
- **`(inferred)` labels** apply only to: the missing-gcc build error text (Q build preamble), the non-local (S3) backend `errUnsupportedConfig` branch (Q3c), and the non-zero-uid `wrapCmd` credential attachment (Q3a/b). Everything else is backed by captured runtime output.

### Real-entry-point & reproducibility confirmations

- Every behavior was driven through a genuine client path: system `ssh`/`scp -O`/`sftp`/`rsync`, or crafted SCP protocol bytes over a real SSH `exec` channel. No internal shortcut bypassed `processSSHCommand`, the SCP parser, or the permission/containment path.
- Reproducibility-sensitive cases were run **≥2×** with identical results: Q1a `scp -O` refusal, Q1b rsync acceptance, Q1c scp acceptance, all Q4 malformed headers, Q5c deepest-match denial, and Q6b sibling-prefix exfiltration.

---

## Cleanup & Verification

All transient artifacts lived **outside** the repository tree (`/tmp/sftpgo_bin`, `/tmp/sftpgo_run/**` including `sftpgo.db`, host key `id_rsa`, client keys, logs, and all test-user home directories `testuser`/`nosymuser`/`permuser`/`deepuser`; the helper library `/tmp/sftpgo_helpers.sh`; and the evidence directory `/tmp/evidence/`). The server was stopped and all of the above were deleted before completion, leaving the repository working tree clean except for this single new document.

Final verification from the repository root, after deleting every transient artifact and **before** staging the deliverable. The default `git status --porcelain` collapses the untracked tree to its topmost new directory (`blitzy/` did not previously exist); expanding with `-uall` proves the tree contains **exactly one** new file — this document (the empty `blitzy/screenshots/` and `blitzy/screen_recordings/` directories are not shown because git does not track empty directories):

```
$ git status --porcelain
?? blitzy/

$ git status --porcelain -uall
?? blitzy/documentation/sftpgo_44634210287c.md
```

`git diff` on all tracked files is empty — no SFTPGo source, test, or configuration file was modified, and `go.mod`/`go.sum` are untouched:

```
$ git diff --stat
$ git diff --stat -- go.mod go.sum
# (both empty — nothing changed)
```

The single new file was then staged and committed on the working branch; after the commit, `git status --porcelain` reports a clean working tree.

