# Command-Injection Investigation — SFTPGo `0.9.5-dev`

**Repository:** `github.com/drakkan/sftpgo`
**Commit under test:** `44634210287cb192f2a53147eafb84a33a96826b`
**Build banner observed:** `SFTPGo version: 0.9.5-dev`
**Method:** run-first, then write. SFTPGo was built and run in its canonical/default
configuration, the **authenticated SFTP upload** entry point was driven with the attack
payload under three configurations, and the complete unedited output was captured before a
single line of prose was written. Every claim below is grounded in a `file:line` reference
that was re-verified against the source at this exact commit, and/or in captured runtime
output. Where a fact is inferred rather than observed, it is labelled **(inferred)**.

> **Scope note.** All findings are scoped to commit `44634210287c` (`0.9.5-dev`). Behaviours
> that differ in later releases (e.g. the newer docs stating the hook environment *is*
> cleared, or the `EventManager` and provider hooks introduced later) are called out only as
> out-of-scope context and are **not** applied to this commit.

---

## 1. Direct answer — exploitation conditions and the responsible component

**The responsible component (the flagged sink).** The scanner's command-injection finding
points at **`executeNotificationCommand`** in `sftpd/sftpd.go`. The function is declared at
`sftpd/sftpd.go:418`, and the actual `os/exec` sink is:

```go
// sftpd/sftpd.go:418-421
func executeNotificationCommand(operation, username, path, target, sshCmd, fileSize string) error {
	ctx, cancel := context.WithTimeout(context.Background(), 30*time.Second)   // L419: 30s timeout
	defer cancel()
	cmd := exec.CommandContext(ctx, actions.Command, operation, username, path, target, sshCmd)   // L421: THE SINK
```

User-controlled data (the uploaded file's name) arrives as the `path` argument. It is one of
exactly **five positional `argv` arguments** — `operation, username, path, target, sshCmd` —
passed to `exec.CommandContext` at `sftpd/sftpd.go:421`. (The sixth parameter, `fileSize`, is
**not** an `argv` element; it is exported only through the environment — see
`sftpd/sftpd.go:422-429`.)

**The guard that makes it "sometimes vulnerable, sometimes not".** The sink is only reachable
past two guards inside **`executeAction`** (`sftpd/sftpd.go:438`):

```go
// sftpd/sftpd.go:438-447 (guards only)
func executeAction(operation, username, path, target, sshCmd string, fileSize int64) error {
	if !utils.IsStringInSlice(operation, actions.ExecuteOn) {   // L439: GUARD 1 — reachability
		return nil
	}
	...
	if len(actions.Command) > 0 && filepath.IsAbs(actions.Command) {   // L447: GUARD 2 — command set + absolute
```

- **Guard 1 (`sftpd/sftpd.go:439`):** the operation (`upload`, `download`, `delete`,
  `rename`, `ssh_cmd`) must be listed in `actions.execute_on`. In the default configuration
  `execute_on` is empty, so this returns immediately and the sink is **unreachable**.
- **Guard 2 (`sftpd/sftpd.go:447`):** `actions.command` must be non-empty **and** an absolute
  path (`filepath.IsAbs`). In the default configuration `command` is empty, so even if
  Guard 1 passed, no command would run.

**The precise answer to "when is it exploitable?"** The reported inconsistency is **sink
reachability governed by configuration, plus operator shell re-interpretation** — it is
**not** string interpolation performed by SFTPGo. Concretely:

| Condition | `execute_on` | `command` | Operator hook behaviour | Result |
|-----------|--------------|-----------|-------------------------|--------|
| **A** (default) | `[]` | `""` | — (never runs) | **NOT vulnerable** — sink unreachable (Guard 1) |
| **B** | `["upload"]` | absolute path, no shell | passes `argv`/env through literally | **NOT vulnerable** — taint-scanner false positive |
| **C** | `["upload"]` | absolute path, `eval`/`sh -c` | re-parses the file name through a shell | **VULNERABLE** — but the injection is **operator-introduced** |

The single sentence answer: **SFTPGo itself never routes the payload through a shell** (it
uses Go `os/exec` with a separate `argv`), so the reported command injection is reachable
only when (1) an operator enables the file-event hook for the operation *and* points
`command` at an absolute-path program, *and* (2) that operator program itself feeds the
argument/environment back into a shell (e.g. `eval`, `sh -c`). Condition C reproduces the
"vulnerable" reading; Conditions A and B reproduce the "not vulnerable" reading — with the
**identical** payload.

---

## 2. Build & run — canonical/default configuration (recorded verbatim)

SFTPGo was built and run inside the mandated container image
(`ghcr.io/scaleapi/swe-atlas:swe_atlas_QnA_drakkan_sftpgo_1.0`). All build/run artifacts live
under `/tmp` — **no repository file was modified** (verified in §9).

**Toolchain.** Go `1.19.13` with `CGO_ENABLED=1` (CGO is required by
`github.com/mattn/go-sqlite3 v2.0.2+incompatible`, declared in `go.mod`). The module declares
`go 1.13`; Go 1.19.13 builds it backward-compatibly.

**Commit confirmation.**

```text
$ git rev-parse HEAD
44634210287cb192f2a53147eafb84a33a96826b
```

**Canonical build command and result.**

```text
$ GO111MODULE=on CGO_ENABLED=1 go build -o /tmp/sftpgo_exp/sftpgo .
# github.com/mattn/go-sqlite3
sqlite3-binding.c:125801:10: warning: function may return address of local variable [-Wreturn-local-addr]
# (benign go-sqlite3 CGO warning; build exit code 0; binary size 27230224 bytes)
```

**Version banner.**

```text
$ /tmp/sftpgo_exp/sftpgo --version
SFTPGo version: 0.9.5-dev
```

The constant behind that banner is `utils/version.go:3` → `const version = "0.9.5-dev"`.

**SQLite provider bootstrap.** This version has **no `initprovider` subcommand**; the schema
is created manually (matching the project's own CI in `.travis.yml`), because SFTPGo refuses
to start against a missing/empty SQLite database:

```text
$ sqlite3 /tmp/sftpgo_exp/sftpgo.db 'CREATE TABLE "users" (
    "id" integer NOT NULL PRIMARY KEY AUTOINCREMENT,
    "username" varchar(255) NOT NULL UNIQUE, "password" varchar(255) NULL,
    "public_keys" text NULL, "home_dir" varchar(255) NOT NULL,
    "uid" integer NOT NULL, "gid" integer NOT NULL, "max_sessions" integer NOT NULL,
    "quota_size" bigint NOT NULL, "quota_files" integer NOT NULL,
    "permissions" text NOT NULL, "used_quota_size" bigint NOT NULL,
    "used_quota_files" integer NOT NULL, "last_quota_update" bigint NOT NULL,
    "upload_bandwidth" integer NOT NULL, "download_bandwidth" integer NOT NULL,
    "expiration_date" bigint NOT NULL, "last_login" bigint NOT NULL,
    "status" integer NOT NULL, "filters" TEXT NULL, "filesystem" text NULL);'
```

**Start command and observed startup log** (structured zerolog JSON — see `logger/logger.go`;
`--log-verbose` enables the debug-level `executed command` line used later):

```text
$ /tmp/sftpgo_exp/sftpgo serve -c /tmp/sftpgo_exp --log-verbose
```

```json
{"level":"info","time":"2026-07-08T05:15:47.770","sender":"service","connection_id":"","message":"starting SFTPGo 0.9.5-dev, config dir: /tmp/sftpgo_exp, config file: sftpgo, log max size: 10 log max backups: 5 log max age: 28 log verbose: true, log compress: false"}
{"level":"debug","time":"2026-07-08T05:15:47.772","sender":"httpd","connection_id":"","message":"initializing HTTP server with config {BindPort:8080 BindAddress:127.0.0.1 TemplatesPath:templates StaticFilesPath:static BackupsPath:backups}"}
{"level":"debug","time":"2026-07-08T05:15:47.772","sender":"service","connection_id":"","message":"initializing SFTP server with config {Banner:SFTPGo_0.9.5-dev BindPort:2022 BindAddress: IdleTimeout:15 MaxAuthTries:0 Umask:0022 UploadMode:0 Actions:{ExecuteOn:[upload] Command:/tmp/sftpgo_exp/hook_eval.sh HTTPNotificationURL:} Keys:[] IsSCPEnabled:false KexAlgorithms:[] Ciphers:[] MACs:[] LoginBannerFile: SetstatMode:0 EnabledSSHCommands:[md5sum sha1sum cd pwd]}"}
{"level":"info","time":"2026-07-08T05:15:47.774","sender":"sftpd","connection_id":"","message":"server listener registered address: [::]:2022"}
```

The server binds SFTP on `:2022` and the HTTP REST/web API on `127.0.0.1:8080`, exactly as
declared in the default `sftpgo.json`. (The `Actions:{ExecuteOn:[upload] Command:.../hook_eval.sh ...}`
shown above is from the Condition C run; the default/off actions block is shown under
Condition A in §4.)

**Test user creation via the Chi REST API** (`github.com/go-chi/chi v4.0.2+incompatible`):

```text
$ curl -s -w '\nHTTP_STATUS=%{http_code}\n' -X POST http://127.0.0.1:8080/api/v1/user \
    -H 'Content-Type: application/json' -d '{
      "username":"auditor",
      "password":"auditor_pw_2026",
      "home_dir":"/tmp/sftpgo_exp/home/auditor",
      "uid":0,"gid":0,"status":1,
      "permissions":{"/":["*"]}
    }'
HTTP_STATUS=200
```

The API returned HTTP `200` and assigned the user `id:1`; a subsequent `GET /api/v1/user`
confirmed the `auditor` user exists and the SFTP login succeeds (see §3).

---

## 3. The real entry point that was exercised

Exploitation was attempted through the **authenticated SFTP protocol** — the real,
client-facing entry point — **not** through a debug shortcut, an SCP hack, or a hand-crafted
direct call into `executeNotificationCommand`.

**The payload is the uploaded file's name.** A small Go SFTP client
(`github.com/pkg/sftp v1.11.0` over `golang.org/x/crypto` SSH) authenticates as `auditor`
with a password and uploads a 10-byte file whose remote name is, verbatim:

```
audit_$(date +%s).txt
```

The name is a Go string literal in the client source, so the shell metacharacters are **not**
expanded by the client's own shell — the exact bytes travel over the SFTP protocol as the
remote file name. Representative excerpt of the client (full source in the Appendix):

```go
// payloadName is the attacker-chosen file name delivered over SFTP.
const payloadName = "audit_$(date +%s).txt"
...
f, err := client.Create(payloadName)          // SFTP SSH_FXP_OPEN (create)
...
n, err := f.Write([]byte("0123456789"))       // 10 bytes
...
fmt.Printf("UPLOAD_OK remote=%s bytes=%d (SSH_FX_OK)\n", payloadName, n)
```

**How the name reaches the sink.** When the upload completes, `sftpd/transfer.go:156` fires
the file-event action for `operationUpload`, passing the stored path as the `path` argument:

```go
// sftpd/transfer.go:156 (the primary exploit trigger — fires after an SFTP upload completes)
go executeAction(operationUpload, t.user.Username, t.path, "", "", t.bytesReceived+t.minWriteOffset)
```

`executeAction` (`sftpd/sftpd.go:438`) then applies its guards and, if they pass, calls
`executeNotificationCommand`, whose `exec.CommandContext` at `sftpd/sftpd.go:421` receives the
file name as `path`. The SFTP write path itself is `Connection.Filewrite`
(`sftpd/handler.go:98`).

**Other real entry points that carry a filename payload to the same sink** (documented for
completeness; the upload trigger is the one exercised):

| Trigger | `file:line` | Operation |
|---------|-------------|-----------|
| Upload complete | `sftpd/transfer.go:156` | `operationUpload` (exercised) |
| Download complete | `sftpd/transfer.go:153` | `operationDownload` |
| Rename | `sftpd/handler.go:317` | `operationRename` (source, target) |
| Delete | `sftpd/handler.go:407` | `operationDelete` |
| SSH command | `sftpd/ssh_cmd.go:405` | `operationSSHCmd` |

**Observed SFTP response (identical in every condition):**

```text
PAYLOAD_NAME=audit_$(date +%s).txt
UPLOAD_OK remote=audit_$(date +%s).txt bytes=10 (SSH_FX_OK)
SERVER_STORED name="audit_$(date +%s).txt" size=10
```

The transfer always succeeds at the protocol level (`SSH_FX_OK`); the server stores the file
under the **literal** name `audit_$(date +%s).txt` in the user's home directory. Whether any
command injection then occurs is decided entirely by the hook configuration (§4), not by the
upload itself.

---

## 4. Three-condition matrix (each condition run ≥2×; distribution reported)

Only the hook configuration was varied; the payload and everything else were held identical.
The server was restarted between conditions because the configuration is read once at startup
(confirmed by the `config loaded: {... Actions:{...} ...}` line). All three hook scripts use
**absolute paths** (required by the `filepath.IsAbs` guard at `sftpd/sftpd.go:447`) and live
under `/tmp`.

### Condition A — default configuration (sink unreachable)

Config actions block (as loaded by SFTPGo):

```text
Actions:{ExecuteOn:[] Command: HTTPNotificationURL:}
```

Two identical uploads. Both succeeded at the protocol level; the file was stored server-side
with the literal name; **no** `/tmp/audit_*` file was created; the log shows an `Upload`
(info) line for each run but **no** `executed command` line — Guard 1 (`sftpd/sftpd.go:439`)
short-circuits because `upload` is not in the empty `execute_on`, so the sink is never
reached.

```json
{"level":"info","time":"2026-07-08T05:15:39.749","sender":"Upload","elapsed_ms":0,"size_bytes":10,"username":"auditor","file_path":"/tmp/sftpgo_exp/home/auditor/audit_$(date +%s).txt","connection_id":"59408ab951a77ffcb34031af9c7eee6dc6cdda6640e9c90d86edd8cd73e1a98c","protocol":"SFTP"}
{"level":"info","time":"2026-07-08T05:15:39.877","sender":"Upload","elapsed_ms":0,"size_bytes":10,"username":"auditor","file_path":"/tmp/sftpgo_exp/home/auditor/audit_$(date +%s).txt","connection_id":"934aa699238730af75e54edf192fb7fff7653b1a15de0ee9a0f9dd9b96d4db00","protocol":"SFTP"}
```

```text
executed_command_count=0
audit_file_count=0
```

**Verdict: NOT vulnerable.** Distribution across both runs: 0 files created, 0 commands
executed.

### Condition B — hook enabled, operator program does NOT use a shell

Config actions block:

```text
Actions:{ExecuteOn:[upload] Command:/tmp/sftpgo_exp/hook_argv.sh HTTPNotificationURL:}
```

`hook_argv.sh` only prints its positional `argv[]` and its `SFTPGO_ACTION_*` environment; it
does **not** `eval` or `sh -c` anything. Two identical uploads. The hook fired both times;
`$(date +%s)` was delivered as a single **literal** `argv[2]` element and as a literal
`SFTPGO_ACTION_PATH`; it was **not** expanded; **no** `/tmp/audit_*` file was created. Full
unedited hook output (both invocations):

```text
=== hook_argv invoked 1783487744 ===
ARGV_COUNT=5
argv[0]=<upload>
argv[1]=<auditor>
argv[2]=</tmp/sftpgo_exp/home/auditor/audit_$(date +%s).txt>
argv[3]=<>
argv[4]=<>
ENV SFTPGO_ACTION=<upload>
ENV SFTPGO_ACTION_PATH=</tmp/sftpgo_exp/home/auditor/audit_$(date +%s).txt>
ENV SFTPGO_ACTION_USERNAME=<auditor>
ENV SFTPGO_ACTION_TARGET=<>
ENV SFTPGO_ACTION_SSH_CMD=<>
ENV SFTPGO_ACTION_FILE_SIZE=<10>
=== hook_argv invoked 1783487744 ===
ARGV_COUNT=5
argv[0]=<upload>
argv[1]=<auditor>
argv[2]=</tmp/sftpgo_exp/home/auditor/audit_$(date +%s).txt>
argv[3]=<>
argv[4]=<>
ENV SFTPGO_ACTION=<upload>
ENV SFTPGO_ACTION_PATH=</tmp/sftpgo_exp/home/auditor/audit_$(date +%s).txt>
ENV SFTPGO_ACTION_USERNAME=<auditor>
ENV SFTPGO_ACTION_TARGET=<>
ENV SFTPGO_ACTION_SSH_CMD=<>
ENV SFTPGO_ACTION_FILE_SIZE=<10>
```

`ARGV_COUNT=5` matches exactly the five `argv` arguments passed at `sftpd/sftpd.go:421`. The
server's debug log confirms SFTPGo passed the path literally (Go-quoted via the `%#v` verb;
`$(date +%s)` still present, unexpanded), and the hook exited cleanly (`error: <nil>`):

```json
{"level":"debug","time":"2026-07-08T05:15:44.605","sender":"sftpd","connection_id":"","message":"executed command \"/tmp/sftpgo_exp/hook_argv.sh\" with arguments: \"upload\", \"auditor\", \"/tmp/sftpgo_exp/home/auditor/audit_$(date +%s).txt\", \"\", \"\", elapsed: 1.439892ms, error: <nil>"}
{"level":"debug","time":"2026-07-08T05:15:44.735","sender":"sftpd","connection_id":"","message":"executed command \"/tmp/sftpgo_exp/hook_argv.sh\" with arguments: \"upload\", \"auditor\", \"/tmp/sftpgo_exp/home/auditor/audit_$(date +%s).txt\", \"\", \"\", elapsed: 1.438647ms, error: <nil>"}
```

```text
audit_file_count=0
```

**Verdict: NOT vulnerable — this is the taint-scanner false positive.** User-controlled data
provably reaches `exec.CommandContext`, yet no injection occurs because Go `os/exec` does not
invoke a shell. Distribution across both runs: 0 files created (2 commands executed, both
inert). *(Note: the hook's own header line `hook_argv invoked 1783487744` shows an expanded
timestamp because `$(date +%s)` appears in the script's **own text** — not because the
payload was expanded.)*

### Condition C — hook enabled, operator program re-parses input via `eval`

Config actions block:

```text
Actions:{ExecuteOn:[upload] Command:/tmp/sftpgo_exp/hook_eval.sh HTTPNotificationURL:}
```

`hook_eval.sh` takes `SFTPGO_ACTION_PATH`, computes `fname=$(basename "$SFTPGO_ACTION_PATH")`,
and then runs `eval "echo CONFIRMED > /tmp/$fname"` — i.e. the operator's **own** program
feeds the attacker-controlled name into a shell. The **identical** payload was uploaded three
times. The injection fired each time: three distinct `/tmp/audit_<timestamp>.txt` files were
created, each containing `CONFIRMED`, with a **different, monotonically increasing** timestamp
per run — proving that `$(date +%s)` is command-substituted *fresh on each run by the
operator's `eval`*, not by SFTPGo.

Full unedited hook output (three invocations):

```text
=== hook_eval invoked (operator eval) ===
SFTPGO_ACTION_PATH=</tmp/sftpgo_exp/home/auditor/audit_$(date +%s).txt>
basename fname=<audit_$(date +%s).txt>
operator runs: eval "echo CONFIRMED > /tmp/$fname"
=== hook_eval invoked (operator eval) ===
SFTPGO_ACTION_PATH=</tmp/sftpgo_exp/home/auditor/audit_$(date +%s).txt>
basename fname=<audit_$(date +%s).txt>
operator runs: eval "echo CONFIRMED > /tmp/$fname"
=== hook_eval invoked (operator eval) ===
SFTPGO_ACTION_PATH=</tmp/sftpgo_exp/home/auditor/audit_$(date +%s).txt>
basename fname=<audit_$(date +%s).txt>
operator runs: eval "echo CONFIRMED > /tmp/$fname"
```

The server's debug log again shows SFTPGo passed the path **literally** (Go-quoted,
`$(date +%s)` unexpanded) — the expansion happened only inside the operator's `eval`:

```json
{"level":"debug","time":"2026-07-08T05:15:49.479","sender":"sftpd","connection_id":"","message":"executed command \"/tmp/sftpgo_exp/hook_eval.sh\" with arguments: \"upload\", \"auditor\", \"/tmp/sftpgo_exp/home/auditor/audit_$(date +%s).txt\", \"\", \"\", elapsed: 2.219316ms, error: <nil>"}
{"level":"debug","time":"2026-07-08T05:15:51.676","sender":"sftpd","connection_id":"","message":"executed command \"/tmp/sftpgo_exp/hook_eval.sh\" with arguments: \"upload\", \"auditor\", \"/tmp/sftpgo_exp/home/auditor/audit_$(date +%s).txt\", \"\", \"\", elapsed: 2.355087ms, error: <nil>"}
{"level":"debug","time":"2026-07-08T05:15:53.822","sender":"sftpd","connection_id":"","message":"executed command \"/tmp/sftpgo_exp/hook_eval.sh\" with arguments: \"upload\", \"auditor\", \"/tmp/sftpgo_exp/home/auditor/audit_$(date +%s).txt\", \"\", \"\", elapsed: 2.518232ms, error: <nil>"}
```

Created files (name + mtime + content), the concrete proof of arbitrary code execution:

```text
/tmp/audit_1783487749.txt  mtime=2026-07-08 05:15:49  content=CONFIRMED
/tmp/audit_1783487751.txt  mtime=2026-07-08 05:15:51  content=CONFIRMED
/tmp/audit_1783487753.txt  mtime=2026-07-08 05:15:53  content=CONFIRMED
audit_file_count=3
```

**Verdict: VULNERABLE — but the injection is operator-introduced.** Distribution across the
three runs: a fresh `/tmp/audit_<ts>.txt` each run, timestamps `1783487749 → 1783487751 →
1783487753` (strictly increasing), each containing `CONFIRMED`.

### Distribution summary (same unchanged payload throughout)

| Condition | Runs | `/tmp/audit_*` created | `executed command` lines | Verdict |
|-----------|------|------------------------|--------------------------|---------|
| A (default/off) | 2 | 0, 0 | 0 | NOT vulnerable (unreachable) |
| B (hook, no shell) | 2 | 0, 0 | 2 (inert) | NOT vulnerable (false positive) |
| C (hook, `eval`) | 3 | 1, 1, 1 (fresh ts each) | 3 | VULNERABLE (operator-introduced) |

This is the reported "sometimes vulnerable, sometimes not": the **same** input
(`audit_$(date +%s).txt`) is harmless under A and B and dangerous under C, and the toggling
variable is the operator's configuration + hook implementation, never a change in the payload
or in SFTPGo's own code.


---

## 5. The four required evidence items (verbatim, unedited)

**(a) The exact payload string used.**

```
audit_$(date +%s).txt
```

Delivered as the uploaded file's remote name over authenticated SFTP. It becomes the `path`
argument at `sftpd/sftpd.go:421`.

**(b) The complete server response observed during the attack** (identical in every
condition — the upload always succeeds at the protocol level):

```text
PAYLOAD_NAME=audit_$(date +%s).txt
UPLOAD_OK remote=audit_$(date +%s).txt bytes=10 (SSH_FX_OK)
SERVER_STORED name="audit_$(date +%s).txt" size=10
```

The protocol result is `SSH_FX_OK`; the server stores the file under the literal name
`audit_$(date +%s).txt`.

**(c) The exact filename(s) that get created (including the timestamp).** Only Condition C
(operator `eval`) creates them; Conditions A and B create **none**. From the three Condition C
runs:

```text
/tmp/audit_1783487749.txt   (content: CONFIRMED,  mtime 2026-07-08 05:15:49)
/tmp/audit_1783487751.txt   (content: CONFIRMED,  mtime 2026-07-08 05:15:51)
/tmp/audit_1783487753.txt   (content: CONFIRMED,  mtime 2026-07-08 05:15:53)
```

Each timestamp is a fresh Unix epoch produced by the operator's `eval` running `date +%s` at
execution time; the strictly increasing values (`…49 → …51 → …53`) prove the substitution is
performed anew on every run.

**(d) The server log entries generated when it executes.** Two kinds, from
`logger/logger.go` (structured zerolog JSON):

*Condition A — an `Upload` (info) line but no `executed command` line (sink unreachable):*

```json
{"level":"info","time":"2026-07-08T05:15:39.749","sender":"Upload","elapsed_ms":0,"size_bytes":10,"username":"auditor","file_path":"/tmp/sftpgo_exp/home/auditor/audit_$(date +%s).txt","connection_id":"59408ab951a77ffcb34031af9c7eee6dc6cdda6640e9c90d86edd8cd73e1a98c","protocol":"SFTP"}
```

*Conditions B and C — the debug `executed command` line emitted at `sftpd/sftpd.go:432-433`
(here from Condition C). Note the `%#v` verb renders every argument Go-quoted, and the path is
still the literal `$(date +%s)` — SFTPGo never expanded it:*

```json
{"level":"debug","time":"2026-07-08T05:15:49.479","sender":"sftpd","connection_id":"","message":"executed command \"/tmp/sftpgo_exp/hook_eval.sh\" with arguments: \"upload\", \"auditor\", \"/tmp/sftpgo_exp/home/auditor/audit_$(date +%s).txt\", \"\", \"\", elapsed: 2.219316ms, error: <nil>"}
```

The emitting statement:

```go
// sftpd/sftpd.go:432-433
logger.Debug(logSender, "", "executed command %#v with arguments: %#v, %#v, %#v, %#v, %#v, elapsed: %v, error: %v",
	actions.Command, operation, username, path, target, sshCmd, time.Since(startTime), err)
```

---

## 6. Root cause and the honest negative result

**Go `os/exec` is shell-free by design.** The sink builds the child process `argv` directly
from the supplied arguments; it never spawns `/bin/sh`. Therefore shell metacharacters such
as `$(date +%s)` are inert **inside SFTPGo** — they are passed as a single literal argument.
This is exactly what Conditions A and B demonstrate, and what the `executed command` log lines
in B and C confirm (the path stays Go-quoted and unexpanded on the SFTPGo side).

**The decisive negative scan (re-run here; paste of the exact command and result).** A search
for every shell-invocation pattern across all non-test Go files returns **zero** matches —
SFTPGo never routes a command through a shell:

```text
$ grep -rn -e '"sh"' -e '"bash"' -e 'sh -c' -e 'bash -c' -e '/bin/sh' -e '/bin/bash' -e '"-c"' \
    --include='*.go' . | grep -v '_test.go'
$ echo "MATCH_COUNT=$(grep -rn -e '"sh"' -e '"bash"' -e 'sh -c' -e 'bash -c' -e '/bin/sh' -e '/bin/bash' -e '"-c"' --include='*.go' . | grep -v '_test.go' | wc -l) EXIT=$?"
MATCH_COUNT=0 EXIT=1
```

*(The empty first line is the `grep` producing no output; `MATCH_COUNT=0` and the non-zero
exit status confirm no matches.)*

**Where the injection actually comes from.** The only condition under which the reported
command injection fires (Condition C) is one an **operator** creates: a custom-action hook
whose own program re-interprets its `argv`/environment through a shell (`eval`, `sh -c`,
backticks, …). That is operator responsibility, outside SFTPGo's code. SFTPGo's contract, per
`README.md` (§8), is to invoke the operator's absolute-path program with fixed positional
arguments and `SFTPGO_ACTION_*` environment variables; it makes no guarantee about — and has
no control over — what that program then does with them.

**Observed vs inferred.**

- **Observed:** all §4/§5 runtime results; the zero-match scan; the `argv`/env literal
  delivery (Condition B); the fresh-timestamp file creation (Condition C); the Go-quoted,
  unexpanded path in the server logs; the build banner and startup logs.
- **Inferred:** that the same shell-free behaviour holds for the parallel sinks in §7 — this
  follows from reading their source (they use the same `os/exec` API with separate `argv` and
  no shell) and from the zero-match scan, but only the file-event upload hook was exercised at
  runtime. This is labelled inferred accordingly.

**Version-specific caveat (must not be generalized).** In this commit the hook inherits the
process environment: `cmd.Env = append(os.Environ(), …)` at `sftpd/sftpd.go:422`, and
`README.md:373` states plainly that previous global environment variables are **not** cleared
when the script is called. Newer SFTPGo documentation states the opposite (that the
environment *is* cleared for security). The finding here is scoped strictly to commit
`44634210287c`.

---

## 7. Parallel `os/exec` sinks (same shell-free model — documented, not exploited)

Three other command-execution sites exist. Each uses Go `os/exec` with a separate `argv` (or
environment only) and is gated; none routes data through a shell. **(inferred** from source +
the zero-match scan; only the file-event upload hook was driven at runtime.)**

**1. Data-provider action hook** — `dataprovider/dataprovider.go:787`:

```go
// dataprovider/dataprovider.go:786-787
commandArgs := user.getNotificationFieldsAsSlice(operation)   // args from dataprovider/user.go:440
cmd := exec.CommandContext(ctx, config.Actions.Command, commandArgs...)
```

Guarded by `len(config.Actions.Command) > 0 && filepath.IsAbs(config.Actions.Command)` at
`dataprovider/dataprovider.go:810`; `Actions` struct at `dataprovider/dataprovider.go:89`.
Same shell-free `argv` model as the file-event hook.

**2. External-authentication program** — `dataprovider/dataprovider.go:742` (function
`doExternalAuth` at `dataprovider/dataprovider.go:730`):

```go
// dataprovider/dataprovider.go:742
cmd := exec.CommandContext(ctx, config.ExternalAuthProgram)
```

It passes **no** `argv`; the username/password/public-key travel via environment only
(`dataprovider/dataprovider.go:744-746`, `SFTPGO_AUTHD_USERNAME` / `…_PASSWORD` /
`…_PUBLIC_KEY`). Guarded by an absolute-path + existence check at
`dataprovider/dataprovider.go:248-252` (`filepath.IsAbs` L249, `os.Stat` L252); field
`ExternalAuthProgram` at `dataprovider/dataprovider.go:169`.

**3. SSH system-command runner** — `sftpd/ssh_cmd.go:324`:

```go
// sftpd/ssh_cmd.go:324
cmd := exec.Command(c.command, args...)
```

It is allow-list gated. Dispatch in `handle()` (`sftpd/ssh_cmd.go:83`) routes hash commands
(`sftpd/ssh_cmd.go:87`) to an internal handler (no `exec`), only `systemCommands`
(`sftpd/ssh_cmd.go:89`) reach `exec` (via `executeSystemCommand` at `sftpd/ssh_cmd.go:94`),
and `cd`/`pwd` (`sftpd/ssh_cmd.go:95`/`97`) are handled in-process. The command sets are in `sftpd/sftpd.go:66-70`:

```go
// sftpd/sftpd.go:66-70
supportedSSHCommands = []string{"scp", "md5sum", "sha1sum", "sha256sum", "sha384sum", "sha512sum", "cd", "pwd",
	"git-receive-pack", "git-upload-pack", "git-upload-archive", "rsync"}
defaultSSHCommands = []string{"md5sum", "sha1sum", "cd", "pwd"}
sshHashCommands    = []string{"md5sum", "sha1sum", "sha256sum", "sha384sum", "sha512sum"}
systemCommands     = []string{"git-receive-pack", "git-upload-pack", "git-upload-archive", "rsync"}
```

The default `enabled_ssh_commands = ["md5sum","sha1sum","cd","pwd"]` contains **no** member of
`systemCommands`, so the default set **executes no external program**. Validation of the
configured list is at `sftpd/server.go:405-406` (field `EnabledSSHCommands` at
`sftpd/server.go:99`).


---

## 8. Corroboration with authoritative sources

**Go `os/exec` contract (pkg.go.dev/os/exec).** The standard-library documentation states the
package `os/exec` "does not invoke the system shell" and does not expand glob patterns,
pipelines, redirections, or other shell expansions; it runs programs the way C's `exec`
family does. Consequently a metacharacter sequence like `$(date +%s)` passed to
`exec.Command`/`exec.CommandContext` is a single **literal** argument and command substitution
never occurs inside the Go process. This is precisely what Conditions A and B demonstrate at
runtime, and it is the reason SFTPGo cannot itself perform the injection.

**SFTPGo custom-action hook contract.** The documentation
(`docs.sftpgo.com`) defines the hook as "the absolute path of your program or an HTTP URL".
This matches this commit's code: the absolute-path requirement is the `filepath.IsAbs` guard
at `sftpd/sftpd.go:447`. The documented 30-second execution bound is likewise enforced in
code by the `context.WithTimeout(context.Background(), 30*time.Second)` at
`sftpd/sftpd.go:419` and is stated in the in-repo `README.md:374`. The in-repo contract at
this commit is `README.md` "Custom Actions" (≈`README.md:348-374`), which enumerates the
positional arguments (`action, username, path, target_path, ssh_cmd`) and the
`SFTPGO_ACTION_*` environment variables.

**Version difference in environment handling — explicitly noted.** The current
`docs.sftpgo.com` says global environment variables are cleared for security when the script
is called; but at **this commit** `README.md:373` states the opposite — that previous global
environment variables are **not** cleared — which matches the code's `os.Environ()` at
`sftpd/sftpd.go:422`. The finding in this document is scoped to commit `44634210287c`; the
newer env-clearing behaviour is **not** applied here.

**Out-of-scope context (mentioned only).** Later SFTPGo releases removed Git/rsync support to
reduce attack surface, and introduced an `EventManager` with provider hooks. Neither exists in
`0.9.5-dev`; they are noted only to bound the scope and are not part of this analysis.

---

## 9. Read-only verification

The investigation created exactly one repository file — this document. All build/run
scaffolding (throwaway config directory, SQLite database, user home directory, built binary,
the two operator hook scripts, the Go SFTP client) lived under `/tmp`, and every
`/tmp/audit_*.txt` created during Condition C was outside the source tree. After the runs and
cleanup, the source repository is unchanged apart from the added document:

```text
$ git status --porcelain
?? blitzy/

$ git status --porcelain -uall     # expand the untracked directory to show individual files
?? blitzy/documentation/sftpgo_44634210287c.md

$ git diff --name-only HEAD         # tracked source files modified?
                                    # (empty output — none modified)

$ git rev-parse HEAD
44634210287cb192f2a53147eafb84a33a96826b
```

The default `git status --porcelain` collapses the wholly-untracked `blitzy/` directory to a
single `?? blitzy/` entry; `-uall` expands it to reveal that the one and only untracked file
is this answer document. `git diff --name-only HEAD` is empty, so no existing source file was
modified, added, or deleted; the only change to the repository is the new document under
`blitzy/documentation/`.

---

## Appendix A — the operator hook scripts (ephemeral, under `/tmp`)

**`hook_argv.sh` (Condition B — does NOT re-interpret input through a shell):**

```sh
#!/bin/sh
# Operator hook that DOES NOT re-interpret its input through a shell.
# It only logs its positional argv[] and the SFTPGO_ACTION_* environment.
# The hook's OWN header timestamp uses $(date +%s) in the SCRIPT TEXT (expanded by this script);
# the payload value in argv/env is echoed via "$a" / "$VAR" and is therefore printed LITERALLY.
{
  echo "=== hook_argv invoked $(date +%s) ==="
  echo "ARGV_COUNT=$#"
  i=0
  for a in "$@"; do
    echo "argv[$i]=<$a>"
    i=$((i+1))
  done
  echo "ENV SFTPGO_ACTION=<$SFTPGO_ACTION>"
  echo "ENV SFTPGO_ACTION_PATH=<$SFTPGO_ACTION_PATH>"
  echo "ENV SFTPGO_ACTION_USERNAME=<$SFTPGO_ACTION_USERNAME>"
  echo "ENV SFTPGO_ACTION_TARGET=<$SFTPGO_ACTION_TARGET>"
  echo "ENV SFTPGO_ACTION_SSH_CMD=<$SFTPGO_ACTION_SSH_CMD>"
  echo "ENV SFTPGO_ACTION_FILE_SIZE=<$SFTPGO_ACTION_FILE_SIZE>"
} >> /tmp/sftpgo_exp/hook_argv.out 2>&1
```

**`hook_eval.sh` (Condition C — the vulnerable operator hook that runs `eval`):**

```sh
#!/bin/sh
# VULNERABLE operator hook. It re-interprets the attacker-controlled file name through
# the shell via `eval`. The operator's (contrived) intent is to write a marker file named
# after the uploaded file into /tmp. Because the file name contains a shell command
# substitution, eval expands it. This is the OPERATOR'S mistake, NOT SFTPGo:
# SFTPGo passed the name to the hook as a single literal argv element / env value.
fname=$(basename "$SFTPGO_ACTION_PATH")
{
  echo "=== hook_eval invoked (operator eval) ==="
  echo "SFTPGO_ACTION_PATH=<$SFTPGO_ACTION_PATH>"
  echo "basename fname=<$fname>"
  echo "operator runs: eval \"echo CONFIRMED > /tmp/\$fname\""
} >> /tmp/sftpgo_exp/hook_eval.out 2>&1
eval "echo CONFIRMED > /tmp/$fname"
```

Line 14 (`eval "echo CONFIRMED > /tmp/$fname"`) is the operator's shell re-interpretation —
the sole place where `$(date +%s)` is expanded in the entire pipeline.

## Appendix B — the SFTP client (real, authenticated entry point)

```go
package main

import (
	"fmt"
	"os"

	"github.com/pkg/sftp"
	"golang.org/x/crypto/ssh"
)

// payloadName is the attacker-chosen file name delivered over SFTP.
// It is a Go string literal, so the shell metacharacters are NOT expanded here;
// the exact bytes below travel over the SFTP protocol as the remote file name.
const payloadName = "audit_$(date +%s).txt"

func main() {
	addr := "127.0.0.1:2022"
	user := "auditor"
	pass := "auditor_pw_2026"
	if len(os.Args) > 1 {
		addr = os.Args[1]
	}

	config := &ssh.ClientConfig{
		User:            user,
		Auth:            []ssh.AuthMethod{ssh.Password(pass)},
		HostKeyCallback: ssh.InsecureIgnoreHostKey(),
	}

	conn, err := ssh.Dial("tcp", addr, config)
	if err != nil {
		fmt.Printf("SSH_DIAL_ERROR: %v\n", err)
		os.Exit(1)
	}
	defer conn.Close()

	client, err := sftp.NewClient(conn)
	if err != nil {
		fmt.Printf("SFTP_CLIENT_ERROR: %v\n", err)
		os.Exit(1)
	}
	defer client.Close()

	fmt.Printf("PAYLOAD_NAME=%s\n", payloadName)

	f, err := client.Create(payloadName)
	if err != nil {
		fmt.Printf("UPLOAD_ERROR remote=%s err=%v\n", payloadName, err)
		os.Exit(2)
	}
	// exactly 10 bytes of content
	n, err := f.Write([]byte("0123456789"))
	if err != nil {
		fmt.Printf("WRITE_ERROR remote=%s err=%v\n", payloadName, err)
		f.Close()
		os.Exit(3)
	}
	if err := f.Close(); err != nil {
		fmt.Printf("CLOSE_ERROR remote=%s err=%v\n", payloadName, err)
		os.Exit(4)
	}
	fmt.Printf("UPLOAD_OK remote=%s bytes=%d (SSH_FX_OK)\n", payloadName, n)

	// Confirm what the server stored by listing the home directory.
	entries, err := client.ReadDir(".")
	if err != nil {
		fmt.Printf("READDIR_ERROR err=%v\n", err)
		os.Exit(5)
	}
	for _, e := range entries {
		fmt.Printf("SERVER_STORED name=%q size=%d\n", e.Name(), e.Size())
	}
}
```

---

## Summary

- **Responsible component:** `executeNotificationCommand` at `sftpd/sftpd.go:418`; the sink is
  `exec.CommandContext(ctx, actions.Command, operation, username, path, target, sshCmd)` at
  `sftpd/sftpd.go:421`, reachable only past the guards in `executeAction`
  (`sftpd/sftpd.go:438` — Guard 1 at L439, Guard 2 at L447).
- **Exploitation conditions:** the file-event hook must be enabled for the operation
  (`execute_on`) and `command` set to an absolute path (reachability), **and** the operator's
  hook program must re-interpret its arguments through a shell (`eval`/`sh -c`). Only then is
  the payload expanded.
- **"Sometimes vulnerable":** reproduced exactly with the identical payload
  `audit_$(date +%s).txt` — harmless under Conditions A and B, dangerous under Condition C.
- **Root cause of any real injection:** operator-introduced shell re-interpretation, **not**
  string interpolation in SFTPGo. Go `os/exec` is shell-free, corroborated by the zero-match
  shell-pattern scan and the `pkg.go.dev/os/exec` contract.

