# SFTPGo 0.9.5-dev — Startup Lifecycle, Observed at Runtime

This document explains what **SFTPGo version `0.9.5-dev`** (source branch `sftpgo_44634210287c`, commit `44634210`) *actually does* when it is started — not what the code merely suggests should happen. Every behavioral claim below was produced by **building the server from source and running it**, then capturing the real output (build → run → observe → capture). The version constant is `const version = "0.9.5-dev"` [utils/version.go:L3]. The entry point is `main.go`, whose `main()` calls `cmd.Execute()` (`main.go:L14`); the `serve` command builds a `service.Service` and calls `Start()`, then calls `Wait()` **only when `Start()` returns nil** — `if err := service.Start(); err == nil { service.Wait() }` [cmd/serve.go:L29-30]. That single control-flow fact governs the whole lifecycle: if `Start()` fails (for example, because the database is missing — see Q4), the process never enters `Wait()` and exits immediately, before any network listener is opened.

The document answers five questions:

- **Q1** — Config-less startup: which TCP ports open, and what signals readiness?
- **Q2** — SFTP login with a non-existent username: what appears in the logs, and how does the server react?
- **Q3** — Web admin first access: what does the root endpoint (`/`) return, and how is the browser redirected?
- **Q4** — Missing database on first startup: what warnings/errors appear, and what steps precede a fully operational state?
- **Q5** — Default configuration: how does it reveal itself through log output when no config file is found?

> **Reading the evidence.** Timestamps, PIDs, socket inodes, HTTP `request_id`s, and `Date` headers differ on every run; only their *structure and content* are meaningful. Anything derived by reading the code rather than observing it at runtime is explicitly labelled **inferred**.

---

## Methodology

### Toolchain (environment-only; nothing committed)

The server was built and observed with the following tools. None of them is a repository dependency and none is committed anywhere:

- **Go `1.13.15`** — the highest patch of the documented `1.13.x` [.travis.yml:L8], matching `go 1.13` [go.mod:L3].
- **`gcc 13.3.0`** + libc development headers — **required**, because the default SQLite data provider uses the CGO driver `github.com/mattn/go-sqlite3 v2.0.2+incompatible` [go.mod:L15]. Building without CGO would not yield a working default provider.
- **`sqlite3 3.45.1`** — used to bootstrap the database with the canonical schema (Q4).
- **`openssh-client 9.6p1`** + **`sshpass 1.09`** — used to drive a real, non-interactive `sftp` client for the Q2 authentication probe.

### Build (CGO required)

```
CGO_ENABLED=1 GO111MODULE=on GOPATH=/tmp/gopath GOCACHE=/tmp/gocache \
  /usr/local/go/bin/go build -o /tmp/sftpgo_bin github.com/drakkan/sftpgo
/tmp/sftpgo_bin --version
```

Observed:

```
SFTPGo version: 0.9.5-dev
```

The build was performed from the module directory (equivalently `CGO_ENABLED=1 ... go build -o /tmp/sftpgo_bin .`). The binary and **all** run artifacts (the throwaway database, the copied `templates/`+`static/` asset directories, the auto-generated `id_rsa` host key, and every `sftpgo.log`) live **outside** the repository working tree, so the source tree stays unchanged.

### Logging model (where the evidence lives)

SFTPGo uses `rs/zerolog` structured JSON. `logger.InitLogger` [logger/logger.go:L47] creates the main logger over a `lumberjack` rotating **file** writer when the log path is non-empty (it is, by default) [logger/logger.go:L50-56]; the default log file is `sftpgo.log` [cmd/root.go:L35]. A colorized **console** writer prints only the explicit `*ToConsole` messages to stdout [logger/logger.go:L75-80]. **Therefore the complete structured stream is in `sftpgo.log`; stdout shows only a curated subset.** Standard records carry the fields `level`, `time`, `sender`, `connection_id`, `message`; the Fail2ban connection-failure record instead carries `client_ip`, `username`, `login_type`, `error` [logger/logger.go:L175-183].

The default CLI flags that feed every run below come from `cmd/root.go`: config dir `.` [cmd/root.go:L33], config name `sftpgo` [cmd/root.go:L34], log file `sftpgo.log` [cmd/root.go:L35], log max size `10` [cmd/root.go:L36], backups `5` [cmd/root.go:L37], age `28` [cmd/root.go:L38], compress `false` [cmd/root.go:L39], verbose `true` [cmd/root.go:L40].

### Observation sequence

Each scenario was run from a clean throwaway directory **outside** the repository:

1. **Config-less + no database** → Q5 (default config surfaced through logs) and Q4 "before" (missing-database abort).
2. **Bootstrap the database** with the `.travis.yml` schema, copy `templates/`+`static/`, then **launch** → Q1 (ports + readiness) and Q4 "after" (transition to operational). Listening sockets were enumerated from `/proc/net/tcp` and `/proc/net/tcp6` and their ownership proven via `/proc/<pid>/fd`.
3. **Probe the root endpoint** with `curl` → Q3.
4. **Drive a real `sftp` client** as a non-existent user → Q2 (plus the modern-client host-key edge).

### Read-only compliance

All build/run artifacts — the binary, the throwaway database, the copied asset directories, the auto-generated `id_rsa`, every `sftpgo.log`, and any temporary observation script — were kept outside the repository and removed afterward. `git status --porcelain` reports no changes to the source tree.

---

## Q1 — Config-less startup: which TCP ports open, and what signals readiness?

**Direct answer.** Started with **no config file** (but with an initialized database present), SFTPGo opens **exactly two** TCP listeners — **SFTP on port `2022`** bound to `[::]` (the IPv6 wildcard, dual-stack, all interfaces) and the **HTTP admin on port `8080`** bound to `127.0.0.1`. The **SFTP readiness signal** is the log line `server listener registered address: [::]:2022`. The **HTTP server emits no dedicated "listening" line** — its readiness signal is the preceding DEBUG record `initializing HTTP server with config …`, logged immediately before it blocks in `ListenAndServe`.

**Orchestration.** `service.Start` runs the two servers as sibling goroutines: the SFTP server at [service/service.go:L91-93] (`sftpdConf.Initialize`), and the HTTP server at [service/service.go:L100-104], launched because the default `HTTPDConfig.BindPort` is `8080 > 0` [config/config.go:L88]. Whichever goroutine exits first signals the `Shutdown` channel, ending the process.

**Command used (launch):**

```
# from a clean dir OUTSIDE the repo, with an initialized sftpgo.db + templates/ + static/ present (see Q4 "after")
/tmp/sftpgo_bin serve
```

**Observed readiness sequence — complete `sftpgo.log` startup records (verbatim; timestamps/PID vary per run):**

```
{"level":"info","time":"2026-07-08T03:25:46.667","sender":"service","connection_id":"","message":"starting SFTPGo 0.9.5-dev, config dir: ., config file: sftpgo, log max size: 10 log max backups: 5 log max age: 28 log verbose: true, log compress: false"}
{"level":"warn","time":"2026-07-08T03:25:46.668","sender":"config","connection_id":"","message":"error loading configuration file: Config File \"sftpgo\" Not Found in \"[/tmp/sftpgo_runs/run_b /root/.config/sftpgo /etc/sftpgo]\". Default configuration will be used: {SFTPD:{Banner:SFTPGo_0.9.5-dev BindPort:2022 BindAddress: IdleTimeout:15 MaxAuthTries:0 Umask:0022 UploadMode:0 Actions:{ExecuteOn:[] Command: HTTPNotificationURL:} Keys:[] IsSCPEnabled:false KexAlgorithms:[] Ciphers:[] MACs:[] LoginBannerFile: SetstatMode:0 EnabledSSHCommands:[md5sum sha1sum cd pwd]} ProviderConf:{Driver:sqlite Name:sftpgo.db Host: Port:5432 Username: Password:[redacted] SSLMode:0 ConnectionString: UsersTable:users ManageUsers:1 TrackQuota:1 PoolSize:0 UsersBaseDir: Actions:{ExecuteOn:[] Command: HTTPNotificationURL:} ExternalAuthProgram: ExternalAuthScope:0} HTTPDConfig:{BindPort:8080 BindAddress:127.0.0.1 TemplatesPath:templates StaticFilesPath:static BackupsPath:backups}}"}
{"level":"debug","time":"2026-07-08T03:25:46.668","sender":"sqlite","connection_id":"","message":"sqlite database handle created, connection string: \"file:sftpgo.db?cache=shared\""}
{"level":"debug","time":"2026-07-08T03:25:46.668","sender":"httpd","connection_id":"","message":"initializing HTTP server with config {BindPort:8080 BindAddress:127.0.0.1 TemplatesPath:templates StaticFilesPath:static BackupsPath:backups}"}
{"level":"debug","time":"2026-07-08T03:25:46.668","sender":"service","connection_id":"","message":"initializing SFTP server with config {Banner:SFTPGo_0.9.5-dev BindPort:2022 BindAddress: IdleTimeout:15 MaxAuthTries:0 Umask:0022 UploadMode:0 Actions:{ExecuteOn:[] Command: HTTPNotificationURL:} Keys:[] IsSCPEnabled:false KexAlgorithms:[] Ciphers:[] MACs:[] LoginBannerFile: SetstatMode:0 EnabledSSHCommands:[md5sum sha1sum cd pwd]}"}
{"level":"debug","time":"2026-07-08T03:25:46.668","sender":"utils","connection_id":"","message":"set umask to 0022 (18)"}
{"level":"info","time":"2026-07-08T03:25:46.668","sender":"sftpd","connection_id":"","message":"No host keys configured and \"id_rsa\" does not exist; creating new private key for server"}
{"level":"info","time":"2026-07-08T03:25:48.226","sender":"sftpd","connection_id":"","message":"Loading private key: id_rsa"}
{"level":"info","time":"2026-07-08T03:25:48.226","sender":"sftpd","connection_id":"","message":"server listener registered address: [::]:2022"}
```

**Grounding and cause → effect:**

- The `server listener registered address: [::]:2022` line is emitted at [sftpd/server.go:L187], right after `net.Listen("tcp", fmt.Sprintf("%s:%d", c.BindAddress, c.BindPort))` [sftpd/server.go:L178]. With the default `BindAddress:""` and `BindPort:2022`, the listen address is `:2022`, which the kernel reports as `[::]:2022` (the IPv6 wildcard, dual-stack). **This is the SFTP readiness signal.**
- The HTTP goroutine logs `initializing HTTP server with config …` at [httpd/httpd.go:L76] inside `Conf.Initialize` [httpd/httpd.go:L75], then calls `httpServer.ListenAndServe()` [httpd/httpd.go:L98], which blocks and prints **no** further "listening" line. **The HTTP readiness signal is therefore this DEBUG line** — there is no dedicated HTTP "listening" log in this version.
- **Transition (before → during → after): host-key auto-generation.** On first launch there is no `id_rsa`, so `checkHostKeys` generates a fresh key: `os.Stat` on the auto key path fails, and the INFO record `No host keys configured and "id_rsa" does not exist; creating new private key for server` is emitted [sftpd/server.go:L422-423]; the key is produced by `rsa.GenerateKey(rand.Reader, 4096)` (RSA-4096, in `generatePrivateKey`); then the INFO record `Loading private key: id_rsa` is emitted [sftpd/server.go:L157], after which the listener registers. In the captured run this took ≈**1.56 s** (`46.668` → `48.226`). The `id_rsa` file is written into the working directory with mode `0600`. On subsequent launches (key already present) this step is skipped, so startup is near-instant.
- The DEBUG record `set umask to 0022 (18)` (`sender:"utils"`) reflects the default `Umask:0022` being applied.
- **Relative ordering note.** The `initializing … server with config` DEBUG lines for `httpd` and `service` (SFTP) are produced by independent goroutines, so their relative order can flip between runs. A re-run that shows the SFTP line before the HTTP line is not a discrepancy.

**Command used (enumerate listeners and prove ownership):**

```
# find the server PID, then:
awk 'NR==1 || $4=="0A"' /proc/net/tcp      # IPv4 listeners (state 0A = LISTEN)
awk 'NR==1 || $4=="0A"' /proc/net/tcp6     # IPv6 listeners
ls -l /proc/<pid>/fd | grep -o 'socket:\[[0-9]*\]' | sort -u   # socket inodes owned by the server
```

**Observed (verbatim; inodes vary per run):**

```
# /proc/net/tcp  (IPv4, LISTEN)
   0: 0100007F:1F90 00000000:0000 0A ...  inode 700831809
# /proc/net/tcp6 (IPv6, LISTEN)
   0: 00000000000000000000000000000000:07E6 ... 0A ... inode 700770739
# /proc/<pid>/fd socket inodes:
socket:[700770739]
socket:[700831809]
```

**Decode and prove.** `0100007F` is `127.0.0.1` (little-endian bytes `01`,`00`,`00`,`7F`) and `1F90` is **8080** → the HTTP admin listener; the IPv6 all-zeros address `::` with `07E6` is **2022** → the SFTP listener. Both socket inodes (`700831809`, `700770739`) also appear under `/proc/<pid>/fd`, **proving both listeners are owned by the SFTPGo process**. Conclusion: exactly two ports — `2022` (SFTP, `[::]`) and `8080` (HTTP, `127.0.0.1`). (`ss -ltn` corroborates the two listening ports; in this environment `ss` did not attach the owning PID, so the `/proc/<pid>/fd` inode match is the authoritative ownership proof.)

---

## Q2 — SFTP login with a non-existent username

**Direct answer.** When a client authenticates as a username that does not exist, the server writes **three** records to `sftpgo.log`, in this order — (1) a provider `WARN` `error authenticating user: <user>, error: Not found: sql: no rows in result set`, (2) a `DEBUG` `connection_failed` record (for Fail2ban) with `login_type:"password"`, and (3) an `sftpd` `WARN` `failed to accept an incoming connection: [ssh: no auth passed yet, Authentication error: could not validate password credentials: Not found: sql: no rows in result set]`. It then closes the connection, and the client sees `Permission denied`.

**Command used (canonical path — accept the server's legacy `ssh-rsa` host key):**

```
sshpass -p 'whatever' sftp -P 2022 \
  -o HostKeyAlgorithms=+ssh-rsa \
  -o StrictHostKeyChecking=no \
  -o UserKnownHostsFile=/dev/null \
  ghost@127.0.0.1
```

**Observed client output (verbatim):**

```
Warning: Permanently added '[127.0.0.1]:2022' (RSA) to the list of known hosts.
ghost@127.0.0.1: Permission denied (password,publickey).
Connection closed.
```

**Observed server records in `sftpgo.log` (verbatim; the three records, in order):**

```
{"level":"warn","time":"2026-07-08T03:27:42.166","sender":"sqlite","connection_id":"","message":"error authenticating user: ghost, error: Not found: sql: no rows in result set"}
{"level":"debug","time":"2026-07-08T03:27:42.166","sender":"connection_failed","client_ip":"127.0.0.1","username":"ghost","login_type":"password","error":"Not found: sql: no rows in result set"}
{"level":"warn","time":"2026-07-08T03:27:42.167","sender":"sftpd","connection_id":"","message":"failed to accept an incoming connection: [ssh: no auth passed yet, Authentication error: could not validate password credentials: Not found: sql: no rows in result set]"}
```

**Cause → effect chain (naming every function and its `file:line`):**

- The SSH `PasswordCallback` [sftpd/server.go:L128] calls `validatePasswordCredentials` [sftpd/server.go:L448], which calls `dataprovider.CheckUserAndPass` [sftpd/server.go:L454] → `sqlCommonValidateUserAndPass` [dataprovider/sqlcommon.go:L28] → `getUserByUsername` [dataprovider/sqlcommon.go:L33].
- The row scan hits `sql.ErrNoRows`, which is mapped to `&RecordNotFoundError{err: err.Error()}` at [dataprovider/sqlcommon.go:L295-296]. `RecordNotFoundError.Error()` formats as `"Not found: %s"` [dataprovider/dataprovider.go:L205-206], and `sql.ErrNoRows.Error()` is `"sql: no rows in result set"`, so the composed error is exactly `Not found: sql: no rows in result set`.
- `sqlCommonValidateUserAndPass` logs the provider `WARN` `error authenticating user: %v, error: %v` [dataprovider/sqlcommon.go:L35]. The `sender` is `"sqlite"` because `initializeSQLiteProvider` sets the package-level `logSender = SQLiteDataProviderName` [dataprovider/sqlite.go:L21].
- Back in `validatePasswordCredentials`, the error (`else`) branch calls `logger.ConnectionFailedLog(conn.User(), ip, "password", err.Error())` [sftpd/server.go:L457] → the `DEBUG` `connection_failed` record [logger/logger.go:L175-183] with `login_type:"password"`, `username:"ghost"`, `client_ip:"127.0.0.1"`.
- The callback returns the error wrapped as `authenticationError{... could not validate password credentials: ...}` [sftpd/server.go:L131]; `ssh.NewServerConn` inside `AcceptInboundConnection` [sftpd/server.go:L243,L249] then fails, so the `WARN failed to accept an incoming connection: %v` is logged [sftpd/server.go:L251]. Because that error **is** an `*ssh.ServerAuthError`, the guard `if _, ok := err.(*ssh.ServerAuthError); !ok` [sftpd/server.go:L252] is false, so **no second `connection_failed` record is written** on this path — which is precisely why there are exactly three records.

**Edge / sibling condition (modern client vs. legacy host key).** A modern OpenSSH client with default settings rejects the server's legacy `ssh-rsa`/SHA-1 host key at **host-key negotiation, before authentication**. This is a client-vs-old-server artifact, **not** the authentication path — hence the canonical path above needs `-o HostKeyAlgorithms=+ssh-rsa`.

**Command used (edge):**

```
sshpass -p 'whatever' sftp -P 2022 \
  -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
  ghost@127.0.0.1        # NOTE: no +ssh-rsa
```

**Observed client output (verbatim):**

```
Unable to negotiate with 127.0.0.1 port 2022: no matching host key type found. Their offer: ssh-rsa
```

**Observed server records (verbatim; only TWO records, NO provider WARN, and the WARN precedes the DEBUG — the long client algorithm list is shown in full in the WARN and clearly elided as `[ ... ]` in the DEBUG):**

```
{"level":"warn","time":"2026-07-08T03:27:42.701","sender":"sftpd","connection_id":"","message":"failed to accept an incoming connection: ssh: no common algorithm for host key; client offered: [ssh-ed25519-cert-v01@openssh.com ecdsa-sha2-nistp256-cert-v01@openssh.com ecdsa-sha2-nistp384-cert-v01@openssh.com ecdsa-sha2-nistp521-cert-v01@openssh.com sk-ssh-ed25519-cert-v01@openssh.com sk-ecdsa-sha2-nistp256-cert-v01@openssh.com rsa-sha2-512-cert-v01@openssh.com rsa-sha2-256-cert-v01@openssh.com ssh-ed25519 ecdsa-sha2-nistp256 ecdsa-sha2-nistp384 ecdsa-sha2-nistp521 sk-ssh-ed25519@openssh.com sk-ecdsa-sha2-nistp256@openssh.com rsa-sha2-512 rsa-sha2-256], server offered: [ssh-rsa]"}
{"level":"debug","time":"2026-07-08T03:27:42.701","sender":"connection_failed","client_ip":"127.0.0.1","username":"","login_type":"no_auth_tryed","error":"ssh: no common algorithm for host key; client offered: [ ... ], server offered: [ssh-rsa]"}
```

**Cause → effect for the edge.** `ssh.NewServerConn` fails during key exchange **before** any auth callback runs, so there is **no provider WARN and no password `connection_failed`**. The failure error is *not* an `*ssh.ServerAuthError`, so the guard at [sftpd/server.go:L252] is true and `logger.ConnectionFailedLog("", ip, "no_auth_tryed", err.Error())` [sftpd/server.go:L253] emits a `connection_failed` record with an **empty `username`** and `login_type:"no_auth_tryed"`. Contrast this with the canonical path: the order and count of records differ (two records here, WARN-then-DEBUG, no provider WARN; three records there, provider-WARN-then-DEBUG-then-`sftpd`-WARN). The server offers only `ssh-rsa` because `ServerVersion` is `"SSH-2.0-" + Banner` = `SSH-2.0-SFTPGo_0.9.5-dev` [sftpd/server.go:L144] and the auto-generated host key is RSA.


---

## Q3 — Web admin first access: root endpoint and redirect

**Direct answer.** A `GET` to the root path `/` returns **HTTP `301 Moved Permanently`** with the header **`Location: /web/users`** and a tiny HTML body. Following the redirect, `GET /web/users` returns **HTTP `200 OK`** and renders the users-management page **directly** — this early version has **no** admin login or setup interstitial.

**Command used:**

```
curl -sS -i http://127.0.0.1:8080/
```

**Observed output (verbatim; the `Date` value varies per run):**

```
HTTP/1.1 301 Moved Permanently
Content-Type: text/html; charset=utf-8
Location: /web/users
Date: Wed, 08 Jul 2026 03:26:36 GMT
Content-Length: 45

<a href="/web/users">Moved Permanently</a>.
```

**Command used (follow the redirect):**

```
curl -sS -i http://127.0.0.1:8080/web/users
```

**Observed output (verbatim, headers + start of body):**

```
HTTP/1.1 200 OK
Date: Wed, 08 Jul 2026 03:26:36 GMT
Content-Type: text/html; charset=utf-8
Transfer-Encoding: chunked

<!DOCTYPE html>
<html lang="en">
...
    <title>SFTPGo - Users</title>
```

**Grounding and cause → effect.** The router registers `router.Get("/", …)` [httpd/router.go:L36]; the handler calls `http.Redirect(w, r, webUsersPath, http.StatusMovedPermanently)` [httpd/router.go:L37]. `webUsersPath = "/web/users"` [httpd/httpd.go:L31] and `http.StatusMovedPermanently` is `301`, which is exactly the observed status and `Location`. There is **no authentication middleware** in front of `/web/users` in this version, so it renders directly.

**Bonus evidence (the chi structured request logger).** Each request also produces an INFO record with `sender:"httpd"` via the structured request logger — `StructuredLoggerEntry.Write` sets `Str("sender", "httpd")` [logger/request_logger.go:L57,L61]. Observed (verbatim; ids/addresses vary per run):

```
{"level":"info","time":"2026-07-08T03:26:36.088","sender":"httpd","method":"GET","proto":"HTTP/1.1","remote_addr":"127.0.0.1:55322","request_id":"...","uri":"http://127.0.0.1:8080/","user_agent":"curl/8.5.0","resp_status":301,"resp_size":45,"elapsed_ms":0}
{"level":"info","time":"2026-07-08T03:26:36.096","sender":"httpd","method":"GET","proto":"HTTP/1.1","remote_addr":"127.0.0.1:55326","request_id":"...","uri":"http://127.0.0.1:8080/web/users","user_agent":"curl/8.5.0","resp_status":200,"resp_size":11319,"elapsed_ms":0}
```

**Edge condition (inferred; transitional / before-state).** The HTTP admin depends on the `templates/` directory being resolvable from the config dir. `loadTemplates` [httpd/web.go:L78] wraps `template.ParseFiles(...)` in `template.Must(...)` [httpd/web.go:L95-98], which **panics** if the templates cannot be loaded. In the observed run, `templates/` and `static/` were copied into the run directory so the page rendered; the panic-on-missing-templates behavior is therefore **inferred from code** (the observed run had the assets present, so the panic path was not triggered — it is not claimed as observed).


---

## Q4 — Missing database on first startup

**Direct answer.** If the expected SQLite database (`sftpgo.db`) is missing, startup **aborts before any listener opens** — the server logs a provider `WARN` and a `service` `ERROR`, then exits **cleanly (exit code 0)** without opening port `2022` or `8080`. This build does **not** auto-create or migrate the schema; the database must be pre-initialized.

**Command used (no config file, no database, from an empty dir):**

```
/tmp/sftpgo_bin serve
echo "exit code: $?"
```

**Observed — complete `sftpgo.log` (verbatim, all four records; note there are NO subsequent startup lines — the run ends here):**

```
{"level":"info","time":"2026-07-08T03:24:41.317","sender":"service","connection_id":"","message":"starting SFTPGo 0.9.5-dev, config dir: ., config file: sftpgo, log max size: 10 log max backups: 5 log max age: 28 log verbose: true, log compress: false"}
{"level":"warn","time":"2026-07-08T03:24:41.317","sender":"config","connection_id":"","message":"error loading configuration file: Config File \"sftpgo\" Not Found in \"[/tmp/sftpgo_runs/run_a /root/.config/sftpgo /etc/sftpgo]\". Default configuration will be used: {SFTPD:{Banner:SFTPGo_0.9.5-dev BindPort:2022 BindAddress: IdleTimeout:15 MaxAuthTries:0 Umask:0022 UploadMode:0 Actions:{ExecuteOn:[] Command: HTTPNotificationURL:} Keys:[] IsSCPEnabled:false KexAlgorithms:[] Ciphers:[] MACs:[] LoginBannerFile: SetstatMode:0 EnabledSSHCommands:[md5sum sha1sum cd pwd]} ProviderConf:{Driver:sqlite Name:sftpgo.db Host: Port:5432 Username: Password:[redacted] SSLMode:0 ConnectionString: UsersTable:users ManageUsers:1 TrackQuota:1 PoolSize:0 UsersBaseDir: Actions:{ExecuteOn:[] Command: HTTPNotificationURL:} ExternalAuthProgram: ExternalAuthScope:0} HTTPDConfig:{BindPort:8080 BindAddress:127.0.0.1 TemplatesPath:templates StaticFilesPath:static BackupsPath:backups}}"}
{"level":"warn","time":"2026-07-08T03:24:41.317","sender":"sqlite","connection_id":"","message":"sqlite database file does not exists, please be sure to create and initialize a database before starting sftpgo"}
{"level":"error","time":"2026-07-08T03:24:41.317","sender":"service","connection_id":"","message":"error initializing data provider: stat sftpgo.db: no such file or directory"}
```

**Observed stdout (the curated console subset — note the console version omits the config dump):**

```
<ts> WRN error loading configuration file: Config File "sftpgo" Not Found in "[<cwd> /root/.config/sftpgo /etc/sftpgo]". Default configuration will be used.
<ts> ERR error initializing data provider: stat sftpgo.db: no such file or directory
```

These stdout lines are ANSI-colorized (greyed timestamp, red `WRN`/`ERR` tag, then the message). The observed **exit code is `0`**.

**Cause → effect.** `service.Start` calls `dataprovider.Initialize` [service/service.go:L69], which dispatches on the driver to `initializeSQLiteProvider` [dataprovider/dataprovider.go:L243,L259 → dataprovider/sqlite.go:L18]. `os.Stat(dbPath)` [dataprovider/sqlite.go:L27] fails for the missing file, so the provider logs `WARN sqlite database file does not exists, …` [dataprovider/sqlite.go:L29] and returns the `os.Stat` error. That error propagates up; `service.Start` logs `ERROR error initializing data provider: %v` [service/service.go:L71] — where `%v` is the raw stat error `stat sftpgo.db: no such file or directory` — and does `return err` [service/service.go:L73], **before** either goroutine or listener is launched. In `cmd/serve.go`, because `Start()` returned non-nil, `service.Wait()` is skipped [cmd/serve.go:L29-30]; the command returns and the process exits `0`.

**Edge condition (zero-byte database file).** A **different** message and a **different record set** result. When `sftpgo.db` exists but is 0 bytes, `os.Stat` succeeds, so the missing-file WARN is **not** emitted; instead the `fi.Size() == 0` branch returns `errors.New("sqlite database file is invalid, …")` [dataprovider/sqlite.go:L33-34] **without logging**, so only the `service` ERROR reports it. Observed (verbatim — note there are only THREE records and **no `sqlite` WARN**):

```
{"level":"error","time":"2026-07-08T03:25:15.891","sender":"service","connection_id":"","message":"error initializing data provider: sqlite database file is invalid, please be sure to create and initialize a database before starting sftpgo"}
```

(The command for the edge: `: > sftpgo.db` to create the 0-byte file, then `/tmp/sftpgo_bin serve`.) Explicitly contrasting the two cases: a **missing** file yields `WARN sqlite` + `ERROR service (stat … no such file or directory)`; a **zero-byte** file yields `ERROR service (sqlite database file is invalid …)` only.

**Transition to operational (before → after).** Bootstrapping the database with the canonical `.travis.yml` schema, then re-running, reaches readiness (this is the same run that feeds Q1). Command used (verbatim, the exact `.travis.yml:L14` DDL):

```
sqlite3 sftpgo.db 'CREATE TABLE "users" ("id" integer NOT NULL PRIMARY KEY AUTOINCREMENT, "username" varchar(255) NOT NULL UNIQUE, "password" varchar(255) NULL, "public_keys" text NULL, "home_dir" varchar(255) NOT NULL, "uid" integer NOT NULL, "gid" integer NOT NULL, "max_sessions" integer NOT NULL, "quota_size" bigint NOT NULL, "quota_files" integer NOT NULL, "permissions" text NOT NULL, "used_quota_size" bigint NOT NULL, "used_quota_files" integer NOT NULL, "last_quota_update" bigint NOT NULL, "upload_bandwidth" integer NOT NULL, "download_bandwidth" integer NOT NULL, "expiration_date" bigint NOT NULL, "last_login" bigint NOT NULL, "status" integer NOT NULL, "filters" TEXT NULL, "filesystem" text NULL);'
# then copy templates/ and static/ into the run dir and launch:
/tmp/sftpgo_bin serve
```

After bootstrapping, the provider initializes — the DEBUG record `sqlite database handle created, connection string: "file:sftpgo.db?cache=shared"` [dataprovider/sqlite.go:L37,L43] appears — and startup proceeds to the readiness sequence shown in Q1. Schema lineage: the base schema is `sql/sqlite/20190828.sql`, and the migrations `20191112.sql`, `20191230.sql`, and `20200116.sql` aggregate to the 21-column `users` table used by the `.travis.yml` bootstrap.


---

## Q5 — Default configuration revealed through logs

**Direct answer.** When no config file is found, `config.LoadConfig` emits a single `WARN` that **dumps the entire effective default configuration** (with the database password redacted), so the applied defaults are directly readable from `sftpgo.log`: SFTP binds `:2022` on all interfaces, HTTP binds `127.0.0.1:8080`, the SFTP idle timeout is `15` minutes, `MaxAuthTries:0`, `Umask:0022`, the data provider is `sqlite`/`sftpgo.db`, and log rotation is size `10` / backups `5` / age `28` — the last three are also echoed in the INFO startup banner.

**Command used:** the same config-less run as Q4 (`/tmp/sftpgo_bin serve` from an empty dir). The two authoritative records are (reproduced verbatim from the same capture as Q4):

1. **The INFO banner** [service/service.go:L60]:

```
{"level":"info","time":"2026-07-08T03:24:41.317","sender":"service","connection_id":"","message":"starting SFTPGo 0.9.5-dev, config dir: ., config file: sftpgo, log max size: 10 log max backups: 5 log max age: 28 log verbose: true, log compress: false"}
```

These banner values come from the CLI defaults [cmd/root.go:L33-40].

2. **The `WARN` config dump** [config/config.go:L152-153] — the full effective default struct:

```
{"level":"warn","time":"2026-07-08T03:24:41.317","sender":"config","connection_id":"","message":"error loading configuration file: Config File \"sftpgo\" Not Found in \"[/tmp/sftpgo_runs/run_a /root/.config/sftpgo /etc/sftpgo]\". Default configuration will be used: {SFTPD:{Banner:SFTPGo_0.9.5-dev BindPort:2022 BindAddress: IdleTimeout:15 MaxAuthTries:0 Umask:0022 UploadMode:0 Actions:{ExecuteOn:[] Command: HTTPNotificationURL:} Keys:[] IsSCPEnabled:false KexAlgorithms:[] Ciphers:[] MACs:[] LoginBannerFile: SetstatMode:0 EnabledSSHCommands:[md5sum sha1sum cd pwd]} ProviderConf:{Driver:sqlite Name:sftpgo.db Host: Port:5432 Username: Password:[redacted] SSLMode:0 ConnectionString: UsersTable:users ManageUsers:1 TrackQuota:1 PoolSize:0 UsersBaseDir: Actions:{ExecuteOn:[] Command: HTTPNotificationURL:} ExternalAuthProgram: ExternalAuthScope:0} HTTPDConfig:{BindPort:8080 BindAddress:127.0.0.1 TemplatesPath:templates StaticFilesPath:static BackupsPath:backups}}"}
```

**Concrete values with `file:line`, cross-checked against the `globalConf` literal in `init()` [config/config.go:L41]:**

- **SFTPD:** `BindPort:2022` [config/config.go:L46]; `BindAddress:` empty → `:2022`, all interfaces; `IdleTimeout:15` (minutes) [config/config.go:L48]; `MaxAuthTries:0` [config/config.go:L49]; `Umask:0022` [config/config.go:L50]; `Banner:SFTPGo_0.9.5-dev` (= `"SFTPGo_" + version`, version `0.9.5-dev` [utils/version.go:L3]); `EnabledSSHCommands:[md5sum sha1sum cd pwd]`.
- **ProviderConf:** `Driver:sqlite`, `Name:sftpgo.db`, `Port:5432`, `UsersTable:users`, `ManageUsers:1`, `TrackQuota:1`, `PoolSize:0`; `Password:[redacted]` — the redaction is applied by `getRedactedGlobalConf` [config/config.go:L134] (the real configured value is empty and is printed as `[redacted]`).
- **HTTPDConfig:** `BindPort:8080` [config/config.go:L88], `BindAddress:127.0.0.1`, `TemplatesPath:templates`, `StaticFilesPath:static`, `BackupsPath:backups`.
- **Config search path** shown in the message — `"[<cwd> /root/.config/sftpgo /etc/sftpgo]"` — reflects `LoadConfig` adding the config dir, `$HOME/.config/sftpgo`, `/etc/sftpgo`, and `.` [config/config.go:L145-151]; the first entry is the current working directory.

**HTTP timeouts / limits (set in code, not in the dump) for completeness:** `ReadTimeout` and `WriteTimeout` are each `300 * time.Second` [httpd/httpd.go:L94-95], and `MaxHeaderBytes = 1 << 16` (64 KB) [httpd/httpd.go:L96].

**`MaxAuthTries:0` nuance (inferred).** The value `0` is passed straight into `ssh.ServerConfig{MaxAuthTries: c.MaxAuthTries}` [sftpd/server.go:L127] (the field is declared at [sftpd/server.go:L44]); when it is `<= 0`, `golang.org/x/crypto/ssh` applies its built-in default of **6** auth attempts. This "capped at 6" behavior is **inferred** (it is the SSH library's default, not directly observed here); it is corroborated in-repo by the field's own doc comment — [sftpd/server.go:L43] states that when set to zero, "the number of attempts are limited to 6."

**Logger-level nuance (inferred, corroborated by the observed stream).** `logger.InitLogger` calls `logger.Level(level)` but **discards the return value** [logger/logger.go:L64]. Because zerolog's `Level` is non-mutating (it returns a *new* logger rather than mutating the receiver), the **main file logger is not level-filtered** — so DEBUG records appear in `sftpgo.log` regardless of the `log_verbose` flag. Only the console writer is level-filtered, via `.Level(level)` applied inline in `EnableConsoleLogger` [logger/logger.go:L80]. This is **inferred** from reading the code, but it is **corroborated by the observed stream**: the DEBUG `sqlite` / `httpd` / `service` / `utils` records appear in `sftpgo.log` in the Q1 capture even though only a curated subset reaches stdout.


---

## Appendix A — Verified `file:line` reference table

Every location below was validated against the source at branch `sftpgo_44634210287c` (commit `44634210`).

| Behavior | Location |
|---|---|
| Version constant `0.9.5-dev` | `utils/version.go:L3` |
| Entry point `main()` → `cmd.Execute()` | `main.go:L14` |
| CLI defaults (config dir/name, log file/size/backups/age/compress/verbose) | `cmd/root.go:L33-40` |
| `serve` → `Start()` then `Wait()` only if nil | `cmd/serve.go:L29-30` |
| Default `globalConf` literal | `config/config.go:L41` (init) |
| SFTPD defaults: `BindPort:2022` / `IdleTimeout:15` / `MaxAuthTries:0` / `Umask:0022` | `config/config.go:L46` / `L48` / `L49` / `L50` |
| HTTPD default `BindPort:8080` | `config/config.go:L88` |
| Config-not-found WARN + full dump; password redaction | `config/config.go:L152-153`; `L134` |
| Config search-path assembly | `config/config.go:L145-151` |
| Startup banner (INFO) | `service/service.go:L60` |
| `dataprovider.Initialize` call | `service/service.go:L69` |
| Abort-on-error (ERROR + return) | `service/service.go:L71-73` |
| SFTP goroutine / HTTP goroutine (`BindPort > 0`) | `service/service.go:L91-93` / `L100-104` |
| SFTP listener readiness line | `sftpd/server.go:L187` (after `net.Listen` `L178`) |
| Host-key auto-gen INFO / "Loading private key" | `sftpd/server.go:L422-423` / `L157` |
| `MaxAuthTries` field (doc comment "limited to 6" at `L43`) / wiring | `sftpd/server.go:L44` / `L127` |
| `PasswordCallback` / `authenticationError` wrap / `validatePasswordCredentials` | `sftpd/server.go:L128` / `L131` / `L448` |
| `CheckUserAndPass` call / `ConnectionFailedLog("password")` | `sftpd/server.go:L454` / `L457` |
| `AcceptInboundConnection` / `ssh.NewServerConn` / WARN / guard / `no_auth_tryed` | `sftpd/server.go:L243` / `L249` / `L251` / `L252` / `L253` |
| `ServerVersion` = `"SSH-2.0-" + Banner` | `sftpd/server.go:L144` |
| `initializeSQLiteProvider` / `logSender=sqlite` | `dataprovider/sqlite.go:L18` / `L21` |
| SQLite `os.Stat` / missing-file WARN / zero-byte "invalid" / handle DEBUG | `dataprovider/sqlite.go:L27` / `L29` / `L33-34` / `L37`,`L43` |
| provider "error authenticating user" WARN / `getUserByUsername` call | `dataprovider/sqlcommon.go:L35` / `L33` |
| `sql.ErrNoRows` → `RecordNotFoundError` | `dataprovider/sqlcommon.go:L295-296` |
| `RecordNotFoundError.Error()` → `"Not found: %s"` | `dataprovider/dataprovider.go:L205-206` |
| `Initialize` dispatch on driver | `dataprovider/dataprovider.go:L243`,`L259` |
| `webUsersPath="/web/users"` / HTTP init DEBUG / timeouts / `MaxHeaderBytes` / `ListenAndServe` | `httpd/httpd.go:L31` / `L76` / `L94-95` / `L96` / `L98` |
| Root `/` → 301 → `/web/users` | `httpd/router.go:L36-37` |
| `loadTemplates` panics if templates unresolvable (inferred edge) | `httpd/web.go:L78`,`L95-98` |
| `InitLogger` / `Level` return discarded / console `.Level` / `ConnectionFailedLog` | `logger/logger.go:L47` / `L64` / `L80` / `L175-183` |
| chi HTTP request logger (`sender:"httpd"`) | `logger/request_logger.go:L57`,`L61` |
| Go toolchain `1.13.x` / canonical DB bootstrap DDL | `.travis.yml:L8` / `L14` |
| CGO SQLite driver dependency / module Go version | `go.mod:L15` / `L3` |

> **Note on one AAP citation.** The `sql.ErrNoRows` → `RecordNotFoundError` mapping was verified to live in `dataprovider/sqlcommon.go:L295-296` (inside `getUserByUsername`'s row scan), which is where the observed `Not found:` prefix originates; that is the citation used above.

---

## Appendix B — Coverage recap

All five questions are answered above, each leading with a direct answer and backed by the exact command, the complete unedited observed output, and `file:line` grounding that names the responsible function/struct:

- **Q1** — exactly **two** ports open: SFTP `2022` on `[::]` and HTTP `8080` on `127.0.0.1`; the SFTP readiness signal is `server listener registered address: [::]:2022` [sftpd/server.go:L187], and the HTTP server has **no** dedicated "listening" line (its readiness signal is the `initializing HTTP server with config` DEBUG [httpd/httpd.go:L76]). The host-key auto-generation transition and the `/proc` socket-inode ownership proof are included.
- **Q2** — a non-existent user produces **three** ordered records (provider WARN → `connection_failed`/`password` → `sftpd` WARN) ending in `Not found: sql: no rows in result set`, with the client seeing `Permission denied (password,publickey)`; the modern-client host-key-rejection edge (two records, empty `username`, `login_type:"no_auth_tryed"`) is contrasted explicitly.
- **Q3** — `/` returns **301** with `Location: /web/users` and a 45-byte body; `/web/users` returns **200** and renders directly (no login/setup interstitial). The template-load panic is documented as an inferred edge.
- **Q4** — a missing database aborts startup with a `sqlite` WARN + a `service` ERROR (`stat sftpgo.db: no such file or directory`), exit code `0`, and **no** listener; the zero-byte-file edge yields the distinct `sqlite database file is invalid …` ERROR with no `sqlite` WARN; the transition to operational via the `.travis.yml` DDL is shown.
- **Q5** — the config-not-found WARN dumps the full effective default configuration (bind addresses, idle timeout, `MaxAuthTries`, umask, provider, log rotation), reproduced verbatim with each value cited to the `globalConf` literal; the `MaxAuthTries:0 → 6` cap and the discarded `logger.Level` return value are labelled inferred (and corroborated).

**Read-only compliance.** Only this document was created. No existing repository file was modified, and no code was added to the source tree; all build/run artifacts were kept outside the repository and removed afterward, leaving `git status --porcelain` empty. Only the observed behavior of this build (`0.9.5-dev`, branch `sftpgo_44634210287c`) is reported — no features from later SFTPGo releases (admin login/setup flows, FTP/WebDAV servers, automatic schema initialization) are projected onto it.

